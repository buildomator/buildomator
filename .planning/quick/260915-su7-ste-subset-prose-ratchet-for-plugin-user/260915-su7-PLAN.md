---
phase: quick-260915-su7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/maintenance/check-user-docs-prose.cjs
  - tests/drift-baseline.json
  - tests/user-docs-prose.test.cjs
  - bin/maintenance/pre-commit-drift-baseline.sh
  - .github/workflows/check-drift.yml
  - dist/bm (regenerated: carries the checker, the test, the baseline, the hook, the workflow)
autonomous: true
requirements: [QUICK-260915-su7]

must_haves:
  truths:
    - "node bin/maintenance/check-user-docs-prose.cjs run from the repo root against the real README.md and CHANGELOG.md exits 0 and prints Status: PASS, using a baseline captured from the real files (not hand-written zeros)"
    - "The same checker run inside a sandbox whose README contains an em-dash, a banned marketing word, or a cliche opener above a zero baseline exits 1 and names the offending category (em_en_dash, marketing_words, or cliche_openers) in the regression line"
    - "A banned word or dash placed inside a fenced ``` code block is stripped before counting and does not trip the ratchet"
    - "tests/drift-baseline.json gains a user_docs_prose section with numeric em_en_dash, marketing_words, cliche_openers counts, and every pre-existing top-level section (file_layout today) survives byte-for-byte as JSON (--write-baseline merges, never overwrites)"
    - "node tests/user-docs-prose.test.cjs exits 0 with every discrimination check passing (clean PASS, dash FAIL, word FAIL, cliche FAIL, fence-strip PASS, baseline-merge)"
    - "The pre-commit hook source and the check-drift workflow each run the prose checker next to the jargon checker; bash bin/maintenance/pre-commit-drift-baseline.sh exits 0 on the current tree"
    - "dist/bm is regenerated and node bin/build-bm.cjs --check passes; dist/bm/bin/maintenance/check-user-docs-prose.cjs, dist/bm/tests/user-docs-prose.test.cjs, and the user_docs_prose section in dist/bm/tests/drift-baseline.json all exist"
    - "Zero NEW CJS test failures vs the pre-change battery baseline (known baseline failures: context-monitor-hook-event, version-command); every line added across all three tasks (diffed from the plan-start base SHA captured before the first edit, not from HEAD) in bin/maintenance, tests, and .github contains no issue/PR-number refs, no literal U+2014/U+2013 characters, and none of the banned word"
  artifacts:
    - path: "bin/maintenance/check-user-docs-prose.cjs"
      provides: "counts-based prose ratchet (em_en_dash, marketing_words, cliche_openers) over README.md + CHANGELOG.md with fence stripping, --dry, --write-baseline, exit 0/1/2, repo-root guard"
      contains: "user_docs_prose"
      min_lines: 120
    - path: "tests/drift-baseline.json"
      provides: "user_docs_prose section captured from the real docs"
      contains: "user_docs_prose"
    - path: "tests/user-docs-prose.test.cjs"
      provides: "sandbox discrimination test for the prose checker"
      min_lines: 120
    - path: "bin/maintenance/pre-commit-drift-baseline.sh"
      provides: "PROSE_SCRIPT block mirroring the JARGON_SCRIPT block (no auto-regen)"
      contains: "check-user-docs-prose.cjs"
    - path: ".github/workflows/check-drift.yml"
      provides: "user-docs-prose job mirroring user-docs-jargon"
      contains: "user-docs-prose"
    - path: "dist/bm/bin/maintenance/check-user-docs-prose.cjs"
      provides: "generated copy in sync with source (build-bm --check proves it)"
  key_links:
    - from: "bin/maintenance/check-user-docs-prose.cjs"
      to: "tests/drift-baseline.json"
      via: "readBaseline/writeBaseline on the user_docs_prose section"
      pattern: "user_docs_prose"
    - from: "tests/user-docs-prose.test.cjs"
      to: "bin/maintenance/check-user-docs-prose.cjs"
      via: "spawnSync of the checker inside a mkdtemp sandbox with .git/ skills/ tests/ markers"
      pattern: "check-user-docs-prose\\.cjs"
    - from: "bin/maintenance/pre-commit-drift-baseline.sh"
      to: "bin/maintenance/check-user-docs-prose.cjs"
      via: "PROSE_SCRIPT invocation after the JARGON_SCRIPT block"
      pattern: "PROSE_SCRIPT"
    - from: ".github/workflows/check-drift.yml"
      to: "bin/maintenance/check-user-docs-prose.cjs and tests/user-docs-prose.test.cjs"
      via: "user-docs-prose job run steps"
      pattern: "check-user-docs-prose"
    - from: "bin/maintenance/, tests/, .github/"
      to: "dist/bm/"
      via: "node bin/build-bm.cjs regeneration"
      pattern: "build-bm"
    - from: "Task 1 (plan start)"
      to: "Task 3 hygiene gate"
      via: "base-sha.txt captured before the first edit; Task 3 diffs base..HEAD so lines committed by Tasks 1 and 2 are still scanned"
      pattern: "base-sha\\.txt"
---

<objective>
Add a second counts-based ratchet over the plugin's own user-facing docs (README.md, CHANGELOG.md): a machine-checkable anti-slop gate inspired by ASD-STE100 Simplified Technical English. It enforces the writing rules the user already declared globally (no em/en dashes, no marketing filler words, no cliche openers) which currently have zero enforcement. It is the sibling of the existing GSD-jargon ratchet and mirrors it in shape, wiring, and test style.

Purpose: turn three declared-but-unenforced prose rules into a CI and pre-commit gate that can only ratchet downward, without ever policing downstream user projects.

Output: bin/maintenance/check-user-docs-prose.cjs, a user_docs_prose section in tests/drift-baseline.json, tests/user-docs-prose.test.cjs, a PROSE_SCRIPT block in the pre-commit hook source, a user-docs-prose CI job, and a regenerated dist/bm.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/bm/4.6.0/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/bm/4.6.0/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@bin/maintenance/check-user-docs-jargon.cjs
@tests/user-docs-jargon.test.cjs
@bin/maintenance/pre-commit-drift-baseline.sh
@.github/workflows/check-drift.yml
@tests/drift-baseline.json

Planning-time facts the executor must not rediscover:

1. The jargon ratchet's baseline is NOT a separate file. It is a named section (user_docs_jargon) inside tests/drift-baseline.json, written by --write-baseline via read-merge-write so the sibling file_layout section survives. The prose ratchet uses the same file with a user_docs_prose section. There is no .user-docs-prose-baseline.json.

2. The task description assumed the docs currently have zero em-dashes. That is only true for README.md. A planning-time scan (fence-stripped, U+2014 or U+2013) found CHANGELOG.md holds about 155 em-dashes in historical release notes (for example the 2.40.x-era "Hotfix ... restores" lines, which carry an em-dash right after "Hotfix"), plus 4 "robustness" hits across both files, and 0 cliche openers. The baseline is therefore NOT near-zero for em_en_dash. That is fine and is the honest number: the ratchet freezes it there, any new dash fails, and each cleanup that lowers it can be locked in with --write-baseline. Do NOT rewrite CHANGELOG history to reach zero (the bm build deliberately preserves CHANGELOG verbatim for the same reason). Do NOT hand-write the baseline; run --write-baseline against the real files and report the numbers it captured.

3. long_sentences is OMITTED. A planning-time splitter (split on sentence punctuation followed by whitespace and a capital, skip list/heading/table lines) flagged 18 README + 46 CHANGELOG "sentences" and its top hits were an HTML <p align> block, a version string ("v2.42.0" splits mid-token), and paragraphs dense with backticked paths. That is a flaky metric, and the description says to omit rather than ship noise. The three remaining patterns are all exact, word-boundary, high-precision matches.

4. dist/bm carries byte copies (with a /gsd: to /bm: command-ref rewrite on text files) of bin/maintenance/*, tests/*, tests/drift-baseline.json, and .github/workflows/check-drift.yml. Every file this plan touches lands in dist/bm, so node bin/build-bm.cjs --check FAILS until dist/bm is regenerated. The new checker must not contain the token /gsd: so its dist copy stays byte-identical (the jargon checker's copy differs only because its header mentions /gsd:foo).

5. The full CJS battery is `for f in tests/*.test.cjs; do node "$f" >/dev/null 2>&1 || echo "FAIL $f"; done`. Known pre-existing failures: context-monitor-hook-event, version-command. Capture the baseline BEFORE any edit.

6. The checker needs the em-dash and en-dash characters (U+2014 and U+2013) in its regex. Write them as backslash-u escapes (backslash, lowercase u, then the four hex digits 2014 or 2013) inside the RegExp character class, and as the same backslash-u escapes inside the JavaScript string fixtures in the test, so no literal dash character appears anywhere in source. This plan file follows the same rule: it names the code points and never embeds the characters. The hygiene grep in Task 3 then runs over ALL added lines with no exclusions.

7. Per-task auto-commit means Tasks 1 and 2 are already committed by the time Task 3 runs, so a plain `git diff HEAD` in Task 3 would scan an empty window. Task 1 therefore records the plan-start commit SHA (base-sha.txt) and a copy of the pre-edit tests/drift-baseline.json (pre-baseline.json) in the quick-task directory before its first edit. Task 1's verify compares the post-edit baseline against pre-baseline.json (not a hardcoded number), and Task 3's hygiene gate diffs base-sha..HEAD so every line added by all three tasks is scanned. Both capture files live under .planning/ (gitignored) and are never committed.

Generated-code hygiene (project CLAUDE.md): comments describe behavior and purpose only. A header comment naming ASD-STE100 as the inspiration and stating the plugin-self-only scope is fine (it describes what the code is for). NO phase/plan/quick IDs, NO GSD bookkeeping, NO upstream issue/PR numbers in product source, tests, or commit messages. Never the banned word that the jargon checker header happens to use on its line 17 (do not copy that header wording). NO version bump, NO CHANGELOG entry, NO README features-table row (this is plugin-self infrastructure, not a downstream capability), NO new /bm: command. .planning/ is gitignored: run on the MAIN tree (no worktree), commit product files only. No Co-Authored-By lines.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the prose checker and capture an honest baseline</name>
  <files>bin/maintenance/check-user-docs-prose.cjs, tests/drift-baseline.json</files>
  <behavior>
    - Run from repo root on the real docs after --write-baseline: exit 0, stdout contains "Status: PASS"
    - Run in a sandbox with zero baseline and a README holding "seamless", an em-dash, and "when it comes to": exit 1, stdout contains "FAIL" and all three category names
    - Run in a sandbox where the only offending text sits inside a ``` fence: exit 0
    - --write-baseline on a drift-baseline.json that already has file_layout and user_docs_jargon sections: both sections preserved, user_docs_prose added with numeric counts and a generated_at date
    - Run outside a repo root (no .git/ or skills/): exit 2 with the usage error on stderr
  </behavior>
  <action>
    First, before ANY edit, capture three things into the quick-task directory (all gitignored, never committed), in this order:
    cd /Users/jnuyens/src/gsd-plugin && Q=.planning/quick/260915-su7-ste-subset-prose-ratchet-for-plugin-user && git rev-parse HEAD > "$Q/base-sha.txt" && cp tests/drift-baseline.json "$Q/pre-baseline.json" && for f in tests/*.test.cjs; do node "$f" >/dev/null 2>&1 || echo "FAIL $f"; done > "$Q/baseline-cjs.txt"
    base-sha.txt is the plan-start commit that Task 3's hygiene gate diffs against (context note 7). pre-baseline.json is the pre-edit drift baseline that this task's verify compares against, so the "sibling sections survive" assertion tracks whatever the file held at plan start rather than a number typed into the plan.

    Then create bin/maintenance/check-user-docs-prose.cjs by copying the structure of check-user-docs-jargon.cjs function for function (args parsing, repo-root guard requiring .git/ and skills/, TARGETS = README.md + CHANGELOG.md, stripFencedCodeBlocks with the same non-greedy [\s\S] regex, countMatches with lastIndex reset, scanFile, scanAll with per-file null for missing targets, readBaseline from tests/drift-baseline.json, writeBaseline that read-merges into the user_docs_prose key, report, main with identical --dry / --write-baseline / no-baseline / regression / reduced branches, exit 0/1/2). Change only:

    - Header comment: name the file, say it is a counts-based ratchet against AI-flavored slop in plugin-self user-facing docs, inspired by ASD-STE100 Simplified Technical English (a machine-checkable subset only: no semantic rules like triads or parallelism), scope PLUGIN SELF ONLY, list the three patterns, note fenced blocks are stripped and inline code spans are not, usage lines, exit codes. Do not mention /gsd: anywhere in the file.
    - PATTERNS object with exactly three keys, in this order:
      em_en_dash: a global RegExp character class holding the two code points U+2014 (em-dash) and U+2013 (en-dash), each written as a backslash-u escape with its four hex digits, never as the literal character (context note 6).
      marketing_words: global, case-insensitive, word-bounded alternation covering the base words and their obvious inflections: seamless(ly)?, robust(ly|ness)?, leverag(e|es|ed|ing), ensur(e|es|ed|ing), systematic(ally)?, powerful(ly)?, elevat(e|es|ed|ing), delv(e|es|ed|ing). Word boundaries on both sides so "insure" or "unleveraged" style substrings do not match.
      cliche_openers: global, case-insensitive, word-bounded alternation of exactly these six fixed phrases: "in today's fast-paced world", "in the world of", "when it comes to", "at the end of the day", "it's worth noting that", "needless to say". Accept both the straight apostrophe and the curly right single quote (U+2019, as a backslash-u escape) in the two phrases that carry one. Keep the list to these six; precision over recall.
    - Baseline section key: user_docs_prose. The note string explains it is a prose-slop ratchet over README.md + CHANGELOG.md, fenced blocks stripped, and that em_en_dash counts historical CHANGELOG entries that are frozen rather than rewritten; regenerate with --write-baseline only after a deliberate cleanup lowers a count.
    - Report header "user-docs prose scan:"; FAIL message "Status: FAIL, prose slop count regressed beyond baseline" followed by the same three-line guidance (regenerate only if intentional, otherwise fix and rerun) pointing at check-user-docs-prose.cjs; PASS and REDUCED messages reworded for prose.
    - No literal em-dash, en-dash, or the jargon header's banned word anywhere in the file.

    Then capture the baseline from the real docs: node bin/maintenance/check-user-docs-prose.cjs --write-baseline. Read back tests/drift-baseline.json and confirm every top-level section that pre-baseline.json held is still present with identical JSON, and user_docs_prose holds the real counts (expected magnitude from planning: em_en_dash in the low hundreds, all from CHANGELOG.md; marketing_words a handful; cliche_openers 0). Record the exact captured numbers in the SUMMARY. Then run the checker with no flags and confirm exit 0 with Status: PASS.
  </action>
  <verify>
    <automated>cd /Users/jnuyens/src/gsd-plugin && Q=.planning/quick/260915-su7-ste-subset-prose-ratchet-for-plugin-user && test -s "$Q/base-sha.txt" && git cat-file -e "$(cat "$Q/base-sha.txt")^{commit}" && test -s "$Q/pre-baseline.json" && test -s "$Q/baseline-cjs.txt" && node bin/maintenance/check-user-docs-prose.cjs | tail -6 && node -e 'const pre=require("./'"$Q"'/pre-baseline.json"); const b=require("./tests/drift-baseline.json"); for (const k of Object.keys(pre)) { if (JSON.stringify(pre[k])!==JSON.stringify(b[k])) throw new Error("pre-existing section clobbered: "+k); } if (Object.keys(pre).includes("user_docs_prose")) throw new Error("pre-baseline already had user_docs_prose; capture happened after the edit"); const p=b.user_docs_prose; for (const k of ["em_en_dash","marketing_words","cliche_openers"]) if (typeof p[k]!=="number") throw new Error("missing "+k); console.log("sibling sections intact:", Object.keys(pre).join(","), "| baseline captured:", JSON.stringify(p))' && S=$(mktemp -d) && mkdir -p "$S/.git" "$S/skills" "$S/tests" && printf '# X\n\nA seamless flow \xe2\x80\x94 when it comes to it.\n\n```\nrobust \xe2\x80\x94 inside fence\n```\n' > "$S/README.md" && printf '# Changelog\n' > "$S/CHANGELOG.md" && printf '{"user_docs_prose":{"em_en_dash":0,"marketing_words":0,"cliche_openers":0}}\n' > "$S/tests/drift-baseline.json" && out=$(cd "$S" && node /Users/jnuyens/src/gsd-plugin/bin/maintenance/check-user-docs-prose.cjs; echo "rc=$?") && echo "$out" | tail -9 && echo "$out" | grep -q 'rc=1' && echo "$out" | grep -q 'em_en_dash 1 > 0' && echo "$out" | grep -q 'marketing_words 1 > 0' && echo "$out" | grep -q 'cliche_openers 1 > 0' && rc2=$(cd "$S/skills" && node /Users/jnuyens/src/gsd-plugin/bin/maintenance/check-user-docs-prose.cjs >/dev/null 2>&1; echo $?) && [ "$rc2" -eq 2 ] && echo "slop doc exit 1 with all three categories (fenced hits not counted); non-root exit 2; real docs exit 0" && rm -rf "$S" && ! grep -n "gsd:" bin/maintenance/check-user-docs-prose.cjs</automated>
  </verify>
  <done>base-sha.txt, pre-baseline.json, and baseline-cjs.txt exist in the quick-task directory and were captured before the first edit; checker exists and runs clean (exit 0, PASS) on the real README+CHANGELOG against a baseline captured by --write-baseline (real numbers reported); every top-level section from pre-baseline.json is present with identical JSON in the post-edit file; the sandbox slop doc exits 1 naming em_en_dash, marketing_words, and cliche_openers each at exactly 1 (so the fenced "robust" and fenced dash were stripped); running from a non-root dir exits 2; the file contains no /gsd: token, no literal dash characters, and no banned word.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Discrimination test plus pre-commit and CI wiring</name>
  <files>tests/user-docs-prose.test.cjs, bin/maintenance/pre-commit-drift-baseline.sh, .github/workflows/check-drift.yml</files>
  <behavior>
    - check 1: clean README/CHANGELOG + zero user_docs_prose baseline: exit 0, stdout has PASS
    - check 2: README gains one em-dash and one en-dash (both fixtures built from the U+2014 and U+2013 backslash-u escapes, never the literal characters): exit 1, stdout has FAIL and em_en_dash
    - check 3: README gains "a seamless, robust workflow that leverages": exit 1, stdout has FAIL and marketing_words
    - check 4: README gains "When it comes to setup, at the end of the day": exit 1, stdout has FAIL and cliche_openers
    - check 5: README gains a ```bash fence containing "seamless", an em-dash (U+2014 escape), and "leverage", with nothing outside the fence: exit 0
    - check 6: --write-baseline on a drift-baseline.json that has file_layout {total_dangling:100,...} and user_docs_jargon {planning_paths:0,...}: exit 0, both sections preserved with original values, user_docs_prose added with numeric em_en_dash
  </behavior>
  <action>
    Create tests/user-docs-prose.test.cjs by mirroring tests/user-docs-jargon.test.cjs exactly: same withSandbox helper (mkdtemp with a gsd-prose-test- prefix, .git/ skills/ tests/ markers, rmSync in finally), same runDetector spawnSync wrapper with a 5000 ms timeout, same check/assert accumulator and the final "user-docs-prose detector: N/M checks passed" summary with per-check PASS/FAIL lines and exit 1 on any failure. DETECTOR resolves to bin/maintenance/check-user-docs-prose.cjs. A shared ZERO_BASELINE constant holds { user_docs_prose: { em_en_dash: 0, marketing_words: 0, cliche_openers: 0 } }. Implement the six checks in the behavior block. Every dash in a fixture is written as a JavaScript backslash-u escape for U+2014 or U+2013 (context note 6); the file must contain no literal dash characters. Run node tests/user-docs-prose.test.cjs and confirm 6/6.

    Edit bin/maintenance/pre-commit-drift-baseline.sh: directly after the JARGON_SCRIPT block (before the final exit 0) add a PROSE_SCRIPT block that is a copy of the JARGON_SCRIPT block with the script path changed to bin/maintenance/check-user-docs-prose.cjs, the error line "ERROR: user-docs prose ratchet regressed.", and the guidance rewritten for prose: fix the wording (remove the dash, replace the filler word, drop the opener) and rerun; only if a count was deliberately lowered, regenerate with --write-baseline and git add tests/drift-baseline.json; override with git commit --no-verify. Precede it with a short comment block in the same style as the jargon one explaining that, like the jargon ratchet, it never auto-regens because the pause is the point. Use plain hyphens (or the same U+2500 box character the jargon header line uses) for the section rule; never U+2014/U+2013. Keep the file executable.

    Edit .github/workflows/check-drift.yml: directly after the user-docs-jargon job add a user-docs-prose job with name "User-docs prose ratchet", the same checkout@v4 + setup-node@v4 node 22 steps, then "Run user-docs prose detector" (node bin/maintenance/check-user-docs-prose.cjs) and "Run user-docs prose discrimination test" (node tests/user-docs-prose.test.cjs). Two-space YAML indentation identical to the jargon job.

    Then run the hook source directly (bash bin/maintenance/pre-commit-drift-baseline.sh) from the repo root: it must exit 0 on the current tree since both ratchets pass. Also prove the new block fires: temporarily point a copy of the hook at a sandbox is over-engineering; instead confirm wiring by grep for PROSE_SCRIPT and check-user-docs-prose.cjs in the hook, and validate the YAML parses by loading it with node and checking jobs['user-docs-prose'].steps has 4 entries (use a tiny inline parser-free check: count the two run lines under the new job with grep -A).
  </action>
  <verify>
    <automated>cd /Users/jnuyens/src/gsd-plugin && node tests/user-docs-prose.test.cjs && bash -n bin/maintenance/pre-commit-drift-baseline.sh && test -x bin/maintenance/pre-commit-drift-baseline.sh && bash bin/maintenance/pre-commit-drift-baseline.sh && echo "hook exit 0 on current tree" && grep -c 'PROSE_SCRIPT' bin/maintenance/pre-commit-drift-baseline.sh | xargs -I{} sh -c '[ {} -ge 3 ]' && grep -q 'check-user-docs-prose.cjs --write-baseline' bin/maintenance/pre-commit-drift-baseline.sh && grep -q '^  user-docs-prose:$' .github/workflows/check-drift.yml && grep -A 12 '^  user-docs-prose:$' .github/workflows/check-drift.yml | grep -c 'run: node' | xargs -I{} sh -c '[ {} -eq 2 ]' && grep -A 12 '^  user-docs-prose:$' .github/workflows/check-drift.yml | grep -q 'bin/maintenance/check-user-docs-prose.cjs' && grep -A 12 '^  user-docs-prose:$' .github/workflows/check-drift.yml | grep -q 'tests/user-docs-prose.test.cjs' && node bin/maintenance/check-user-docs-jargon.cjs >/dev/null && echo "jargon sibling still PASS; prose test, hook, and workflow wiring verified"</automated>
  </verify>
  <done>node tests/user-docs-prose.test.cjs reports 6/6 checks passed and exits 0; the hook source parses, stays executable, exits 0 on the real tree, and contains the PROSE_SCRIPT block with the --write-baseline guidance; check-drift.yml has a user-docs-prose job with exactly two run steps (detector + discrimination test); the jargon ratchet still passes.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate dist/bm and run the hard gates</name>
  <files>dist/bm (regenerated)</files>
  <action>
    Run node bin/build-bm.cjs to regenerate dist/bm, then node bin/build-bm.cjs --check which must PASS. Confirm the five carried files exist in dist/bm (checker, test, baseline with user_docs_prose, hook, workflow) and that the checker copy is byte-identical to source (cmp), which holds because the file contains no /gsd: token.

    Then the gates, all in the verify command:
      a. Full CJS battery, zero NEW FAIL lines vs baseline-cjs.txt (context-monitor-hook-event and version-command are the known baseline).
      b. Hygiene greps over ALL lines added since the plan-start base SHA. Tasks 1 and 2 are already committed by now, so the diff window is `git diff "$(cat base-sha.txt)"..HEAD` for bin/maintenance, tests, and .github, plus the uncommitted working tree on top (a second `git diff HEAD` over the same paths, so anything Task 3 itself touched before its commit is included too). Added lines only (lines starting with a single plus, excluding the +++ file header), so pre-existing issue-number refs already in check-drift.yml are not flagged. No exclusions inside the added lines, since the dash characters are escapes. Three checks, each must be empty: no issue/PR refs (a hash followed by three or more digits), no literal U+2014/U+2013, no banned word.
      c. Both doc ratchets and the file-layout detector pass on the tree (the new .cjs has no @-include refs so file-layout counts should not move; if the pre-commit hook auto-regens the file_layout section on commit that is its documented behavior, just make sure user_docs_prose survives).

    Commit product files only (bin/maintenance/check-user-docs-prose.cjs, tests/user-docs-prose.test.cjs, tests/drift-baseline.json, bin/maintenance/pre-commit-drift-baseline.sh, .github/workflows/check-drift.yml, dist/bm) with a message like "feat(maintenance): add user-docs prose ratchet (STE-inspired anti-slop gate over README and CHANGELOG)". No Co-Authored-By trailer, no dashes, no issue numbers, no version bump, no CHANGELOG entry. The installed pre-commit hook will run both ratchets during the commit; both must pass. Never commit base-sha.txt, pre-baseline.json, or baseline-cjs.txt (they sit under gitignored .planning/).
  </action>
  <verify>
    <automated>cd /Users/jnuyens/src/gsd-plugin && Q=.planning/quick/260915-su7-ste-subset-prose-ratchet-for-plugin-user && BASE=$(cat "$Q/base-sha.txt") && git cat-file -e "$BASE^{commit}" && [ "$BASE" != "$(git rev-parse HEAD)" ] && node bin/build-bm.cjs >/dev/null && node bin/build-bm.cjs --check && cmp bin/maintenance/check-user-docs-prose.cjs dist/bm/bin/maintenance/check-user-docs-prose.cjs && test -f dist/bm/tests/user-docs-prose.test.cjs && test -f dist/bm/bin/maintenance/pre-commit-drift-baseline.sh && grep -q 'user-docs-prose' dist/bm/.github/workflows/check-drift.yml && node -e 'const p=require("./dist/bm/tests/drift-baseline.json").user_docs_prose; if(typeof p.em_en_dash!=="number") process.exit(1); console.log("dist/bm baseline user_docs_prose:", JSON.stringify(p))' && for f in tests/*.test.cjs; do node "$f" >/dev/null 2>&1 || echo "FAIL $f"; done > /private/tmp/claude-503/-Users-jnuyens-src-gsd-plugin/b17e3628-eb00-4942-aa8c-5af54ba313a9/scratchpad/su7-after.txt; new=$(comm -13 <(sort "$Q/baseline-cjs.txt") <(sort /private/tmp/claude-503/-Users-jnuyens-src-gsd-plugin/b17e3628-eb00-4942-aa8c-5af54ba313a9/scratchpad/su7-after.txt)); if [ -n "$new" ]; then echo "NEW CJS FAILURES:"; echo "$new"; exit 1; fi; added=$( { git diff "$BASE"..HEAD -- bin/maintenance tests .github; git diff HEAD -- bin/maintenance tests .github; } | grep '^+' | grep -v '^+++' || true); cnt=$(printf '%s\n' "$added" | grep -c . || true); [ "$cnt" -gt 100 ] || { echo "hygiene window too small ($cnt added lines): base SHA did not precede the Task 1/2 commits"; exit 1; }; h1=$(echo "$added" | grep -nE '#[0-9]{3,}' || true); h2=$(echo "$added" | perl -CSD -ne 'print if /\x{2014}|\x{2013}/' || true); h3=$(echo "$added" | grep -i "can""onical" || true); if [ -n "$h1$h2$h3" ]; then echo "HYGIENE HIT:"; echo "$h1"; echo "$h2"; echo "$h3"; exit 1; fi; echo "hygiene scanned $cnt added lines since $BASE: clean"; node bin/maintenance/check-user-docs-prose.cjs >/dev/null && node bin/maintenance/check-user-docs-jargon.cjs >/dev/null && node bin/maintenance/check-file-layout.cjs >/dev/null && echo "GATES PASS: build-bm check, battery (zero new), hygiene empty over base..HEAD plus worktree, all three detectors green"</automated>
  </verify>
  <done>node bin/build-bm.cjs --check passes after regeneration; the checker's dist copy is byte-identical and the test, hook, workflow, and user_docs_prose baseline are present in dist/bm; the battery diff vs baseline-cjs.txt shows zero NEW FAIL lines; the hygiene window (base-sha..HEAD plus working tree, added lines only, bin/maintenance + tests + .github) is non-trivial (more than 100 added lines, proving it covers Tasks 1 and 2) and all three hygiene greps over it are empty; prose, jargon, and file-layout detectors all exit 0; product files committed with no attribution trailer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo docs to regex scanner | README.md/CHANGELOG.md content (author-controlled, in-repo) is read by a Node script during pre-commit and CI |
| baseline JSON to ratchet | tests/drift-baseline.json values decide pass/fail; committed and reviewed like any source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-su7-01 | Denial of Service | PATTERNS regexes in check-user-docs-prose.cjs | mitigate | All three patterns are linear literal alternations with word boundaries and no nested quantifiers, so no catastrophic backtracking on adversarial doc content |
| T-su7-02 | Tampering | tests/drift-baseline.json user_docs_prose | accept | A contributor can raise the baseline to hide slop, but the file is committed and diffed in review; the hook deliberately refuses to auto-regen so any raise is a visible explicit act |
| T-su7-03 | Tampering | pre-commit hook source | accept | Same shell block shape as the existing jargon block; runs only repo-local scripts by fixed relative path after cd to git toplevel |
| T-su7-SC | Tampering | package installs | accept | No new dependencies; checker and test use Node built-ins only (fs, path, os, child_process) |
</threat_model>

<verification>
- node bin/maintenance/check-user-docs-prose.cjs exits 0 with Status: PASS on the real docs; baseline numbers in tests/drift-baseline.json were captured by --write-baseline, not typed.
- Sandbox slop doc trips exit 1 and names all three categories; fenced hits are not counted; non-root run exits 2.
- node tests/user-docs-prose.test.cjs: 6/6 PASS.
- Pre-commit hook source and check-drift.yml both invoke the prose checker beside the jargon checker.
- node bin/build-bm.cjs --check PASS after regen; the five carried files exist in dist/bm.
- Full battery diff vs baseline-cjs.txt shows zero NEW FAIL lines.
- Every pre-existing top-level section of tests/drift-baseline.json (compared against pre-baseline.json captured before the first edit) is intact after --write-baseline.
- Added lines since base-sha.txt (base..HEAD plus working tree) in bin/maintenance, tests, .github: no #NNN refs, no literal U+2014/U+2013, no banned word; the window is proven non-empty.
</verification>

<success_criteria>
- The plugin's own README.md and CHANGELOG.md are guarded by a second ratchet that fails pre-commit and CI when em/en dashes, the eight marketing filler words, or the six cliche openers increase above a baseline captured from the real files.
- The gate is proven by running it, not by reading it: real-docs exit 0, slop-doc exit 1, discrimination test green.
- dist/bm stays in sync (build-bm --check), the CJS battery has no new failures, and the change carries no bookkeeping, dashes, or issue numbers into product source (hygiene proven over the full plan window, not an empty post-commit diff).
- No version bump, no CHANGELOG entry, no README feature row, no new command.
</success_criteria>

<output>
Create `.planning/quick/260915-su7-ste-subset-prose-ratchet-for-plugin-user/260915-su7-SUMMARY.md` when done. Record the exact baseline numbers --write-baseline captured (em_en_dash, marketing_words, cliche_openers, and which file they came from), the 6/6 test result, the build-bm --check result, the battery diff result, and the number of added lines the hygiene gate scanned.
</output>
