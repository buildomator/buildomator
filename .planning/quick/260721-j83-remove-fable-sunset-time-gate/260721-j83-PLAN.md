---
phase: quick-260721-j83
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/core.cjs
  - sdk/src/query/config-query.ts
  - bin/lib/config-schema.cjs
  - sdk/src/query/config-schema.ts
  - tests/fable-sunset.test.cjs
  - README.md
  - sdk/dist/cli.js
  - dist/bm
autonomous: true
requirements: [J83-FABLE-SUNSET-REMOVE]

must_haves:
  truths:
    - "FABLE_SUNSET_DATE, applyFableSunset, fableAvailable, readFableKnob, and GSD_FABLE_SUNSET_NOW no longer exist in bin/lib/core.cjs or sdk/src/query/config-query.ts"
    - "fable.mode and fable.until are gone from both config-schema files"
    - "Resolving the fable tier returns fable / claude-fable-5 regardless of date; GSD_FABLE_SUNSET_NOW set to a future date is a no-op (proven by running node against both resolvers)"
    - "fable stays a valid tier: 'fable' remains in VALID_TIERS and MODEL_PROFILES, and tests/fable-tier.test.cjs still passes"
    - "No empty test file is left behind; tests/fable-sunset.test.cjs is deleted"
    - "node bin/build-bm.cjs --check passes and the FABLE_SUNSET gate is gone from dist/bm code (core.cjs, sdk/dist/cli.js, sdk/src/query/config-query.ts) and dist/bm/tests"
  artifacts:
    - path: "bin/lib/core.cjs"
      provides: "CJS model resolver with the fable date-gate removed; requestedTier flows straight through as tier"
      contains: "function resolveModelInternal"
    - path: "sdk/src/query/config-query.ts"
      provides: "SDK model resolver mirroring CJS with the fable date-gate removed"
      contains: "export const resolveModel"
    - path: "bin/lib/config-schema.cjs"
      provides: "CJS config key set without fable.mode / fable.until"
    - path: "sdk/src/query/config-schema.ts"
      provides: "SDK config key set without fable.mode / fable.until (parity with CJS)"
  key_links:
    - from: "sdk/src/query/config-query.ts"
      to: "sdk/dist/cli.js"
      via: "npm --prefix sdk run build"
      pattern: "resolveModel"
    - from: "source tree"
      to: "dist/bm"
      via: "npm run build:bm regenerates the bm plugin twin"
      pattern: "build-bm"
---

<objective>
Remove the Claude Fable 5 sunset / time-gate from BOTH model resolvers so `fable` becomes an ordinary always-available model tier. Fable 5 is now a permanent part of the Claude Max plan; the sunset mechanism is obsolete and actively harmful — `FABLE_SUNSET_DATE = 2026-07-19` is in the past, so `applyFableSunset` silently downgrades every `fable` request to `opus` in the default `auto` mode. Verified live on the repo-local paths: `node sdk/dist/cli.js query init.quick` returns `planner_model: opus` (should be `fable` under the repo's `quality` profile), and CJS `resolveModelForTier(cwd, 'gsd-planner', 0)` returns `opus`.

Purpose: `fable` must resolve straight to its catalog model (claude-fable-5) with no date dependency, keeping the CJS and SDK twins in lock-step.
Output: Both resolvers and both config schemas cleaned of the gate; the sunset test deleted; README sunset prose removed; `sdk/dist` rebuilt and `dist/bm` regenerated; full test battery + live resolution checks green.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.1.1/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.1.1/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Extracted from the codebase. The gate lives at these exact sites — remove, do not refactor beyond what is stated. -->

bin/lib/core.cjs:
  ~1400-1414  block comment describing the fable sunset  -> DELETE
  ~1415       const FABLE_SUNSET_DATE = '2026-07-19';      -> DELETE
  ~1417-1432  readFableKnob(cwd)                            -> DELETE (incl. its comment)
  ~1434-1454  fableAvailable(now, knob)                     -> DELETE
  ~1456-1458  applyFableSunset(tier, now, knob)             -> DELETE
  ~1496-1500  const requestedTier = ( ... )                 -> RENAME to `const tier = ( ... )`
  ~1502-1505  the "Fable availability" comment + `const tier = applyFableSunset(requestedTier, undefined, readFableKnob(cwd));`
                                                            -> DELETE both lines (the renamed `tier` above now feeds step 3 directly)
  ~2132-2135  module.exports: applyFableSunset, fableAvailable, readFableKnob, FABLE_SUNSET_DATE
                                                            -> DELETE these four export lines
  KEEP: 'fable' in VALID_TIERS (~1487), all MODEL_PROFILES fable entries, resolveModelForTier.

sdk/src/query/config-query.ts:
  ~177-188  block comment (fable sunset mirror)            -> DELETE
  ~189      const FABLE_SUNSET_DATE = '2026-07-19';         -> DELETE
  ~191-205  readFableKnob(projectDir, workstream)           -> DELETE (incl. its comment)
  ~207-221  fableAvailable(now, knob)                        -> DELETE (incl. GSD_FABLE_SUNSET_NOW read)
  ~223-225  applyFableSunset(tier, knob)                     -> DELETE
  ~274-278  "Fable availability" comment + `const fableKnob = readFableKnob(...)` + `const alias = applyFableSunset(agentModels[profile] || agentModels['balanced'] || 'sonnet', fableKnob);`
                                                            -> replace with `const alias = agentModels[profile] || agentModels['balanced'] || 'sonnet';`
  ~283     `const tier = applyFableSunset(typeof phaseTier === 'string' ? phaseTier : alias, fableKnob);`
                                                            -> replace with `const tier = typeof phaseTier === 'string' ? phaseTier : alias;`
  KEEP: everything else (override path, configExists path, inherit path, runtime tier, resolve_model_ids, final `return { data: { model: alias, profile } }`).

bin/lib/config-schema.cjs ~70-71 and sdk/src/query/config-schema.ts ~72-73:
  the two adjacent lines  'fable.mode',  and  'fable.until',  -> DELETE from BOTH (keep them in parity).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture integration baseline, then rip the gate out of both resolvers and both config schemas</name>
  <files>bin/lib/core.cjs, sdk/src/query/config-query.ts, bin/lib/config-schema.cjs, sdk/src/query/config-schema.ts</files>
  <action>
FIRST, before editing anything, capture the pre-change SDK integration baseline (there is a known ~13-failure baseline from the archived-v4.1 CJS/SDK milestone divergence — the post-change failing set must be a strict subset of this): run `npm --prefix sdk run test:integration 2>&1 | tee /tmp/fable-int-baseline.txt` and note the set of failing test names. This snapshot is the golden baseline for the zero-new-failures gate in Task 2.

THEN make the removals exactly as spelled out in the `<interfaces>` block above:

1. bin/lib/core.cjs — delete the sunset comment, `FABLE_SUNSET_DATE`, `readFableKnob`, `fableAvailable`, `applyFableSunset`. In `resolveModelInternal`, rename the `const requestedTier = (...)` binding to `const tier = (...)` and delete the two-line "Fable availability" comment + `applyFableSunset(...)` reassignment so the computed `tier` flows straight into step 3 (`if (config.runtime && ...)`). Remove the four names from `module.exports`. KEEP `'fable'` in `VALID_TIERS` and every `MODEL_PROFILES` fable entry — fable stays a real tier; only the date-gate goes. Remove all `GSD_FABLE_SUNSET_NOW` handling (it lived only inside `fableAvailable`).

2. sdk/src/query/config-query.ts — mirror the exact removal: delete the sunset comment, `FABLE_SUNSET_DATE`, `readFableKnob`, `fableAvailable`, `applyFableSunset`, and the `GSD_FABLE_SUNSET_NOW` read (inside `fableAvailable`). At the two call sites, pass the tier through unchanged: `const alias = agentModels[profile] || agentModels['balanced'] || 'sonnet';` and `const tier = typeof phaseTier === 'string' ? phaseTier : alias;`. After this `resolveModel` returns fable/claude-fable-5 with no date logic.

3. bin/lib/config-schema.cjs and sdk/src/query/config-schema.ts — delete the `'fable.mode',` and `'fable.until',` lines from BOTH key sets (keep them in parity; the config-schema-sdk-parity test compares the two as text).

HARD RULE: product source and comments describe behavior only. Do NOT leave any GSD bookkeeping (issue/PR numbers, "removed because…", task/wave/status notes) in bin/lib or sdk/src. Just delete the code cleanly; no tombstone comments.
  </action>
  <verify>
Grep gates (each MUST be 0) — run against the source files only:
`grep -c 'FABLE_SUNSET_DATE\|applyFableSunset\|fableAvailable\|readFableKnob\|GSD_FABLE_SUNSET_NOW' bin/lib/core.cjs sdk/src/query/config-query.ts` → every file reports 0.
`grep -c 'fable\.mode\|fable\.until' bin/lib/config-schema.cjs sdk/src/query/config-schema.ts` → both 0.
Fable-tier still intact: `grep -c "'fable'" bin/lib/core.cjs` → non-zero (VALID_TIERS + MODEL_PROFILES survive).
CJS loads + syntax: `node -c bin/lib/core.cjs` and `node -c bin/lib/config-schema.cjs` exit 0.
CJS live resolution (date must not matter): `GSD_FABLE_SUNSET_NOW=2030-01-01 node -e "const c=require('./bin/lib/core.cjs'); console.log('planner tier ->', c.resolveModelForTier(process.cwd(),'gsd-planner',0))"` — report the REAL output; it MUST be fable / claude-fable-5, NOT `opus` (was `opus` pre-change under the repo quality profile).
SDK typechecks: `npx tsc --noEmit -p sdk` exits 0.
CJS regression tests still pass: `node tests/fable-tier.test.cjs` and `node tests/config-schema-sdk-parity.test.cjs` both exit 0.
  </verify>
  <done>The five gate symbols and the two config keys are gone from source (grep == 0); `fable` remains a valid tier; CJS resolves gsd-planner to a fable-based result even with GSD_FABLE_SUNSET_NOW pinned to 2030 (real output reported, not `opus`); tsc, fable-tier, and schema-parity tests pass; /tmp/fable-int-baseline.txt captured.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the sunset test, prune README, rebuild sdk/dist and regenerate dist/bm, then run the full battery</name>
  <files>tests/fable-sunset.test.cjs, README.md, sdk/dist/cli.js, dist/bm</files>
  <action>
1. Delete tests/fable-sunset.test.cjs entirely (it only tests the gate): `git rm tests/fable-sunset.test.cjs` (or `rm`). Do NOT create any replacement — no empty file.

2. Leave tests/fable-tier.test.cjs UNCHANGED. It contains catalog / profile / config-schema-key assertions only and has NO date-gate/sunset/mode/until assertions, so nothing there needs pruning and it must not become empty. Confirm by inspection that it references only the catalog (RUNTIME_PROFILE_MAP), profile maps, and `model_profile_overrides.claude.fable` — none of the removed symbols.

3. README.md (~line 184, the "Added features beyond upstream" table row for the Fable tier): remove the sunset-window / auto-fallback / `fable.mode`/`fable.until` knob prose. Reword the row so it describes the current reality — `fable` is a first-class model tier the quality profile assigns to the heaviest agents (Anthropic-compatible runtimes get `claude-fable-5`; non-Claude runtimes alias it to their top model). Keep the row (the feature still exists); just drop the withdrawal/fallback/knob language. Do NOT touch CHANGELOG.md — historical entries stay as written and no new entry or version bump is added here (release is cut separately).

4. Rebuild the SDK bundle so the live spawn path reflects the source: `npm --prefix sdk run build`.

5. Regenerate the bm twin: `npm run build:bm`, then `npm run check:bm-drift`.
  </action>
  <verify>
Sunset test gone, no empty file: `test ! -f tests/fable-sunset.test.cjs` exits 0; `test ! -f dist/bm/tests/fable-sunset.test.cjs` exits 0.
fable-tier test still green: `node tests/fable-tier.test.cjs` exits 0.
README sunset prose gone: `grep -ci 'sunset\|auto-fall\|fable.mode\|fable.until\|withdrawn' README.md` → 0 (case-insensitive).
SDK unit tests: `npm --prefix sdk run test` passes.
SDK integration (zero NEW failures vs baseline): `npm --prefix sdk run test:integration 2>&1 | tee /tmp/fable-int-post.txt`; compare the failing set to /tmp/fable-int-baseline.txt from Task 1 — the post failing set MUST be a strict subset (zero new failures). Report both counts and the diff.
Live SDK resolution on the REBUILT repo-local CLI (not the on-PATH cache): `node sdk/dist/cli.js query init.quick` → report real `planner_model`; MUST be `fable` (was `opus` pre-change). Then prove the env var is a no-op: `GSD_FABLE_SUNSET_NOW=2030-01-01 node sdk/dist/cli.js query init.quick` → still `fable`. Report both.
bm twin clean: `npm run check:bm-drift` PASS; and the gate is gone from bm CODE:
`grep -rc 'FABLE_SUNSET\|applyFableSunset\|fableAvailable\|readFableKnob' dist/bm/bin/lib/core.cjs dist/bm/sdk/dist/cli.js dist/bm/sdk/src/query/config-query.ts` → all 0. (Note: dist/bm/CHANGELOG.md retains historical "Fable" mentions mirrored from the root CHANGELOG — that is expected and must NOT be touched.)
  </verify>
  <done>fable-sunset.test.cjs deleted from source and dist/bm with no empty replacement; fable-tier test untouched and passing; README sunset prose removed; sdk/dist rebuilt; SDK unit tests pass; integration failing set is a strict subset of the captured baseline (zero new failures, reported); `node sdk/dist/cli.js query init.quick` returns `planner_model: fable` and stays `fable` with GSD_FABLE_SUNSET_NOW=2030 (real output reported); `check:bm-drift` passes and the gate symbols are absent from dist/bm code files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CJS resolver ↔ SDK resolver | The two twins must produce identical tier resolution; drift silently misroutes agent spawns. |
| source ↔ sdk/dist ↔ dist/bm | Built artifacts must mirror source; a stale build serves the old gate on the live spawn path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-j83-01 | Tampering | CJS/SDK resolver drift after edit | mitigate | Symmetric removal in both files; config-schema-sdk-parity test + grep==0 gates on both source files (Task 1 verify). |
| T-j83-02 | Denial of Service | stale sdk/dist or dist/bm still carrying the gate on the live spawn path | mitigate | Rebuild sdk/dist and regenerate dist/bm; verify via repo-local `node sdk/dist/cli.js query init.quick` and dist/bm grep==0 + check:bm-drift (Task 2). |
| T-j83-03 | Repudiation | GSD bookkeeping leaking into product source | mitigate | HARD RULE in Task 1 action: no tombstone/issue/status comments; behavior-only source. |
| T-j83-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this task; existing devDeps only. |
</threat_model>

<verification>
- Gate symbols absent from both resolvers and both config schemas (grep == 0).
- `fable` still a valid tier; fable-tier + schema-parity CJS tests pass.
- Both resolvers resolve `fable` to fable/claude-fable-5 with a future GSD_FABLE_SUNSET_NOW (live node output reported for CJS and SDK).
- No empty test file; fable-sunset.test.cjs deleted from source and dist/bm.
- SDK unit tests pass; integration failing set is a strict subset of the pre-change baseline (zero new failures).
- `check:bm-drift` passes; gate absent from dist/bm code.
</verification>

<success_criteria>
The Fable sunset/time-gate is fully removed from both twins, `fable` resolves straight to claude-fable-5 regardless of date, GSD_FABLE_SUNSET_NOW is a no-op, the sunset test is deleted with nothing empty left behind, README no longer promises a sunset window, and sdk/dist + dist/bm are rebuilt with no new integration failures and a passing bm-drift check.
</success_criteria>

<output>
Create `.planning/quick/260721-j83-remove-fable-sunset-time-gate/260721-j83-SUMMARY.md` when done.
</output>
