---
phase: quick-260716-r1c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/query/audit-open.ts
  - bin/lib/audit.cjs
  - sdk/src/query/audit-open.test.ts
  - sdk/src/golden/read-only-parity.integration.test.ts
  - tests/audit-open-quick-tasks.test.cjs
autonomous: true
requirements: [QUICK-260716-R1C]
tags: [audit-open, quick-tasks, cjs-sdk-parity, milestone-close]

must_haves:
  truths:
    - "audit-open (CJS and SDK) no longer flags a completed quick task whose SUMMARY is named <quick_id>-SUMMARY.md"
    - "A readable SUMMARY with no status field counts as complete and is not flagged"
    - "A quick dir with no SUMMARY at all is still flagged with status missing"
    - "A SUMMARY with status incomplete/gaps/gaps_found/partial/blocked (any case) is still flagged with that status"
    - "bin/gsd-tools.cjs audit-open --json and node sdk/dist/cli.js query audit-open --json produce identical quick_tasks arrays on the same project"
  artifacts:
    - path: "sdk/src/query/audit-open.ts"
      provides: "scanQuickTasks with prefixed-SUMMARY discovery and incomplete-status-set rule"
      contains: "-SUMMARY.md"
    - path: "bin/lib/audit.cjs"
      provides: "scanQuickTasks with incomplete-status-set rule matching the SDK"
      contains: "INCOMPLETE_QUICK_STATUSES"
    - path: "tests/audit-open-quick-tasks.test.cjs"
      provides: "zero-dep node:assert unit coverage for the CJS scanner (4 fixture cases)"
      min_lines: 60
    - path: "sdk/src/query/audit-open.test.ts"
      provides: "vitest unit coverage for the SDK scanner (same 4 fixture cases)"
      min_lines: 60
  key_links:
    - from: "sdk/src/golden/read-only-parity.integration.test.ts"
      to: "bin/lib/audit.cjs and sdk/src/query/audit-open.ts"
      via: "captureGsdToolsOutput vs registry.dispatch on a fixture project dir"
      pattern: "audit-open.*fixture"
    - from: "bin/build-bm.cjs --check"
      to: "dist/bm/bin/lib/audit.cjs"
      via: "regenerated bm mirror stays drift-free"
      pattern: "build-bm"
---

<objective>
Stop `audit-open` from flagging completed quick tasks as open work at milestone close, in both the CJS and SDK implementations, without weakening the golden parity guarantee between them.

Purpose: today all 37 quick tasks in this repo show status `missing` in the SDK audit (bare `SUMMARY.md` lookup never matches the mandated `<quick_id>-SUMMARY.md` name), and even the fixed CJS scanner still flags genuinely finished tasks because their SUMMARYs carry `phase:` but no `status:` field. The milestone-close gate is drowning real gaps in false positives.

Output: fixed `scanQuickTasks` in `sdk/src/query/audit-open.ts` and `bin/lib/audit.cjs` with identical semantics, rebuilt `sdk/dist`, regenerated `dist/bm`, plus unit tests on both sides and a fixture-based CJS-vs-SDK parity assertion.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.1.1/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.1.1/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@bin/lib/audit.cjs
@sdk/src/query/audit-open.ts
@sdk/src/query/frontmatter.ts
@sdk/src/golden/read-only-golden-rows.ts
@sdk/src/golden/read-only-parity.integration.test.ts
@sdk/src/golden/capture.ts
@tests/coexist.test.cjs

Style constraints from CLAUDE.md that apply to every line written in this plan's execution: no em-dashes or en-dashes anywhere (code, comments, tests, commit messages), never use the word "canonical", and no GSD workflow metadata (phase or quick-task ids, status notes) in product code or comments.

<interfaces>
Key contracts already in the codebase. Use directly, no exploration needed.

From bin/lib/audit.cjs (the CJS scanner, lines 81-162):
- `scanQuickTasks(planDir)` already resolves the SUMMARY path via readdir: accepts `SUMMARY.md` or `*-SUMMARY.md`, prefers `${dirName}-SUMMARY.md`, then the first `*-SUMMARY.md`, then bare (lines 106-121). Status resolution: `missing` when no file, `unreadable` when platformReadSync returns null, else `(fm.status || 'unknown').toLowerCase()`. The over-strict gate is line 142: `if (status === 'complete') continue;`.
- A hoisted module-level Set already exists as a pattern to mirror: `const TERMINAL_UAT_STATUSES = new Set([...])` at line 351.

From sdk/src/query/audit-open.ts (the broken SDK scanner, lines 67-118):
- `scanQuickTasks(planDir)` does `const summaryPath = join(taskDir, 'SUMMARY.md')` at line 84 (bare name only, never matches prefixed SUMMARYs) and the same over-strict `if (status === 'complete') continue;` at line 99. Imports available at top of file: `existsSync, readdirSync, readFileSync` from node:fs, `join, basename` from node:path, `extractFrontmatter` from './frontmatter.js'.
- Public entry points: `export function auditOpenArtifacts(projectDir: string, workstream?: string): AuditOpenResult` and `export const auditOpen: QueryHandler`.

From sdk/src/query/frontmatter.ts:
- `extractFrontmatter(content: string): Record<string, unknown>` parses only the leading `---...---` block; absent `status:` yields `fm.status === undefined`.

From sdk/src/golden/capture.ts:
- `captureGsdToolsOutput(command: string, args: string[], projectDir: string)` runs the repo's own bin/gsd-tools.cjs as a subprocess with cwd = projectDir and returns parsed JSON. Works against any directory, including a temp fixture project.

From sdk/src/golden/read-only-parity.integration.test.ts (lines 41-54):
- The existing audit-open parity test compares `captureGsdToolsOutput('audit-open', ['--json'], REPO_ROOT)` with `registry.dispatch('audit-open', ['--json'], REPO_ROOT)` after stripping `scanned_at` and `has_scan_errors`. audit-open is listed in `readOnlyGoldenCanonicals()` in read-only-golden-rows.ts line 73; no rows-file change is needed.

Ground truth for the end-to-end check: `.planning/quick/260716-0ao-.../260716-0ao-SUMMARY.md` and `.planning/quick/260714-coq-.../260714-coq-SUMMARY.md` both exist, both use the prefixed filename, and neither has a `status:` line in frontmatter (verified with grep during planning).

Gotcha: `gsd-sdk` on PATH resolves the installed plugin cache, not this repo. All verify commands must invoke `node sdk/dist/cli.js` and `node bin/gsd-tools.cjs` directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix both scanQuickTasks implementations, rebuild sdk/dist, regenerate dist/bm</name>
  <files>sdk/src/query/audit-open.ts, bin/lib/audit.cjs</files>
  <action>
Root cause 1 (SDK only). In sdk/src/query/audit-open.ts scanQuickTasks, replace the bare `join(taskDir, 'SUMMARY.md')` lookup (line 84) with a port of the CJS discovery logic from bin/lib/audit.cjs lines 106-121: readdirSync the task dir with withFileTypes, filter to files where name === 'SUMMARY.md' or name ends with '-SUMMARY.md', prefer `${dirName}-SUMMARY.md`, else the first name ending in '-SUMMARY.md', else the bare file; wrap the readdir in try/catch that falls through with summaryPath = null so status stays 'missing'. Keep using the existing existsSync/readFileSync/readdirSync imports; the SDK file has no requireSafePath equivalent and must not grow one here (the surrounding scanners read directly and the parity target is the JSON output, not the guard).

Root cause 2 (both files). Define one module-level constant per file, `INCOMPLETE_QUICK_STATUSES = new Set(['incomplete', 'gaps', 'gaps_found', 'partial', 'blocked'])`, hoisted next to TERMINAL_UAT_STATUSES in the CJS file and near the top of the SDK file. Replace the `if (status === 'complete') continue;` gate in BOTH scanQuickTasks with the same classification: flag the task only when status === 'missing', status === 'unreadable', or INCOMPLETE_QUICK_STATUSES.has(status); otherwise skip (this makes 'complete', absent status which currently resolves to 'unknown', and any other value all count as complete). Status is already lowercased at read time in both files, which satisfies the case-insensitive requirement; keep that. Do not change what status string is reported for flagged items. Update the stale doc comment above the CJS scanQuickTasks ("Incomplete if SUMMARY.md missing or status !== 'complete'") and the SDK file's equivalent inline expectations to describe the new rule, using no em-dashes in any new or edited comment text.

The two implementations must be line-for-line equivalent in behavior: same set contents, same ordering of checks, same defaults. Diff them mentally field by field before moving on; the golden parity test compares full JSON output.

Then rebuild and propagate: run `npm --prefix sdk run build` to refresh sdk/dist, then `node bin/build-bm.cjs` to regenerate dist/bm, then `node bin/build-bm.cjs --check` which must stay green.
  </action>
  <verify>
    <automated>node bin/gsd-tools.cjs audit-open --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const bad=j.items.quick_tasks.filter(t=>/auto-enable-bm|centralize-marketplace/.test(String(t.slug)));if(bad.length){console.error(JSON.stringify(bad));process.exit(1)}console.log('cjs clean')})" && node sdk/dist/cli.js query audit-open --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const q=(j.items||j.data&&j.data.items||{}).quick_tasks||[];const bad=q.filter(t=>/auto-enable-bm|centralize-marketplace/.test(String(t.slug)));if(bad.length){console.error(JSON.stringify(bad));process.exit(1)}console.log('sdk clean')})" && node bin/build-bm.cjs --check</automated>
  </verify>
  <done>Both CLIs run in this repo no longer list 260716-0ao (auto-enable-bm) or 260714-coq (centralize-marketplace) under quick_tasks; sdk build succeeds; build-bm --check green. Quick tasks that genuinely lack any SUMMARY still appear with status missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests on both sides plus a fixture-based CJS-vs-SDK parity assertion</name>
  <files>tests/audit-open-quick-tasks.test.cjs, sdk/src/query/audit-open.test.ts, sdk/src/golden/read-only-parity.integration.test.ts</files>
  <behavior>
    - Case A: quick dir containing `<dirName>-SUMMARY.md` with frontmatter `phase: x` and NO status field: not flagged (both scanners).
    - Case B: quick dir with no SUMMARY file at all: flagged with status 'missing' (both).
    - Case C: quick dir containing `<dirName>-SUMMARY.md` with `status: incomplete`: flagged with status 'incomplete' (both). Also cover one uppercase variant, `status: BLOCKED`, flagged as 'blocked'.
    - Case D: quick dir containing bare `SUMMARY.md` with no status field: not flagged (backward compat, both).
    - Parity: on a single fixture project containing cases A-D, the CJS CLI and the SDK registry produce identical audit-open JSON after stripping scanned_at and has_scan_errors.
  </behavior>
  <action>
Create tests/audit-open-quick-tasks.test.cjs mirroring the zero-dep node:assert pattern of tests/coexist.test.cjs (checks array, check(name, fn), summary print, process.exit(1) on failure). In the test, build a temp fixture project under os.tmpdir() (mkdtemp, suffix X's at the end per the mktemp portability convention) with `.planning/quick/` dirs covering cases A-D, require('../bin/lib/audit.cjs'), call auditOpenArtifacts(fixtureRoot), and assert: quick_tasks contains exactly the case B and case C entries with the expected status strings, and neither case A nor case D appears. Clean up the temp dir in a finally block.

Create sdk/src/query/audit-open.test.ts as a vitest unit test (mirror the fixture-building style of the existing sdk/src/query/*.test.ts files): same four cases against the exported auditOpenArtifacts from './audit-open.js', same assertions, mkdtempSync/rmSync lifecycle in beforeEach/afterEach or a helper.

Extend the existing `describe('audit-open golden parity (excluding scanned_at)')` block in sdk/src/golden/read-only-parity.integration.test.ts with a second `it` that builds the same A-D fixture project in a temp dir, then compares `captureGsdToolsOutput('audit-open', ['--json'], fixtureDir)` against `registry.dispatch('audit-open', ['--json'], fixtureDir)` using the existing strip helper pattern (delete scanned_at and has_scan_errors), asserting full deep equality. This is the required identical-results test across the CJS/SDK boundary (a shared classification module is not practical across that boundary, so the two files stay separate and this test is the lockstep guard). The existing repo-root parity `it` stays untouched; do not weaken or filter it. No change to read-only-golden-rows.ts is needed since audit-open is already in the golden set.

Write the failing tests first, confirm they exercise the new rule (if Task 1 is already applied they should pass immediately; if run against the pre-fix code they must fail on cases A and D), then run the full relevant suites. Comments in all new test files: plain hyphens and colons only, no em-dashes, no GSD phase/quick-id bookkeeping beyond what the fixture data itself requires.
  </action>
  <verify>
    <automated>node tests/audit-open-quick-tasks.test.cjs && cd /Users/jnuyens/src/gsd-plugin/sdk && npx vitest run src/query/audit-open.test.ts src/golden/read-only-parity.integration.test.ts</automated>
  </verify>
  <done>CJS unit test passes standalone via node; SDK unit test and both audit-open parity assertions (repo root and A-D fixture) pass under vitest; no existing golden parity test was removed or filtered.</done>
</task>

</tasks>

<verification>
- `node bin/gsd-tools.cjs audit-open --json` and `node sdk/dist/cli.js query audit-open --json` agree on quick_tasks for this repo, and neither lists 260716-0ao or 260714-coq.
- Genuinely unfinished quick tasks (dirs with no SUMMARY at all, per the 35 historical carry-overs noted in STATE.md) still appear with status missing, so the milestone-close gate keeps catching real gaps.
- `node bin/build-bm.cjs --check` green after regeneration.
- Full sdk golden parity suite green: `cd /Users/jnuyens/src/gsd-plugin/sdk && npx vitest run src/golden/read-only-parity.integration.test.ts`.
</verification>

<success_criteria>
- Both scanners implement the identical rule: flag only missing / unreadable / {incomplete, gaps, gaps_found, partial, blocked}; everything else (complete, absent status, unknown values) is complete.
- SDK scanner discovers `<quick_id>-SUMMARY.md` with the same preference order as CJS (exact dirName prefix, then any prefixed, then bare).
- Five test behaviors from Task 2 pass on both sides plus the cross-boundary parity assertion.
- sdk/dist rebuilt and dist/bm regenerated in the same change; build-bm --check green.
</success_criteria>

<output>
Create `.planning/quick/260716-r1c-fix-audit-open-quick-task-false-positive/260716-r1c-SUMMARY.md` when done, with `status: complete` in frontmatter.
</output>
