---
phase: 260919-298
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-advisor-researcher.md
  - agents/gsd-assumptions-analyzer.md
  - agents/gsd-codebase-mapper.md
  - agents/gsd-debugger.md
  - agents/gsd-doc-writer.md
  - agents/gsd-executor.md
  - agents/gsd-integration-checker.md
  - agents/gsd-nyquist-auditor.md
  - agents/gsd-phase-researcher.md
  - agents/gsd-plan-checker.md
  - agents/gsd-planner.md
  - agents/gsd-project-researcher.md
  - agents/gsd-research-synthesizer.md
  - agents/gsd-roadmapper.md
  - agents/gsd-security-auditor.md
  - agents/gsd-ui-auditor.md
  - agents/gsd-ui-checker.md
  - agents/gsd-ui-researcher.md
  - agents/gsd-verifier.md
  - dist/bm/agents/gsd-advisor-researcher.md
  - dist/bm/agents/gsd-assumptions-analyzer.md
  - dist/bm/agents/gsd-codebase-mapper.md
  - dist/bm/agents/gsd-debugger.md
  - dist/bm/agents/gsd-doc-writer.md
  - dist/bm/agents/gsd-executor.md
  - dist/bm/agents/gsd-integration-checker.md
  - dist/bm/agents/gsd-nyquist-auditor.md
  - dist/bm/agents/gsd-phase-researcher.md
  - dist/bm/agents/gsd-plan-checker.md
  - dist/bm/agents/gsd-planner.md
  - dist/bm/agents/gsd-project-researcher.md
  - dist/bm/agents/gsd-research-synthesizer.md
  - dist/bm/agents/gsd-roadmapper.md
  - dist/bm/agents/gsd-security-auditor.md
  - dist/bm/agents/gsd-ui-auditor.md
  - dist/bm/agents/gsd-ui-checker.md
  - dist/bm/agents/gsd-ui-researcher.md
  - dist/bm/agents/gsd-verifier.md
autonomous: true
requirements: [QUICK-260919-298]

must_haves:
  truths:
    - "Each of the 19 agents that the workflows call `agent-skills <name>` for lists `Skill` in its frontmatter tools allowlist, exactly once, in the SOURCE agents/<name>.md file"
    - "The 14 agents outside that list are byte-unchanged (no Skill grant, no other edit)"
    - "The only diff in agents/ is the tools grant: every added line is a `tools:` line ending in `, Skill` or a `  - Skill` list item; every removed line is the prior `tools:` line; name, description, color and body are untouched"
    - "After `node bin/build-bm.cjs`, the same 19 dist/bm/agents/<name>.md files carry Skill and `node bin/build-bm.cjs --check` reports PASS"
    - "With config agent_skills.gsd-executor = [\"global:superpowers:brainstorming\"], the two enforced directive checks pass: `node sdk/dist/cli.js query --project-dir <T> agent-skills gsd-executor` and `node bin/gsd-tools.cjs agent-skills gsd-executor` (cwd <T>) both still emit the Skill-tool load-by-name directive for superpowers:brainstorming (no regression from v4.7.2; sdk/src/query/skills.ts is not touched). Running the installed `bm-sdk` as a third check is optional best-effort and not gated."
    - "Full CJS battery has zero new failures: `comm -13 /tmp/gsd-260919-298-scratch/cjs-before.txt /tmp/gsd-260919-298-scratch/cjs-after.txt` is empty (both files sorted; the known baseline context-monitor-hook-event and version-command may appear on both sides). This is asserted inside Task 2's automated verify, not narrated."
    - "Hygiene greps over the added diff lines in agents/ and dist/bm/agents find no `#NNNN` issue refs, no em/en dashes, no occurrence of the banned c-word (grep pattern canon[i]cal)"
    - "No version bump, no CHANGELOG entry, no Co-Authored-By trailer; the issue number appears only in the SUMMARY"
    - "The SUMMARY cites the concrete evidence for the YAML-list grant form (gsd-nyquist-auditor and gsd-security-auditor already ship YAML-list `tools:` in production, so the runtime parses that form) and flags as a residual that a live spawn of those two was not re-verified"
  artifacts:
    - path: "agents/gsd-executor.md"
      provides: "inline-form grant example"
      contains: "tools: Read, Write, Edit, Bash, Grep, Glob, mcp__context7__*, Skill"
    - path: "agents/gsd-nyquist-auditor.md"
      provides: "YAML-list-form grant example"
      contains: "  - Skill"
    - path: "agents/gsd-security-auditor.md"
      provides: "YAML-list-form grant example"
      contains: "  - Skill"
    - path: "dist/bm/agents/gsd-executor.md"
      provides: "regenerated bm twin carrying the grant"
      contains: "mcp__context7__*, Skill"
    - path: "/tmp/gsd-260919-298-scratch/cjs-before.txt"
      provides: "sorted CJS battery failure baseline captured before any edit (deterministic path shared by Task 1 and Task 2)"
  key_links:
    - from: "agents/<19 names>.md tools: allowlist"
      to: "Claude Code subagent tool set at spawn"
      via: "frontmatter tools: is a strict allow-list; Skill must be listed for the agent to invoke it"
      pattern: "^tools:.*Skill|^  - Skill$"
    - from: "sdk/src/query/skills.ts and bin/lib/init.cjs directive line"
      to: "agent invoking the Skill tool at start"
      via: "the v4.7.2 directive `Invoke the Skill tool with skill \"<plugin>:<skill>\"` is only followable when Skill is granted"
      pattern: "Invoke the Skill tool with skill"
    - from: "agents/*.md"
      to: "dist/bm/agents/*.md"
      via: "bin/build-bm.cjs byte-copy plus colon-only substitution (does not touch tools:)"
      pattern: "bm drift check: PASS"
    - from: "/tmp/gsd-260919-298-scratch/cjs-before.txt (Task 1)"
      to: "/tmp/gsd-260919-298-scratch/cjs-after.txt (Task 2 automated verify)"
      via: "comm -13 before after must be empty; literal path because shell variables do not survive between task Bash invocations"
      pattern: "comm -13 /tmp/gsd-260919-298-scratch/cjs-before.txt /tmp/gsd-260919-298-scratch/cjs-after.txt"
---

<objective>
Grant the `Skill` tool to the 19 agents that receive `agent_skills` blocks, so the v4.7.2 load-by-name directive ("Invoke the Skill tool with skill \"<plugin>:<skill>\" at agent start") is actually followable. Today none of the 33 agents list Skill in `tools:`, and `tools:` is a strict allow-list, so the directive asks for a tool the subagent does not have.

Purpose: Complete the plugin-skill support shipped in v4.7.2. An empirical spike already showed a depth-1 subagent CAN invoke Skill when it is in its tool set (it loaded superpowers:brainstorming and superpowers:systematic-debugging without error). The only missing piece is the static grant in the agent frontmatter.

Output: 19 source agent files with `Skill` appended to their tools allowlist, the regenerated dist/bm twins, and gate output proving nothing else moved.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/bm/4.7.1/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/bm/4.7.1/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@agents/gsd-executor.md
@agents/gsd-nyquist-auditor.md
@sdk/src/query/skills.ts

<interfaces>
<!-- Observed state at planning time. Use directly; no exploration needed. -->

Scratch path shared by BOTH tasks (literal, deterministic; shell variables do NOT persist between the separate Bash invocations of Task 1 and Task 2):
  /tmp/gsd-260919-298-scratch
Task 1 creates it with `mkdir -p` and writes base-sha.txt and cjs-before.txt there. Task 2 reads cjs-before.txt from that literal path and writes cjs-after.txt and added.txt beside it. Never use `mktemp -d` for these files.

Two frontmatter shapes exist among the 19 target agents (line 4 of each file is `tools:`):

Inline comma form (17 agents), example agents/gsd-executor.md:
  tools: Read, Write, Edit, Bash, Grep, Glob, mcp__context7__*

YAML list form (2 agents: gsd-nyquist-auditor, gsd-security-auditor), lines 4 to 10:
  tools:
    - Read
    - Write
    - Edit
    - Bash
    - Glob
    - Grep
  color: purple        (red for gsd-security-auditor)

Evidence that the YAML-list form is honored by the Claude Code runtime: gsd-nyquist-auditor and gsd-security-auditor ALREADY ship with YAML-list `tools:` at HEAD and are spawned in production (nyquist audit and security audit workflows) with exactly that restricted tool set. The runtime therefore demonstrably parses the list form; adding one more `  - Skill` item does not change the parsing path. Residual (must be stated in the SUMMARY): a live spawn of those two specific agents with Skill present was NOT re-verified in this task, consistent with the overall live-spawn caveat in Task 2 step 7.

Current inline tools lines of the other 16 inline targets (append `, Skill` to each):
  gsd-advisor-researcher:   Read, Bash, Grep, Glob, WebSearch, WebFetch, mcp__context7__*
  gsd-assumptions-analyzer: Read, Bash, Grep, Glob
  gsd-codebase-mapper:      Read, Bash, Grep, Glob, Write
  gsd-debugger:             Read, Write, Edit, Bash, Grep, Glob, WebSearch
  gsd-doc-writer:           Read, Bash, Grep, Glob, Write, Edit
  gsd-integration-checker:  Read, Bash, Grep, Glob
  gsd-phase-researcher:     Read, Write, Edit, Bash, Grep, Glob, WebSearch, WebFetch, mcp__context7__*, mcp__firecrawl__*, mcp__exa__*
  gsd-plan-checker:         Read, Bash, Glob, Grep
  gsd-planner:              Read, Write, Bash, Glob, Grep, WebFetch, mcp__context7__*
  gsd-project-researcher:   Read, Write, Bash, Grep, Glob, WebSearch, WebFetch, mcp__context7__*, mcp__firecrawl__*, mcp__exa__*
  gsd-research-synthesizer: Read, Write, Bash
  gsd-roadmapper:           Read, Write, Bash, Glob, Grep
  gsd-ui-auditor:           Read, Write, Bash, Grep, Glob
  gsd-ui-checker:           Read, Bash, Glob, Grep
  gsd-ui-researcher:        Read, Write, Edit, Bash, Grep, Glob, WebSearch, WebFetch, mcp__context7__*, mcp__firecrawl__*, mcp__exa__*
  gsd-verifier:             Read, Write, Bash, Grep, Glob

Baseline facts (verified at planning time):
- Zero agents currently contain Skill in frontmatter (`grep -ln '^tools:.*\bSkill\b' agents/*.md` prints nothing).
- `git status --short agents/ dist/bm/agents/` is clean; src and dist tools lines are identical for gsd-executor and gsd-security-auditor.
- dist/bm/agents is committed and regenerated by `node bin/build-bm.cjs`; the transform is colon-only and does not rewrite `tools:`.
- Pre-existing hygiene noise in agent BODIES: 31 agents contain em/en dashes, 12 contain `#NNNN` refs, 3 contain the banned c-word (grep pattern canon[i]cal). Hygiene gates therefore run over ADDED diff lines only, never whole files.
- CJS battery: 69 files under tests/*.test.cjs. Known baseline failures: context-monitor-hook-event and version-command. No package.json test script exists; run the per-file loop shown in Task 1.
- `bm-sdk` on PATH resolves to the INSTALLED cache copy (/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.7.1/bin/bm-sdk), not this repo. Drive the repo SDK via `node sdk/dist/cli.js query ...`. The two ENFORCED directive checks are the repo SDK CLI and `node bin/gsd-tools.cjs`; running the installed `bm-sdk query agent-skills gsd-executor` as well is optional best-effort (skills.ts is untouched so it should agree) and is not part of any gate.
- The SDK helper `parseAgentTools` in sdk/src/prompt-builder.ts only understands the inline comma form (for a YAML list it captures `- Read`). That is a pre-existing SDK-runner quirk unrelated to the Claude Code plugin runtime, which parses YAML lists correctly (see the evidence paragraph above). OUT OF SCOPE here; do not convert the two list-form agents to inline form and do not touch prompt-builder.ts.

Non-targets (must stay byte-identical): gsd-ai-researcher, gsd-code-fixer, gsd-code-reviewer, gsd-debug-session-manager, gsd-doc-classifier, gsd-doc-synthesizer, gsd-doc-verifier, gsd-domain-researcher, gsd-eval-auditor, gsd-eval-planner, gsd-framework-selector, gsd-intel-updater, gsd-pattern-mapper, gsd-user-profiler.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture baseline, then append Skill to the tools allowlist of the 19 target agents</name>
  <files>agents/gsd-advisor-researcher.md, agents/gsd-assumptions-analyzer.md, agents/gsd-codebase-mapper.md, agents/gsd-debugger.md, agents/gsd-doc-writer.md, agents/gsd-executor.md, agents/gsd-integration-checker.md, agents/gsd-nyquist-auditor.md, agents/gsd-phase-researcher.md, agents/gsd-plan-checker.md, agents/gsd-planner.md, agents/gsd-project-researcher.md, agents/gsd-research-synthesizer.md, agents/gsd-roadmapper.md, agents/gsd-security-auditor.md, agents/gsd-ui-auditor.md, agents/gsd-ui-checker.md, agents/gsd-ui-researcher.md, agents/gsd-verifier.md</files>
  <action>
BASELINE FIRST (before any edit), from /Users/jnuyens/src/gsd-plugin. Use the LITERAL scratch path `/tmp/gsd-260919-298-scratch` (not mktemp: shell variables do not survive into Task 2's separate Bash invocation, and Task 2's automated gate reads this exact path):
- `mkdir -p /tmp/gsd-260919-298-scratch` then `git rev-parse HEAD > /tmp/gsd-260919-298-scratch/base-sha.txt`.
- CJS battery baseline: `for f in tests/*.test.cjs; do node "$f" >/dev/null 2>&1 || echo "$f"; done | sort > /tmp/gsd-260919-298-scratch/cjs-before.txt`. Expected content is exactly tests/context-monitor-hook-event.test.cjs and tests/version-command.test.cjs. If anything else is listed, note it as pre-existing in the SUMMARY and proceed (nothing has been edited yet, so it cannot be caused by this task). Paste the file content into the SUMMARY.
- Confirm `git status --short agents/ dist/bm/agents/` prints nothing and that no agent frontmatter contains Skill, so idempotency is trivially satisfied (nothing to de-duplicate).

THEN EDIT, using the Edit tool per file (19 edits; no whole-file rewrites, no heredocs):
- Inline form (the 17 targets other than gsd-nyquist-auditor and gsd-security-auditor): change the single `tools:` line by appending `, Skill` at the END of the existing list, preserving every existing tool and its order exactly (values listed in the interfaces block). Example result for gsd-executor: `tools: Read, Write, Edit, Bash, Grep, Glob, mcp__context7__*, Skill`.
- YAML list form (gsd-nyquist-auditor, gsd-security-auditor): insert a new list item line `  - Skill` (two spaces, dash, space) immediately after the existing `  - Grep` item and before the `color:` line, matching the two-space indentation of the neighbouring items. Keep the list form; do NOT collapse it to inline. The list form is already honored at runtime for these two agents (they ship with it today, see the evidence paragraph in the interfaces block); do not add any comment or note about that to the agent files.
- Idempotency rule: if a target file somehow already has Skill in its frontmatter tools, leave it alone rather than adding a second one. The verify asserts exactly one occurrence per target.
- Touch NOTHING else: not `name:`, not `description:`, not `color:`, not the body, not any of the 14 non-target agents, not dist/bm (regenerated in Task 2). This task is only the tools grant; the separate bm- agent-name migration is a different task and must not be started here.
- Product-source hygiene per ./CLAUDE.md: the added text is exactly `, Skill` or `  - Skill`; no comments, no issue numbers, no dashes, no GSD bookkeeping in the agent files.
  </action>
  <verify>
    <automated>cd /Users/jnuyens/src/gsd-plugin && [ -f /tmp/gsd-260919-298-scratch/base-sha.txt ] && [ -f /tmp/gsd-260919-298-scratch/cjs-before.txt ] && EXP="gsd-advisor-researcher gsd-assumptions-analyzer gsd-codebase-mapper gsd-debugger gsd-doc-writer gsd-executor gsd-integration-checker gsd-nyquist-auditor gsd-phase-researcher gsd-plan-checker gsd-planner gsd-project-researcher gsd-research-synthesizer gsd-roadmapper gsd-security-auditor gsd-ui-auditor gsd-ui-checker gsd-ui-researcher gsd-verifier" && got=$(for f in agents/*.md; do sed -n '2,/^---$/p' "$f" | grep -qE '^tools:.*(^|, )Skill(,|$)|^  - Skill$' && basename "$f" .md; done | sort) && diff <(echo "$got") <(printf '%s\n' $EXP | sort) && [ "$(echo "$got" | wc -l | tr -d ' ')" = 19 ] && diff <(git diff --name-only -- agents | sort) <(printf 'agents/%s.md\n' $EXP | sort) && dups=$(for a in $EXP; do n=$(sed -n '2,/^---$/p' "agents/$a.md" | grep -oE '(^|, )Skill(,|$)|^  - Skill$' | wc -l | tr -d ' '); [ "$n" = 1 ] || echo "$a:$n"; done) && [ -z "$dups" ] && bad=$(git diff -U0 -- agents | grep -E '^[-+]' | grep -vE '^(\+\+\+|---) ' | grep -vE '^\+tools: .*, Skill$|^\+  - Skill$|^-tools: ') && [ -z "$bad" ] && echo GRANT-OK</automated>
  </verify>
  <done>Output ends with GRANT-OK: `/tmp/gsd-260919-298-scratch/base-sha.txt` and `/tmp/gsd-260919-298-scratch/cjs-before.txt` exist (captured before the edit), the set of agents whose frontmatter grants Skill is exactly the 19 expected names (so no other agent gained it), `git diff --name-only -- agents` lists exactly those 19 files, each has exactly one Skill occurrence, and every changed line in agents/ is either an added `tools: ..., Skill` line, an added `  - Skill` item, or a removed old `tools:` line.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate dist/bm, prove the directive still renders, run the hard gates, commit</name>
  <files>dist/bm/agents/gsd-advisor-researcher.md, dist/bm/agents/gsd-assumptions-analyzer.md, dist/bm/agents/gsd-codebase-mapper.md, dist/bm/agents/gsd-debugger.md, dist/bm/agents/gsd-doc-writer.md, dist/bm/agents/gsd-executor.md, dist/bm/agents/gsd-integration-checker.md, dist/bm/agents/gsd-nyquist-auditor.md, dist/bm/agents/gsd-phase-researcher.md, dist/bm/agents/gsd-plan-checker.md, dist/bm/agents/gsd-planner.md, dist/bm/agents/gsd-project-researcher.md, dist/bm/agents/gsd-research-synthesizer.md, dist/bm/agents/gsd-roadmapper.md, dist/bm/agents/gsd-security-auditor.md, dist/bm/agents/gsd-ui-auditor.md, dist/bm/agents/gsd-ui-checker.md, dist/bm/agents/gsd-ui-researcher.md, dist/bm/agents/gsd-verifier.md</files>
  <action>
All from /Users/jnuyens/src/gsd-plugin. Scratch path is the LITERAL `/tmp/gsd-260919-298-scratch` written by Task 1 (do not recreate it with mktemp; if `cjs-before.txt` is missing there, stop and re-run the Task 1 baseline capture against the base SHA rather than inventing a baseline). Paste the REAL output of each gate into the SUMMARY; never paraphrase a pass.

1. Regenerate and drift-check: `node bin/build-bm.cjs` then `node bin/build-bm.cjs --check` (must print `bm drift check: PASS`). Confirm `git diff --name-only -- dist/bm | sort` lists exactly the 19 dist/bm/agents/<name>.md twins and nothing else in dist/bm (the transform is colon-only; tools lines pass through unchanged, so `diff <(grep -h '^tools:\|^  - Skill$' agents/gsd-executor.md agents/gsd-nyquist-auditor.md) <(grep -h '^tools:\|^  - Skill$' dist/bm/agents/gsd-executor.md dist/bm/agents/gsd-nyquist-auditor.md)` prints nothing).

2. Directive regression (unchanged behavior from v4.7.2; skills.ts is not modified): create T=$(mktemp -d), write `{"agent_skills":{"gsd-executor":["global:superpowers:brainstorming"]}}` to `$T/.planning/config.json`, then run the TWO enforced checks: `node sdk/dist/cli.js query --project-dir "$T" agent-skills gsd-executor` and `(cd "$T" && node /Users/jnuyens/src/gsd-plugin/bin/gsd-tools.cjs agent-skills gsd-executor)`. Each must print an `<agent_skills>` block containing `- Invoke the Skill tool with skill "superpowers:brainstorming" at agent start (plugin skill, loaded by name, not by path)` with empty stderr. Quote the SDK output verbatim in the SUMMARY. Optionally (best-effort, not gated, label it as such in the SUMMARY) also run the installed `bm-sdk query --project-dir "$T" agent-skills gsd-executor`; it should agree because skills.ts is untouched, but a mismatch there is an installed-cache observation, not a failure of this task.

3. CJS battery after (this is machine-enforced in the automated verify below, not just here): `for f in tests/*.test.cjs; do node "$f" >/dev/null 2>&1 || echo "$f"; done | sort > /tmp/gsd-260919-298-scratch/cjs-after.txt` then `comm -13 /tmp/gsd-260919-298-scratch/cjs-before.txt /tmp/gsd-260919-298-scratch/cjs-after.txt` must print nothing (zero failures present after that were not present before; the two baseline failures context-monitor-hook-event and version-command are expected on both sides and cancel out). Paste both files and the empty comm output into the SUMMARY. SDK vitest is NOT required because no file under sdk/ is touched; state that in the SUMMARY rather than skipping silently.

4. Hygiene over ADDED lines only (whole-file greps would trip on pre-existing body noise): `git diff -U0 -- agents dist/bm/agents | grep '^+' | grep -v '^+++' > /tmp/gsd-260919-298-scratch/added.txt`, then require all three of `! grep -nE '#[0-9]{4}' /tmp/gsd-260919-298-scratch/added.txt`, `! grep -nE 'canon[i]cal' /tmp/gsd-260919-298-scratch/added.txt`, `! grep -nP '\x{2014}|\x{2013}' /tmp/gsd-260919-298-scratch/added.txt`. Also confirm the whole tree diff is confined to agents/ and dist/bm/agents/: `git status --short | grep -vE '^ M (agents|dist/bm/agents)/' | grep -vE '^\?\? |^ M \.planning/' ` must print nothing (pre-existing modified .planning files and the untracked rtf file are not part of this task and must NOT be staged).

5. Release hygiene: `git diff --quiet HEAD -- CHANGELOG.md .claude-plugin/plugin.json .claude-plugin/marketplace.json package.json dist/bm/.claude-plugin/plugin.json` must succeed (no version bump, no CHANGELOG).

6. Commit ONLY the 38 agent files: `git add agents/ dist/bm/agents/` then commit with subject `feat(agents): grant the Skill tool to agent_skills recipients` and a short body stating that the 19 agents which receive agent_skills blocks now list Skill in their tools allowlist so plugin skills can be loaded by name, and that dist/bm was regenerated. No Co-Authored-By trailer (user rule overrides the session reminder), no issue number in the message (the issue number belongs only in the SUMMARY), no dashes. Verify with `git log -1 --format=%B | grep -ciE 'co-authored-by|#[0-9]{4}'` printing 0.

7. SUMMARY honesty notes (both required):
   a. Live spawn: the end-to-end proof (spawning bm:gsd-executor from a fresh Claude Code session and watching it load a plugin skill) requires a `/plugin` refresh and `/reload-plugins`, which an agent cannot run. State plainly that the static grant is verified (gates above) and that the decisive mechanic (a depth-1 subagent can invoke Skill when granted) was proven by the earlier spike, NOT re-run here. Do not claim the live spawn was verified.
   b. YAML-list form: do not assert that the list-form grant is runtime-honored; cite the evidence instead. Write: gsd-nyquist-auditor and gsd-security-auditor already use YAML-list `tools:` in production at HEAD for their existing restricted tool sets, so the Claude Code runtime demonstrably parses the list form and the added `  - Skill` item rides the same parser. Then flag the residual explicitly: a live spawn of those two agents with Skill present was not re-verified in this task (same reload caveat as 7a).
  </action>
  <verify>
    <automated>cd /Users/jnuyens/src/gsd-plugin && [ -f /tmp/gsd-260919-298-scratch/cjs-before.txt ] && node bin/build-bm.cjs >/dev/null && node bin/build-bm.cjs --check && [ "$(for f in dist/bm/agents/*.md; do sed -n '2,/^---$/p' "$f" | grep -qE '^tools:.*(^|, )Skill(,|$)|^  - Skill$' && echo x; done | wc -l | tr -d ' ')" = 19 ] && T=$(mktemp -d) && mkdir -p "$T/.planning" && echo '{"agent_skills":{"gsd-executor":["global:superpowers:brainstorming"]}}' > "$T/.planning/config.json" && node sdk/dist/cli.js query --project-dir "$T" agent-skills gsd-executor 2>"$T/sdk-err.txt" | grep -q 'Invoke the Skill tool with skill "superpowers:brainstorming"' && [ ! -s "$T/sdk-err.txt" ] && (cd "$T" && node /Users/jnuyens/src/gsd-plugin/bin/gsd-tools.cjs agent-skills gsd-executor) 2>/dev/null | grep -q 'Invoke the Skill tool with skill "superpowers:brainstorming"' && for f in tests/*.test.cjs; do node "$f" >/dev/null 2>&1 || echo "$f"; done | sort > /tmp/gsd-260919-298-scratch/cjs-after.txt && [ -z "$(comm -13 /tmp/gsd-260919-298-scratch/cjs-before.txt /tmp/gsd-260919-298-scratch/cjs-after.txt)" ] && git diff -U0 HEAD -- agents dist/bm/agents | grep '^+' | grep -v '^+++' | { ! grep -qE '#[0-9]{4}|canon[i]cal'; } && git diff -U0 HEAD -- agents dist/bm/agents | grep '^+' | grep -v '^+++' | { ! grep -qP '\x{2014}|\x{2013}'; } && git diff --quiet HEAD -- CHANGELOG.md .claude-plugin/plugin.json .claude-plugin/marketplace.json package.json && echo GATES-OK</automated>
  </verify>
  <done>Output ends with GATES-OK, which now machine-enforces: drift check PASS, 19 dist twins carrying Skill, the two directive checks (repo SDK CLI and gsd-tools.cjs) emitting the load-by-name line, an empty `comm -13 cjs-before cjs-after` (zero new CJS failures), clean hygiene greps over added lines, and unchanged version/CHANGELOG files. The SUMMARY quotes real output for each of those plus the commit containing only the 38 agent files with no Co-Authored-By and no issue number; the installed bm-sdk run, if done, is labelled best-effort. The SUMMARY states the live spawn was not verified, cites the spike for the Skill-invocation mechanic, cites the existing production YAML-list `tools:` of gsd-nyquist-auditor and gsd-security-auditor as the evidence for the list form, and flags the un-re-verified live spawn of those two as a residual.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent frontmatter tools: allowlist -> Claude Code subagent runtime | widening a subagent's tool set; Skill lets it load skill prompts by name |
| .planning/config.json agent_skills -> agent prompt | user-controlled skill names already validated by PLUGIN_SKILL_NAME_RE in both resolver twins (v4.7.2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260919-298-01 | Elevation of privilege | agents/*.md tools: (19 files) | accept | Skill only loads skill prompts already installed by the user in their own Claude Code plugins; no new filesystem, network or shell capability. Grant is limited to the 19 agents that receive agent_skills; verify asserts no other agent gained it. |
| T-260919-298-02 | Tampering | dist/bm/agents/*.md | mitigate | regenerated only via `node bin/build-bm.cjs`, drift-checked with `--check`, and asserted to differ from src only in the tools lines. |
| T-260919-298-03 | Tampering | agent bodies / non-target agents | mitigate | verify gates require every changed line in agents/ to be a tools grant line and the changed-file set to equal the 19 names. |
| T-260919-298-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this task. |
</threat_model>

<verification>
- Task 1 verify prints GRANT-OK (scratch baseline files present at the literal /tmp/gsd-260919-298-scratch path, exact 19-name set, one Skill per target, only tools lines changed).
- Task 2 verify prints GATES-OK (dist regen + drift PASS, 19 dist twins, directive emitted by the two enforced checks: repo SDK CLI and gsd-tools.cjs, CJS battery `comm -13 cjs-before cjs-after` empty, hygiene clean on added lines, no version/CHANGELOG change).
- Commit contains only agents/ and dist/bm/agents/ changes; `git log -1 --format=%B` has no Co-Authored-By and no `#NNNN`.
</verification>

<success_criteria>
- 19 source agents (and their 19 dist/bm twins) list Skill in their tools allowlist exactly once; the other 14 agents are byte-identical to HEAD.
- `node bin/build-bm.cjs --check` PASS after regen.
- `agent-skills gsd-executor` with the plugin-skill config still emits the load-by-name directive from the repo SDK CLI and from gsd-tools.cjs (the two enforced checks); the installed bm-sdk is optional best-effort.
- Zero new CJS battery failures, machine-enforced by the Task 2 automated verify via the deterministic scratch path; hygiene greps over added lines clean; no version bump, no CHANGELOG, no Co-Authored-By.
- SUMMARY records the issue number, real gate output, the YAML-list evidence (existing production list-form `tools:` on the two auditors) with its residual, and an honest statement that the live subagent spawn was not re-verified here.
</success_criteria>

<output>
Create `.planning/quick/260919-298-grant-the-skill-tool-to-agents-that-rece/260919-298-SUMMARY.md` when done
</output>
