---
phase: quick-260824-t2h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/frontmatter.cjs
  - sdk/src/query/frontmatter.ts
  - sdk/src/query/frontmatter.test.ts
  - tests/frontmatter-bom.test.cjs
  - bin/lib/core.cjs
  - sdk/src/query/state.ts
  - sdk/src/query/state.test.ts
  - bin/lib/roadmap.cjs
  - sdk/src/query/roadmap.ts
  - sdk/src/query/roadmap.test.ts
  - tests/letter-phase-ids.test.cjs
  - bin/lib/phase.cjs
  - sdk/src/query/phase-lifecycle.ts
  - sdk/src/query/phase-lifecycle.test.ts
  - tests/phase-add-milestone-scope.test.cjs
  - sdk/src/query/phase-lifecycle-policy.ts
  - sdk/src/query/summary.ts
  - sdk/src/query/summary.test.ts
  - tests/one-liner-summary-heading.test.cjs
  - bin/lib/state.cjs
  - sdk/src/query/state-mutation.ts
  - sdk/src/query/state-mutation.test.ts
  - bin/lib/init.cjs
  - sdk/src/query/init-complex.ts
  - sdk/src/query/init-complex.test.ts
  - tests/progress-and-frontier.test.cjs
  - bin/lib/verify.cjs
  - sdk/src/query/validate.ts
  - sdk/src/query/validate.test.ts
  - tests/sentinel-validate-guard.test.cjs
  - skills/progress/SKILL.md
  - skills/stats/SKILL.md
  - skills/version/SKILL.md
  - tests/effort-frontmatter.test.cjs
  - workflows/review.md
  - .github/workflows/check-drift.yml
  - sdk/dist (rebuilt)
  - dist/bm (regenerated)
autonomous: true
requirements: [QUICK-260824-t2h]

must_haves:
  truths:
    - "A file whose byte 0 is a UTF-8 BOM (U+FEFF) followed by a normal frontmatter fence parses to the SAME frontmatter object as the BOM-less file, in BOTH twins (bin/lib/frontmatter.cjs extractFrontmatter and sdk/src/query/frontmatter.ts extractFrontmatterLeading); BOM-less files parse byte-identically to before"
    - "getMilestonePhaseFilter in BOTH twins (bin/lib/core.cjs and sdk/src/query/state.ts) counts a letter-named phase dir (A-tool-output-contract under a '### Phase A:' heading) as in-milestone, keeps numeric dirs and project-code-prefixed dirs matching exactly as before, and still excludes undeclared letter dirs (B-..., M-...)"
    - "roadmap.analyze in BOTH twins (bin/lib/roadmap.cjs and sdk/src/query/roadmap.ts) reports a nonzero phase_count for a roadmap whose headings use letter-prefixed ids like '### Phase B7:'; numeric-only roadmaps produce identical output to before"
    - "phase add and phase add-batch in BOTH twins insert the new '### Phase N:' entry inside the ACTIVE milestone section (before any trailing shipped-archive material whose '---' is the file's last separator), and a roadmap with no resolvable current milestone keeps the legacy whole-file insertion behavior"
    - "extractOneLinerFromBody at ALL THREE sites (bin/lib/core.cjs, sdk/src/query/phase-lifecycle-policy.ts, sdk/src/query/summary.ts) only extracts from a summary-shaped heading (text matching summary|overview|accomplish, case-insensitive) and returns null, never the wrong text, when no such heading with a bold run exists; all three sites share the same final logic including the labeled **One-liner:** form"
    - "state update-progress in BOTH twins is a no-op (updated:false, STATE.md byte-unchanged) when the current-milestone scan finds zero plans, so a shipped [██████████] 100% Progress line survives milestone archive; the legitimate 0% case (plans exist, none summarized) still writes 0%"
    - "init.progress next_phase in BOTH twins follows roadmap order: with roadmap phase 8 pending and unscaffolded plus a stray phase-9 artifact dir on disk, next_phase.number is 8, not 9; aligned trees produce the same next_phase as before"
    - "validate consistency and health checks in BOTH twins (bin/lib/verify.cjs, sdk/src/query/validate.ts) no longer emit W006/W007/consistency/gap warnings for sentinel phases (leading integer 0 or 999, e.g. a 999.1 backlog dir or 0-drafts dir); a genuinely unknown dir like 7-stray still warns"
    - "No skills/*/SKILL.md emits an 'effort:' frontmatter line, in source NOR in dist/bm, and the rewritten tests/effort-frontmatter.test.cjs guards the absence"
    - "Both claude reviewer invocations in workflows/review.md carry the env prefix CLAUDE_CODE_DISABLE_CLAUDE_MDS=1 CLAUDE_CODE_DISABLE_AUTO_MEMORY=1 so the claude lane reviews the same self-contained prompt as the gemini/codex lanes; the CLAUDE_MODEL if/else branch structure is otherwise unchanged"
    - "sdk/dist rebuilt and dist/bm regenerated; node bin/build-bm.cjs --check passes; SDK unit suite green; SDK integration failures are a strict subset of the pre-change baseline (zero NEW failures; do NOT fix the ~14 known baseline); every new CJS regression test green and wired into CI"
    - "No upstream issue/PR numbers, no GSD bookkeeping, and no em/en dashes in any ADDED line under bin/, sdk/src/, tests/, skills/, workflows/; provenance numbers appear only in the task SUMMARY"
  artifacts:
    - path: "tests/frontmatter-bom.test.cjs"
      provides: "CJS BOM-tolerance regression guard"
    - path: "tests/letter-phase-ids.test.cjs"
      provides: "CJS letter-named membership + letter-prefixed analyze guard"
    - path: "tests/phase-add-milestone-scope.test.cjs"
      provides: "CJS active-milestone insertion guard (3 rows incl. legacy fallback)"
    - path: "tests/one-liner-summary-heading.test.cjs"
      provides: "CJS summary-shaped-heading anchor guard"
    - path: "tests/progress-and-frontier.test.cjs"
      provides: "CJS zero-plan progress no-op + roadmap-order next_phase guard"
    - path: "tests/sentinel-validate-guard.test.cjs"
      provides: "CJS sentinel W006/W007/gap suppression guard"
    - path: "tests/effort-frontmatter.test.cjs"
      provides: "Inverted guard: NO skill emits effort: (source + dist/bm)"
    - path: "dist/bm"
      provides: "Regenerated bm plugin carrying the skill + workflow changes"
  key_links:
    - from: "bin/lib/frontmatter.cjs extractFrontmatter"
      to: "sdk/src/query/frontmatter.ts extractFrontmatterLeading"
      via: "identical single-BOM strip before the byte-0 fence match"
    - from: "bin/lib/core.cjs getMilestonePhaseFilter"
      to: "sdk/src/query/state.ts getMilestonePhaseFilter"
      via: "identical longest-first segment-boundary membership loop"
    - from: "bin/lib/phase.cjs cmdPhaseAdd/cmdPhaseAddBatch"
      to: "core.cjs phaseEntryInsertOffset (new helper reusing extractCurrentMilestone range logic)"
      via: "both insertion sites call the offset helper"
    - from: "sdk/src/query/phase-lifecycle.ts phaseAdd/phaseAddBatch"
      to: "sdk/src/query/roadmap.ts range helper twin"
      via: "both SDK insertion sites call the offset helper"
    - from: "skills/*/SKILL.md"
      to: "dist/bm/skills/*/SKILL.md"
      via: "node bin/build-bm.cjs regeneration; effort: absent on both sides"
    - from: "workflows/review.md claude lane"
      to: "CLAUDE_CODE_DISABLE_CLAUDE_MDS=1 CLAUDE_CODE_DISABLE_AUTO_MEMORY=1"
      via: "env prefix on both claude invocations (model branch and default branch)"
---

<objective>
Port a batch of 10 Claude-relevant correctness fixes from upstream gsd-core v1.10.0 + v1.11.0 into both hand-synced resolver twins (CJS bin/lib + SDK sdk/src) plus skills and the review workflow. All 10 are pre-triaged in .planning/notes/upstream-sync-triage-260822.md; the upstream diffs were consulted during planning and their exact logic is transcribed into the task actions below, so no upstream access is required to execute (gh commands are listed per task as optional cross-checks).

Purpose: keep the plugin's parsing/routing behavior aligned with upstream correctness work without taking upstream's unported machinery.
Output: 8 committed changes (one per task) with regression tests, rebuilt sdk/dist, regenerated dist/bm, all gates green. NO version bump, NO CHANGELOG entry (release is cut separately).

Out of scope (do NOT attempt): upstream #2648 (fail-closed phase.complete, needs a retired-plan marker design), #3350 (lowest-outstanding next phase, needs native reimpl), #2868 (workflow-prose resume seam). If any in-scope item turns out to need more than a mechanical port, STOP on that item, leave it unported, and record why in the SUMMARY.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/bm/4.5.5/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/bm/4.5.5/templates/summary.md
</execution_context>

<context>
@.planning/notes/upstream-sync-triage-260822.md
@./CLAUDE.md

Key repo facts (verified during planning):
- Both twins are hand-synced, NOT generated. The bm drift gate is colon-only and will NOT catch a one-twin miss; the SDK vitest suite plus the new CJS tests are the real guard.
- CJS tests are standalone node scripts under tests/*.test.cjs, individually wired into .github/workflows/check-drift.yml (no aggregate runner).
- SDK unit tests are colocated sdk/src/query/*.test.ts, run with `npm --prefix sdk run test` (vitest, project unit). Integration: `npm --prefix sdk run test:integration` with ~14 KNOWN pre-existing golden failures (archived-v4.1 milestone-parse divergence). Require zero NEW failures; do NOT fix the baseline.
- sdk/dist and dist/bm are committed build artifacts. Rebuild: `npm --prefix sdk run build`; regenerate: `node bin/build-bm.cjs`; gate: `node bin/build-bm.cjs --check`.
- HARD hygiene rules for every added line in bin/, sdk/src/, tests/, skills/, workflows/: behavior-only comments, NO upstream issue/PR numbers (they go ONLY in the SUMMARY), no em/en dashes, never the word "canonical".
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture integration baseline, then BOM tolerance in both frontmatter parsers</name>
  <files>bin/lib/frontmatter.cjs, sdk/src/query/frontmatter.ts, sdk/src/query/frontmatter.test.ts, tests/frontmatter-bom.test.cjs</files>
  <action>
    FIRST, before touching any file: capture the SDK integration baseline for the whole batch. Run `npm --prefix sdk run test:integration 2>&1 | grep -E "FAIL|failed" ` and save the list of failing test names/files to `.planning/quick/260824-t2h-port-batch-of-10-upstream-gsd-core-claud/integration-baseline.txt` (expected ~14 failures). Task 8 compares against this file.

    Then the fix. In bin/lib/frontmatter.cjs `extractFrontmatter` (line ~94), before the byte-0 fence match, strip a single leading UTF-8 BOM: when `content.charCodeAt(0) === 0xFEFF`, reassign `content = content.slice(1)`. Add a short behavior comment (Windows tooling such as PowerShell Out-File writes a BOM by default; without the strip the byte-0 fence match fails and the parse collapses to an empty object so every frontmatter field silently vanishes). Scope is BOM only: one codepoint, no broader tolerance of pre-fence whitespace or comments, and do NOT touch spliceFrontmatter, parseMustHavesBlock, or any other fence site (upstream scoped the fix to the parse entry; keep both twins on that same scope).

    Mirror the identical strip at the top of sdk/src/query/frontmatter.ts `extractFrontmatterLeading` (line ~195, the shared entry that `extractFrontmatter` delegates to). Do NOT touch stripFrontmatter or parseMustHavesBlock in the SDK either.

    Tests, RED first: write tests/frontmatter-bom.test.cjs (standalone node script following the repo convention: fail()/ok() counters, exit 1 on failure) covering: (1) a BOM-prefixed doc `'\uFEFF---\nphase: 01-x\nplan: 2\n---\nbody'` parses to the same object as its BOM-less twin; (2) BOM-less docs parse unchanged; (3) a doc with NO frontmatter but a leading BOM still returns an empty object; (4) a BOM appearing later in the body does not affect parsing. Run the test BEFORE applying the CJS change and record the failure (RED proof), then apply and re-run. Add the same four cases to sdk/src/query/frontmatter.test.ts against extractFrontmatterLeading and extractFrontmatter.

    Commit: `fix(frontmatter): tolerate a leading UTF-8 BOM before the frontmatter fence (both twins)`.

    Optional upstream cross-check: `gh pr diff 3076 --repo open-gsd/gsd-core`.
  </action>
  <verify>node tests/frontmatter-bom.test.cjs && (cd sdk && npx vitest run --project unit src/query/frontmatter.test.ts)</verify>
  <done>BOM-prefixed frontmatter parses identically to BOM-less in both twins; RED-then-GREEN recorded for the CJS test; integration-baseline.txt exists with the pre-change failing list.</done>
</task>

<task type="auto">
  <name>Task 2: Letter-named phase membership + letter-prefixed analyze ids (both twins)</name>
  <files>bin/lib/core.cjs, sdk/src/query/state.ts, bin/lib/roadmap.cjs, sdk/src/query/roadmap.ts, tests/letter-phase-ids.test.cjs, sdk/src/query/state.test.ts, sdk/src/query/roadmap.test.ts</files>
  <action>
    Part A, milestone membership. In bin/lib/core.cjs `getMilestonePhaseFilter` (isDirInMilestone, lines ~1933-1953) and sdk/src/query/state.ts `getMilestonePhaseFilter` (lines ~67-87): the custom-ID branch currently captures with the greedy `^([A-Za-z][A-Za-z0-9]*(?:-[A-Za-z0-9]+)*)`, which swallows the whole slug, so `A-tool-output-contract` never matches a declared id `a` and letter-named phases fall out of the milestone. Replace that branch, in both twins identically, with a segment-boundary membership test scoped to LETTER-LEADING dir names: precompute (next to the existing `normalized` set) `normalizedIdsLongestFirst = [...normalized].sort((a, b) => b.length - a.length)` (longest-first so a declared hyphenated id like `proj-42` is tested before its prefix `proj`); then in isDirInMilestone, when `/^[A-Za-z]/.test(dirName)`, compute `lowerDir = dirName.toLowerCase()` and return true when for any id `lowerDir === id || lowerDir.startsWith(id + '-')`. Keep the numeric branch FIRST and the project-code strip-and-retry branch AFTER, both unchanged: the letter-leading scope keeps numeric continuation dirs (e.g. 14-02-photos vs phase 14) owned by the numeric grammar. Behavior comment only, no upstream numbers.

    Part B, analyze id widening. In bin/lib/roadmap.cjs `cmdRoadmapAnalyze`: heading capture at line ~207 changes from `(\d+[A-Z]?(?:\.\d+)*)` to `([A-Za-z]?\d+[A-Z]?(?:\.\d+)*)`; the section-boundary regex at line ~230 changes `Phase\s+\d` to `Phase\s+[A-Za-z]?\d`; the checklist pattern at line ~324 gets the same `[A-Za-z]?` widening. Mirror in sdk/src/query/roadmap.ts `roadmapAnalyze`: heading pattern at line ~646, its nextHeader boundary regex, and the checklist pattern at line ~748. Leave `extractPhasesFromSection` (roadmap.ts ~393) and every non-analyze regex untouched. Note the existing sentinel skip uses parseInt, which returns NaN for a letter-prefixed id and therefore does not skip it; that is the intended outcome, leave it.

    Tests: tests/letter-phase-ids.test.cjs with two sections. Membership: temp project whose ROADMAP declares `### Phase A: Tool Output Contract` + `### Phase 01: Inventory`; require phaseCount 2, filter('A-tool-output-contract') true, filter('01-inventory') true, filter('B-evidence-artifact-contract') false; plus a 13-phase A..L fixture requiring all letter dirs in-milestone and an undeclared `M-...` excluded; plus a `### Phase PROJ-42:` declaration still matching dir `PROJ-42-description` (hyphenated declared id via the startsWith segment rule). Analyze: run `node bin/gsd-tools.cjs roadmap analyze` (confirm the exact verb with `node bin/gsd-tools.cjs --help` or by grepping the dispatcher) in a fixture with `### Phase B7: Widget` and require the phase to appear / phase_count nonzero, and a numeric-only fixture to produce the same phases as before the change. Mirror the membership cases in sdk/src/query/state.test.ts and the analyze cases in sdk/src/query/roadmap.test.ts (call the handlers directly, no dist build needed).

    Commit: `fix(roadmap): count letter-named phase dirs in milestone membership and accept letter-prefixed ids in analyze (both twins)`.

    Optional upstream cross-check: `gh pr diff 3368 --repo open-gsd/gsd-core` and `gh pr diff 3117 --repo open-gsd/gsd-core`.
  </action>
  <verify>node tests/letter-phase-ids.test.cjs && (cd sdk && npx vitest run --project unit src/query/state.test.ts src/query/roadmap.test.ts)</verify>
  <done>Letter-named dirs count toward milestone progress in both twins, letter-prefixed heading ids survive analyze in both twins, numeric and project-code-prefixed behavior unchanged, all named accept/reject rows green.</done>
</task>

<task type="auto">
  <name>Task 3: Scope phase add / add-batch insertion to the active milestone (both twins)</name>
  <files>bin/lib/core.cjs, bin/lib/phase.cjs, sdk/src/query/roadmap.ts, sdk/src/query/phase-lifecycle.ts, tests/phase-add-milestone-scope.test.cjs, sdk/src/query/phase-lifecycle.test.ts</files>
  <action>
    Problem: both twins compute the insertion point for a new `### Phase N:` entry with a whole-file `lastIndexOf('\n---')` (phase.cjs ~671 and ~745; phase-lifecycle.ts ~192 and ~312). On a long roadmap whose file-last `---` sits inside shipped/backlog material, the new phase lands under the archive.

    CJS: in bin/lib/core.cjs, refactor `extractCurrentMilestone` (lines ~1069-1183) so its version-resolution + section-locating steps (STATE.md `milestone:` lookup, in-progress marker fallback, sectionPattern match, computeSectionEnd) are shared with a new exported helper `currentMilestoneSectionRange(content, cwd)` that returns `{ start, end }` for the current milestone's PRIMARY window in the raw content, or null when no version resolves or the heading is not found. extractCurrentMilestone's output must stay byte-identical for all existing inputs (it keeps its preamble + phase-details concatenation on top of the shared range). Then add `phaseEntryInsertOffset(rawContent, cwd)`: when the range is null, legacy behavior (`lastIndexOf('\n---')`, and when that is <= 0 return rawContent.length); when the range exists, take `window = rawContent.slice(start, end)`, `lastSeparator = window.lastIndexOf('\n---')`, and return `start + lastSeparator` when lastSeparator > 0, else `end`. Export both from core.cjs. Replace the two insertion sites in phase.cjs (`cmdPhaseAdd` ~669-676 and `cmdPhaseAddBatch` ~745-748) with `const insertAt = phaseEntryInsertOffset(rawContent, cwd)` and a single slice-splice. Leave cmdPhaseInsert (decimal path) untouched, it is already anchored.

    SDK: mirror in sdk/src/query/roadmap.ts next to its `extractCurrentMilestone`: an async `currentMilestoneSectionRange(content, projectDir, workstream?)` sharing the extractor's resolution steps (same byte-identical guarantee), plus `phaseEntryInsertOffset(rawContent, projectDir, workstream?)` with the same null-fallback and window semantics. Export them and replace the two `lastIndexOf('\n---')` sites in phase-lifecycle.ts `phaseAdd` (~192) and `phaseAddBatch` (~312); both call sites are already inside async modifiers.

    Tests, port the three upstream rows into tests/phase-add-milestone-scope.test.cjs (invoking `node bin/gsd-tools.cjs phase add ...` and `phase add-batch --descriptions ...` in temp fixtures): (1) fixture with STATE.md `milestone: v1.0`, a `## v1.0: Active Milestone` section holding Phase 1 + Phase 2, then `---`, then a `## v0.9: Shipped Archive` block whose own trailing `---` is the file's LAST separator; `phase add New Feature` must produce `### Phase 3: New Feature` positioned AFTER Phase 2 and BEFORE the `## v0.9` heading. (2) same fixture, `phase add-batch` with two descriptions, both entries before the archive. (3) no-milestone fallback: ROADMAP with one phase, no STATE.md milestone field and no in-progress marker, trailing `---`; the new phase still inserts before that trailing separator (legacy behavior preserved). Mirror rows 1 and 3 in sdk/src/query/phase-lifecycle.test.ts against the phaseAdd handler.

    Commit: `fix(phase): insert new phase entries inside the active milestone section, not before the archive's trailing separator (both twins)`.

    Optional upstream cross-check: `gh pr diff 3400 --repo open-gsd/gsd-core` (note upstream uses a currentMilestoneRawRanges helper we do not have; the range refactor above is the local equivalent).
  </action>
  <verify>node tests/phase-add-milestone-scope.test.cjs && (cd sdk && npx vitest run --project unit src/query/phase-lifecycle.test.ts src/query/roadmap.test.ts)</verify>
  <done>All three rows green in CJS, rows 1+3 green in SDK; existing phase add/add-batch/insert tests in both suites still green; extractCurrentMilestone output unchanged for existing fixtures.</done>
</task>

<task type="auto">
  <name>Task 4: Anchor one-liner extraction to a summary-shaped heading (three sites)</name>
  <files>bin/lib/core.cjs, sdk/src/query/phase-lifecycle-policy.ts, sdk/src/query/summary.ts, tests/one-liner-summary-heading.test.cjs, sdk/src/query/summary.test.ts, sdk/src/query/phase-lifecycle.test.ts</files>
  <action>
    Problem: `extractOneLinerFromBody` takes the FIRST heading's first bold run, so a rule-list or deviation-note heading at the top of a SUMMARY writes garbage into MILESTONES.md accomplishments and one_liner. Three sites: bin/lib/core.cjs (~1682, the full version with the labeled **One-liner:** branch), sdk/src/query/phase-lifecycle-policy.ts (~54) and sdk/src/query/summary.ts (~30) (both SDK copies are older, bare-bold only).

    Replace all three with ONE identical final logic (bring the SDK copies up to the CJS labeled/bare handling as part of this change): normalize CRLF to LF, strip leading frontmatter, then iterate headings in document order with a global multiline regex shaped like `^#+\s*([^\n]*)\n+\*\*([^*\n]+)\*\*([^\n]*)` capturing heading text, first bold run, and the rest of that line. Accept only headings whose text matches `/summary|overview|accomplish/i`. For an accepted heading apply the existing disambiguation: when the bold run ends with a colon (a label), the trimmed prose AFTER the bold on the same line is the one-liner (skip to the next matching heading when that prose is empty); otherwise the trimmed bold content itself is the one-liner (when non-empty). When no summary-shaped heading yields a value, return null, never a fallback to the wrong heading.

    Tests: tests/one-liner-summary-heading.test.cjs plus mirror cases in sdk/src/query/summary.test.ts and (for the policy copy) sdk/src/query/phase-lifecycle.test.ts or wherever that copy is already exercised (grep for extractOneLinerFromBody usages in the SDK test suite first; add a direct-import test if none exists). Cases: (1) doc whose first heading is `## Deviation rules` with a bold line, followed by `# Phase 4: Foo Summary` and `**One-liner:** real prose here.` returns "real prose here."; (2) bare-bold form under a Summary heading returns the bold text; (3) doc with NO summary-shaped heading returns null; (4) labeled form with empty prose under Summary followed by nothing returns null; (5) existing happy-path (summary heading first) unchanged. Since the CJS export list already carries extractOneLinerFromBody, the CJS test can require core.cjs directly.

    Commit: `fix(summary): extract the one-liner only from a summary-shaped heading, returning null over wrong text (all three sites)`.

    Optional upstream cross-check: `gh pr diff 3401 --repo open-gsd/gsd-core`.
  </action>
  <verify>node tests/one-liner-summary-heading.test.cjs && (cd sdk && npx vitest run --project unit src/query/summary.test.ts src/query/phase-lifecycle.test.ts)</verify>
  <done>All five cases green at the CJS site and both SDK sites; the three implementations are logic-identical (labeled + bare + null-fallback); existing one-liner consumers' tests still green.</done>
</task>

<task type="auto">
  <name>Task 5: Zero-plan progress no-op + roadmap-order next_phase (both twins)</name>
  <files>bin/lib/state.cjs, sdk/src/query/state-mutation.ts, bin/lib/init.cjs, sdk/src/query/init-complex.ts, tests/progress-and-frontier.test.cjs, sdk/src/query/state-mutation.test.ts, sdk/src/query/init-complex.test.ts</files>
  <action>
    Part A, zero-plan no-op. In bin/lib/state.cjs `cmdStateUpdateProgress` (after the milestone scan, before the percent computation at ~414) and sdk/src/query/state-mutation.ts `stateUpdateProgress` (~719): when `totalPlans === 0`, return early WITHOUT calling readModifyWriteStateMd, with `{ updated: false, reason: 'no plans found in current-milestone phases, STATE.md left unchanged (milestone archived?)' }` (CJS via output(..., raw, 'false'); SDK via the data return). Rationale comment: zero plans means nothing to measure (typically the milestone was just archived and phases/ is empty); mapping 0/0 to 0% would clobber a shipped 100% Progress record. The legitimate 0% case (plans exist, none summarized) still flows through because totalPlans > 0.

    Part B, frontier from roadmap order. In bin/lib/init.cjs `cmdInitProgress`, immediately after the `phases.sort(...)` (~1490), and in sdk/src/query/init-complex.ts `initProgress` after its sort (~449): re-derive nextPhase from the sorted union: `const frontier = phases.find(p => p.status === 'pending' || p.status === 'not_started')`; when found, assign it to nextPhase (otherwise keep the loop-derived value, which covers the all-complete milestone keeping null). Comment: the frontier is roadmap order, not artifact presence; the disk loop above can claim nextPhase from a stray out-of-order artifact dir (a phase-9 evidence dir while roadmap phase 8 is pending and unscaffolded), silently skipping 8. Our status field already folds roadmap checkbox completeness in (checkbox-complete entries carry status complete), so no extra roadmap_complete check is needed. Scope strictly to initProgress in both twins; initManager stays untouched.

    Tests: tests/progress-and-frontier.test.cjs. (A-1) temp project, STATE.md with `Progress: [██████████] 100%`, milestone-scoped phases dir EMPTY (or absent); run `node bin/gsd-tools.cjs state update-progress`; require updated:false in the output AND STATE.md byte-identical before/after. (A-2) same but a phase dir with 2 plans + 1 paired summary; require the Progress line rewritten to 50%. (A-3) plans exist, zero summaries: Progress rewritten to 0% (legitimate-zero case preserved). (B-1) ROADMAP with `### Phase 8:` (pending, no dir) and `### Phase 9:`, a disk dir `09-thing/` containing only a UAT/evidence markdown (no PLAN files); run `node bin/gsd-tools.cjs init progress` (confirm the exact verb from the dispatcher) and require next_phase.number equal to 8. (B-2) aligned tree (dir exists for the pending phase) still returns the same next_phase as before. Mirror A-1/A-2 in sdk/src/query/state-mutation.test.ts and B-1/B-2 in sdk/src/query/init-complex.test.ts via direct handler calls.

    Commit: `fix(state,init): keep shipped progress on zero-plan milestone scans and derive next_phase from roadmap order (both twins)`.

    Optional upstream cross-check: `gh pr diff 3375 --repo open-gsd/gsd-core` and `gh pr diff 3603 --repo open-gsd/gsd-core`.
  </action>
  <verify>node tests/progress-and-frontier.test.cjs && (cd sdk && npx vitest run --project unit src/query/state-mutation.test.ts src/query/init-complex.test.ts)</verify>
  <done>Shipped 100% survives a zero-plan scan in both twins; 50% and legitimate-0% paths unchanged; stray higher-numbered artifact dir no longer skips a pending roadmap phase in init.progress in both twins; existing init-progress precedence tests still green.</done>
</task>

<task type="auto">
  <name>Task 6: Sentinel-phase guard on W006/W007 + consistency loops (both twins)</name>
  <files>bin/lib/verify.cjs, sdk/src/query/validate.ts, tests/sentinel-validate-guard.test.cjs, sdk/src/query/validate.test.ts</files>
  <action>
    Problem: a 999.x backlog or 0.x draft dir on disk (conventions this repo itself uses), or a sentinel heading in the roadmap, fires spurious `W007: Phase 999 exists on disk but not in ROADMAP.md` (and the mirror W006 / consistency warnings) and flips validate status to degraded on every run.

    There is no shared isSentinelPhaseId helper in this tree (the sentinel rule exists only inline in roadmap.cjs as `major === 0 || major === 999`). Add a module-local predicate, identical in both files: `isSentinelPhaseId(p)` matches `String(p)` against `/^0*(\d+)/` and returns true when the parsed leading integer is exactly 0 or 999; non-matching (letter-led) ids are never sentinels. Behavior comment: sentinel phases (0 drafts, 999 backlog) are never-on-roadmap by convention, so both "missing dir" and "missing roadmap entry" warnings are wrong for them, and they are never part of sequential numbering.

    Apply in bin/lib/verify.cjs at: (1) the consistency check's roadmap-but-not-on-disk loop (~511-515: `if (isSentinelPhaseId(p)) continue;` first line of the loop body); (2) the disk-but-not-in-roadmap loop (~518-523, same guard); (3) the sequential gap filter (~528-530: extend `.filter(p => !p.includes('.'))` with `&& !isSentinelPhaseId(p)` so a 999 dir cannot produce a "Gap in phase numbering: N to 999" warning); (4) the W006 loop (~850-857) and (5) the W007 loop (~860-865). Mirror all five in sdk/src/query/validate.ts: consistency loops (~237-248), gap filter (~261-264), W006 (~652-660) and W007 (~662-667) loops.

    Tests: tests/sentinel-validate-guard.test.cjs running `node bin/gsd-tools.cjs validate consistency` and `validate health` (confirm verbs from the dispatcher) against fixtures: (1) phases dir with `01-real/`, `999-backlog/`, `999.1-icebox/`, `0-drafts/` and a ROADMAP declaring only Phase 1: NO warning mentioning 999 or 0 in either command's output; (2) an undeclared `7-stray/` dir still produces the disk-but-not-roadmap warning (guard is scoped, not a blanket suppression); (3) a `### Phase 999.1:` roadmap heading with no dir does not fire W006; (4) no "Gap in phase numbering" warning naming 999. Mirror in sdk/src/query/validate.test.ts via the handlers.

    Commit: `fix(validate): stop sentinel phases (0/999) from firing missing-dir and missing-roadmap warnings (both twins)`.

    Optional upstream cross-check: `gh pr diff 3371 --repo open-gsd/gsd-core`.
  </action>
  <verify>node tests/sentinel-validate-guard.test.cjs && (cd sdk && npx vitest run --project unit src/query/validate.test.ts)</verify>
  <done>Sentinel dirs and headings produce zero warnings across consistency + health in both twins, non-sentinel strays still warn, gap check ignores sentinels, existing validate tests green.</done>
</task>

<task type="auto">
  <name>Task 7: Remove skill effort frontmatter + isolate the claude reviewer lane</name>
  <files>skills/progress/SKILL.md, skills/stats/SKILL.md, skills/version/SKILL.md, tests/effort-frontmatter.test.cjs, workflows/review.md</files>
  <action>
    Part A, effort removal. Delete the `effort: low` frontmatter line from skills/progress/SKILL.md (line 5), skills/stats/SKILL.md (line 4), skills/version/SKILL.md (line 4). Then `grep -rn "^effort:" skills/*/SKILL.md` and remove any other hits (planning-time grep found only these three). Rationale: Claude Code reads SKILL.md effort: as output_config.effort; whenever the session baseline differs from the pinned value, invoking the skill flips effort and invalidates the prompt cache at both scope boundaries. This deliberately reverses the earlier effort:low adoption; tests/effort-frontmatter.test.cjs currently asserts the OPPOSITE and must be rewritten in the same change, not deleted: invert it into a guard that scans EVERY skills/*/SKILL.md frontmatter block (and dist/bm/skills/*/SKILL.md when that directory exists) and FAILS on any `^effort:` line, with the cache-invalidation rationale in its header comment. Note: dist/bm still carries the old lines until Task 8 regenerates it; the dist/bm half of the guard will go green there (run only the source half now, or make the dist/bm scan tolerant of a missing dir but strict once present, and accept the expected dist failure until Task 8).

    Part B, reviewer lane isolation. In workflows/review.md, the two claude invocations (~197 model branch, ~199 default branch) currently inherit the orchestrating environment, so the claude lane alone reviews with the user/project CLAUDE.md and auto-memory loaded while the gemini/codex lanes see only the self-contained prompt. Prefix BOTH invocations, immediately before the `claude` token, with `CLAUDE_CODE_DISABLE_CLAUDE_MDS=1 CLAUDE_CODE_DISABLE_AUTO_MEMORY=1 ` (both variables: CLAUDE.md loading and auto-memory are independently toggled mechanisms, and an explicit =1 also defeats an inherited =0 export). Keep the if/else CLAUDE_MODEL structure and everything else byte-identical. Planning-time grep confirmed these are the only two claude reviewer invocations in workflows/.

    Commit: `fix(skills,review): drop effort frontmatter from skills and stop the claude reviewer lane inheriting CLAUDE.md and auto-memory`.
  </action>
  <verify>bash -c 'set -e; ! grep -rn "^effort:" skills/*/SKILL.md; test "$(grep -c "CLAUDE_CODE_DISABLE_CLAUDE_MDS=1 CLAUDE_CODE_DISABLE_AUTO_MEMORY=1 claude" workflows/review.md)" = 2; node tests/effort-frontmatter.test.cjs; echo PASS'</verify>
  <done>Zero effort: lines in source skills, inverted guard green against source, both claude invocations carry the two-variable env prefix, CLAUDE_MODEL branching intact.</done>
</task>

<task type="auto">
  <name>Task 8: Batch gates: rebuild, full suites, integration baseline, dist/bm regen, hygiene, CI wiring</name>
  <files>sdk/dist, dist/bm, .github/workflows/check-drift.yml</files>
  <action>
    Run the whole-batch gates in order; every gate is blocking.

    1. Rebuild the SDK: `npm --prefix sdk run build`. Then live-prove one SDK-path fix through the built CLI, e.g. run `node sdk/dist/cli.js query state.load` or `query init.progress` inside the Task 5 B-1 fixture and confirm next_phase is the pending roadmap phase.
    2. CJS battery: run every new test file (frontmatter-bom, letter-phase-ids, phase-add-milestone-scope, one-liner-summary-heading, progress-and-frontier, sentinel-validate-guard, effort-frontmatter) plus the full existing set: `for f in tests/*.test.cjs; do node "$f" || { echo "FAIL $f"; exit 1; }; done`. If any PRE-EXISTING file fails, verify it also fails on the pre-batch commit (git stash or git worktree on HEAD~) before treating it as baseline; zero NEW failures allowed.
    3. SDK unit: `npm --prefix sdk run test` with zero failures.
    4. SDK integration: `npm --prefix sdk run test:integration`; diff the failing set against `.planning/quick/260824-t2h-port-batch-of-10-upstream-gsd-core-claud/integration-baseline.txt` from Task 1. The post-change failing set MUST be a subset of the baseline (~14 known failures); zero NEW failures; do NOT fix the baseline.
    5. Regenerate dist/bm: `node bin/build-bm.cjs`, then `node bin/build-bm.cjs --check` must PASS.
    6. Carry-through greps on dist/bm: `grep -rn "^effort:" dist/bm/skills/*/SKILL.md` must be empty; `grep -c "CLAUDE_CODE_DISABLE_CLAUDE_MDS=1 CLAUDE_CODE_DISABLE_AUTO_MEMORY=1 claude" dist/bm/workflows/review.md` must be 2; re-run `node tests/effort-frontmatter.test.cjs` (now covering dist/bm too).
    7. Hygiene greps over the batch diff: `git diff <pre-batch-commit>..HEAD -- bin sdk/src tests skills workflows | grep "^+" | grep -nE "#[0-9]{4}"` must be empty (no upstream provenance numbers in product lines) and the same added-lines diff must contain no em/en dash characters and not the word "canonical".
    8. CI wiring: add the six new CJS test files to .github/workflows/check-drift.yml alongside the existing `node tests/...` regression steps (same job that runs worktree-safety and phase-complete-backlog-sentinel), plus tests/effort-frontmatter.test.cjs which was previously unwired.
    9. Commit sdk/dist + dist/bm + check-drift.yml: `chore(build): rebuild sdk/dist, regenerate dist/bm, wire upstream-port regression tests into CI`. Do NOT bump plugin.json/marketplace.json versions and do NOT touch CHANGELOG (release is cut separately).

    Then write the task SUMMARY (this is the ONLY place upstream numbers belong): map each of the 10 items to its commit, note the upstream PR numbers (#2977/#3076, #3213/#3368, #3163/#3400, #3170/#3401, #3233/#3375, #3581/#3603, #3225/#3371, #3036/#3117, #3151/#3425, #2483/#2493), record any item left unported with the reason, and record the integration baseline comparison.
  </action>
  <verify>bash -c 'set -e; npm --prefix sdk run test >/dev/null 2>&1 && echo "sdk unit OK"; node bin/build-bm.cjs --check; ! grep -rn "^effort:" dist/bm/skills/*/SKILL.md; test "$(grep -c "CLAUDE_CODE_DISABLE_CLAUDE_MDS=1 CLAUDE_CODE_DISABLE_AUTO_MEMORY=1 claude" dist/bm/workflows/review.md)" = 2; echo ALL GATES PASS'</verify>
  <done>SDK rebuilt and one fix live-proven through sdk/dist/cli.js; full CJS battery zero-new; SDK unit green; integration failing set a strict subset of the Task 1 baseline; build-bm --check PASS; dist/bm carries the effort removal and the reviewer env prefix; zero provenance numbers / em-dashes / "canonical" in added product lines; new tests wired into CI; SUMMARY written with the item-to-commit map.</done>
</task>

</tasks>

<verification>
- Both twins behavior-identical on every ported fix (the new CJS tests and SDK vitest twins encode the same fixtures on both sides).
- All 8 commits atomic and independently revertable; no version bump, no CHANGELOG.
- Integration baseline discipline honored: post-change failures are a subset of the pre-change list captured in Task 1.
- dist/bm regenerated once at the end and gated by build-bm --check plus content greps.
</verification>

<success_criteria>
All must_haves truths hold, all 8 task verify commands pass, and the SUMMARY maps each of the 10 triage items to a commit (or records a justified stop for any item that turned out to need design work beyond a mechanical port).
</success_criteria>

<output>
Create `.planning/quick/260824-t2h-port-batch-of-10-upstream-gsd-core-claud/260824-t2h-SUMMARY.md` when done.
</output>
