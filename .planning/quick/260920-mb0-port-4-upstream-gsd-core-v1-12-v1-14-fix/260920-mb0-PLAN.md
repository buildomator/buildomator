---
phase: 260920-mb0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sdk/src/query/state-document.ts
  - sdk/scripts/gen-state-document.mjs
  - sdk/package.json
  - bin/lib/state-document.generated.cjs
  - bin/lib/state.cjs
  - sdk/src/query/state.ts
  - bin/lib/init.cjs
  - sdk/src/query/init-complex.ts
  - bin/lib/core.cjs
  - sdk/src/query/helpers.ts
  - bin/lib/roadmap.cjs
  - sdk/src/query/roadmap.ts
  - bin/lib/phase.cjs
  - sdk/src/query/phase-lifecycle.ts
  - sdk/src/query/state-document.test.ts
  - sdk/src/query/state.test.ts
  - sdk/src/query/init-complex.test.ts
  - sdk/src/query/roadmap.test.ts
  - sdk/src/query/phase-lifecycle.test.ts
  - tests/state-field-line-anchor.test.cjs
  - tests/state-document-generated-fresh.test.cjs
  - tests/decimal-phase-order-init.test.cjs
  - tests/roadmap-analyze-line-anchor.test.cjs
  - tests/phase-complete-lowest-successor.test.cjs
  - .github/workflows/check-drift.yml
  - sdk/dist/
  - dist/bm/
autonomous: true
requirements: []
tags: [upstream-sync, state-md, phase-ordering, roadmap-analyze, regex-hardening]

must_haves:
  truths:
    - "Replacing an empty bold STATE.md field (`**Status:**` followed directly by a newline) never consumes the newline or overwrites the next line, in both the CJS module and the built SDK"
    - "A mid-line `**Field:**` mention in prose is never read as the field row and never overwritten by a field write, for both the bold read (stateExtractField, state get) and the bold write (stateReplaceField)"
    - "bin/lib/state-document.generated.cjs is produced from sdk/src/query/state-document.ts by a real, committed generator (`cd sdk && npm run gen:state-document`) and a test proves the committed file is fresh"
    - "init progress orders phases decimal-aware (7, 7.1, 7.2, 7.10, 8) and next_phase is the lowest pending phase in that order, in both twins"
    - "roadmap analyze does not count a `## Phase N:` heading that sits inside a fenced code block, a blockquote, or mid-line prose; phase_count reflects only real line-start headings, in both twins"
    - "phase complete's ROADMAP-only fallback picks the numerically lowest successor (decimal-aware), not the first heading in document order, in both twins"
    - "Full CJS battery, SDK unit suite, and SDK integration suite show zero NEW failures versus the pre-edit baseline; dist/bm is regenerated and `node bin/build-bm.cjs --check` passes"
  artifacts:
    - path: "sdk/scripts/gen-state-document.mjs"
      provides: "Deterministic TS to CJS generator for the STATE.md document module"
      min_lines: 25
    - path: "bin/lib/state-document.generated.cjs"
      provides: "Regenerated CJS twin with line-anchored, same-line-whitespace bold field patterns"
      contains: "^[ \\\\t]*\\\\*\\\\*"
    - path: "sdk/src/query/state-document.ts"
      provides: "Single source: anchored stateExtractField and stateReplaceField"
      contains: "'im'"
    - path: "tests/state-field-line-anchor.test.cjs"
      provides: "CJS regression: empty-field write does not eat next line; prose lookalike not matched; state get anchored"
    - path: "tests/state-document-generated-fresh.test.cjs"
      provides: "Generator freshness gate (regenerate to temp, byte-compare to committed)"
    - path: "tests/decimal-phase-order-init.test.cjs"
      provides: "CJS regression: init progress decimal ordering + next_phase"
    - path: "tests/roadmap-analyze-line-anchor.test.cjs"
      provides: "CJS regression: fenced/blockquote/prose Phase headings not counted"
    - path: "tests/phase-complete-lowest-successor.test.cjs"
      provides: "CJS regression: out-of-order roadmap yields lowest successor"
  key_links:
    - from: "bin/lib/state-document.generated.cjs"
      to: "sdk/src/query/state-document.ts"
      via: "sdk/scripts/gen-state-document.mjs (npm run gen:state-document)"
      pattern: "Regenerate: cd sdk && npm run gen:state-document"
    - from: "bin/lib/init.cjs"
      to: "bin/lib/core.cjs comparePhaseNum"
      via: "phase dir sort and phases.sort"
      pattern: "comparePhaseNum\\("
    - from: "sdk/src/query/init-complex.ts"
      to: "sdk/src/query/helpers.ts comparePhaseNum"
      via: "import from './helpers.js'"
      pattern: "comparePhaseNum"
    - from: "bin/lib/roadmap.cjs"
      to: "bin/lib/core.cjs maskFencedBlocks"
      via: "scan content for phasePattern / checklistPattern"
      pattern: "maskFencedBlocks\\("
    - from: "bin/lib/phase.cjs"
      to: "comparePhaseNum"
      via: "lowest-successor selection in ROADMAP fallback"
      pattern: "comparePhaseNum\\(pm\\[1\\], nextPhaseNum\\) < 0"
---

<objective>
Port four Claude-relevant correctness fixes from upstream gsd-core v1.12 to v1.14 into both twins (CJS `bin/lib/*.cjs` and SDK `sdk/src/**`), with regression tests, a regenerated single-source STATE.md module, a rebuilt SDK, and a regenerated `dist/bm`. Ships in v4.8.0 alongside the completed bm-<role> agent rename; this task does NOT bump versions or touch CHANGELOG.

Purpose: Item 1 is the same data-loss family as the v4.7.1 #32 fix but on a function that fix never touched (`stateReplaceField` bold pattern uses `\s*`, which spans newlines and can overwrite the line after an empty field; neither bold read nor write is line-anchored so prose lookalikes win). Items 2 to 4 are cheap ordering/anchoring hardening in init, roadmap analyze, and phase complete.

Output: edited twins, a new committed generator (`sdk/scripts/gen-state-document.mjs` + `gen:state-document` npm script), five new CJS regression tests, appended SDK vitest cases, rebuilt `sdk/dist`, regenerated `dist/bm`, all gates green with zero new failures.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/bm/4.7.1/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/bm/4.7.1/templates/summary.md
</execution_context>

<context>
@/Users/jnuyens/src/gsd-plugin/.planning/notes/upstream-sync-triage-260920.md
@/Users/jnuyens/src/gsd-plugin/CLAUDE.md
@/Users/jnuyens/src/gsd-plugin/sdk/src/query/state-document.ts
@/Users/jnuyens/src/gsd-plugin/bin/lib/state-document.generated.cjs
@/Users/jnuyens/src/gsd-plugin/tests/progress-and-frontier.test.cjs
@/Users/jnuyens/src/gsd-plugin/tests/phase-complete-backlog-sentinel.test.cjs
@/Users/jnuyens/src/gsd-plugin/tests/letter-phase-ids.test.cjs

<planner_findings>
Facts established during planning (use directly, do not re-derive):

- **No generator exists.** The header of `bin/lib/state-document.generated.cjs` says `Regenerate: cd sdk && npm run gen:state-document`, but no such script exists in `sdk/package.json`, `sdk/scripts/`, or anywhere in the tree (the file was byte-synced from upstream in commit ceee2cc). The committed CJS also lags the `.ts`: it exports only 7 functions (`stateExtractField, stateReplaceField, stateReplaceFieldWithFallback, normalizeStateStatus, computeProgressPercent, shouldPreserveExistingProgress, normalizeProgressNumbers`) and omits `KNOWN_TEMPLATE_DEFAULTS`, `isStateTemplateDefault`, `stateReplaceFieldIfTemplate`, `stateReplaceFieldIfTemplateWithFallback`. Additive exports are safe: the only CJS consumer is `bin/lib/state-document.cjs`, a thin `module.exports = require('./state-document.generated.cjs')`.
- `typescript` (^5.7) and `esbuild` are already sdk devDependencies. `ts.transpileModule` with `module: CommonJS` is the generator mechanism; no new packages (so no package legitimacy gate applies).
- The `.ts` contains one data value in `KNOWN_TEMPLATE_DEFAULTS` written with a literal U+2014 (em-dash) character: the `Phase complete ... ready for verification` entry directly above its ASCII-hyphen variant. It must keep matching legacy STATE.md text, but a regenerated CJS would add that line to `bin/lib` and trip the hygiene gate. Rewrite the character as the JavaScript unicode escape for U+2014 (a backslash, the letter u, then 2014) inside the same string literal; the runtime string is byte-identical.
- Bold-pattern sites: `state-document.ts:14` (extract, `[ \t]*` but no `^`/`m`), `:24` (replace, `\s*`, no `^`/`m`), `:28` (plain replace also `\s*`, spans newlines the same way). Upstream also anchored the `state get` reader; ours is `bin/lib/state.cjs:101` and `sdk/src/query/state.ts:319` (`\\*\\*${fieldEscaped}:\\*\\*\\s*(.*)`, flag `i`).
- Decimal comparator: CJS `comparePhaseNum` at `bin/lib/core.cjs:836` (exported at :2165); SDK `comparePhaseNum` at `sdk/src/query/helpers.ts:231`. `init.cjs:8` does NOT yet destructure `comparePhaseNum` from core.cjs; `init-complex.ts:29-36` imports from `./helpers.js` but not `comparePhaseNum`.
- parseInt sort sites: `bin/lib/init.cjs:1414-1419` (dir sort) and `:1490` (phases.sort); `sdk/src/query/init-complex.ts:361-366` and `:449`. `init.cjs:951-955` is a leading-zero token normalizer (`String(parseInt(head)) + tail`) that already preserves decimal tails; leave it. Our tree has no separate smart-entry surface (`grep -n smart bin/lib/init.cjs` returns nothing); record that in the SUMMARY.
- Analyze regexes: `bin/lib/roadmap.cjs:207` phasePattern and `:324` checklistPattern; `sdk/src/query/roadmap.ts:713` and `:815`. Both `/gi`, no `^`, no `m`. Body slicing (`roadmap.cjs:228-232`, `roadmap.ts:725-729`) uses `content.slice(match.index)`, so any masking applied for the scan MUST preserve string indices.
- Line anchoring alone does not exclude a column-0 `## Phase N:` inside a fenced code block; the mandated fixture requires a fence mask. `sdk/src/query/helpers.ts:342-350` has an inline fence-state pass in a formatter but no reusable masker; add one.
- next_phase fallback: `bin/lib/phase.cjs:1319-1330` and `sdk/src/query/phase-lifecycle.ts:1243-1253`: walk document order and `break` on the first `comparePhaseNum(pm[1], phaseNum) > 0`.
- SDK handler signatures: `initProgress(args, projectDir)`, `roadmapAnalyze(args, projectDir)`, `phaseComplete(args, projectDir)` (`QueryHandler`), returning `{ data }`; existing tests call e.g. `await initProgress([], tmpDir)`. `phase-lifecycle.test.ts:96` has `setupTestProject(tmpDir, { roadmap, state, phases })`.
- CJS tests are zero-dep harnesses run as `node tests/<name>.test.cjs`, with `check(name, fn)` + `failures` counter + `process.exit(failures ? 1 : 0)`; CI enumerates them individually in `.github/workflows/check-drift.yml` (one `run: node tests/x.test.cjs` per test).
- `bin/build-bm.cjs` copies via `git ls-files`, so NEW files must be `git add`ed (at least `git add -N`) before `node bin/build-bm.cjs` or they will be missing from `dist/bm` and `--check` will not see them. `sdk/dist` is tracked (656 files) and included in `dist/bm`.
- Known baselines: CJS battery has two pre-existing failures (`context-monitor-hook-event`, `version-command`); SDK `test:integration` has ~14 known failures (golden read-only parity divergence). Treat as baseline, zero NEW allowed.
- Hygiene gate mechanics: the forbidden-token scan must match the dash characters by their UTF-8 bytes in octal (`$'\342\200\224'` for U+2014, `$'\342\200\223'` for U+2013, under `LC_ALL=C`) and match the forbidden word with the bracket regex `canon[i]cal`, so the gate command itself never contains a forbidden token.
- Upstream references for the executor (planning artifact only; never in code or commits): STATE anchoring PRs 4021, 4453, 4474, 4510; decimal ordering PR 4110; analyze anchoring PR 4578; lowest successor PR 3852, all on `open-gsd/gsd-core`. Upstream 3852 went roadmap-first; we keep the triage scope (fallback only) because roadmap-first would change the reported spelling (padded dir token vs roadmap number) on every aligned project.
</planner_findings>

<interfaces>
From bin/lib/core.cjs (exported at :2165):
  function comparePhaseNum(a, b): number   // decimal- and letter-aware; strips project_code prefix

From sdk/src/query/helpers.ts:231:
  export function comparePhaseNum(a: string, b: string): number

From bin/lib/state-document.generated.cjs (current module.exports, keep all 7; more may be added by the generator):
  stateExtractField(content, fieldName) -> string | null
  stateReplaceField(content, fieldName, newValue) -> string | null
  stateReplaceFieldWithFallback, normalizeStateStatus, computeProgressPercent, shouldPreserveExistingProgress, normalizeProgressNumbers

CLI surfaces used by fixtures:
  node bin/gsd-tools.cjs init progress          -> JSON with phases[].number, next_phase.number
  node bin/gsd-tools.cjs roadmap analyze        -> JSON with phase_count, phases[]
  node bin/gsd-tools.cjs phase complete <N>     -> JSON with next_phase, next_phase_name
  node bin/gsd-tools.cjs state get <Field>      -> JSON { [Field]: value }
  node sdk/dist/cli.js query init.progress | roadmap.analyze | phase.complete <N>  (SDK twin; JSON may be wrapped, read `.data ?? root`)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture baselines, anchor the STATE.md bold field grammar, create the generator, regenerate the CJS twin</name>
  <files>sdk/src/query/state-document.ts, sdk/scripts/gen-state-document.mjs, sdk/package.json, bin/lib/state-document.generated.cjs, bin/lib/state.cjs, sdk/src/query/state.ts, sdk/src/query/state-document.test.ts, sdk/src/query/state.test.ts, tests/state-field-line-anchor.test.cjs, tests/state-document-generated-fresh.test.cjs</files>
  <behavior>
    - Empty bold field followed by a data line: `stateReplaceField('**Status:**\n**Progress:** 3/10 total_phases\n', 'Status', 'Ready')` returns content that still contains `**Progress:** 3/10 total_phases` and has a line matching `^\*\*Status:\*\* Ready$`
    - Prose lookalike before the real row: with `'Note that **Status:** in prose is not a row.\n**Status:** real\n'`, `stateExtractField(..., 'Status')` is `'real'`; `stateReplaceField(..., 'Status', 'X')` leaves the prose line byte-unchanged and rewrites only the real row
    - Only a prose lookalike, no real row: `stateExtractField('Note that **Status:** in prose.\n', 'Status')` is `null`
    - Indented real row is still honored: `'  **Status:** indented\n'` extracts `'indented'` and replaces in place preserving the indentation
    - Plain field with an empty value followed by a line: `stateReplaceField('Status:\nnext: keep\n', 'Status', 'v')` keeps `next: keep`
    - Existing bold/plain replace tests keep passing byte-for-byte (`**Status:** old value` -> `**Status:** new value`)
    - `state get Status` (CJS gsd-tools and SDK handler) on a STATE.md with a prose lookalike above the real indented row returns the real value
    - Generator freshness: regenerating from the .ts into a temp path yields bytes identical to the committed `bin/lib/state-document.generated.cjs`
  </behavior>
  <action>
    Baselines FIRST, before any edit. Write to the scratchpad `/private/tmp/claude-503/-Users-jnuyens-src-gsd-plugin/b17e3628-eb00-4942-aa8c-5af54ba313a9/scratchpad/`: (a) `cjs-baseline.txt` from `for f in tests/*.test.cjs; do node "$f" >/dev/null 2>&1 || echo "FAIL $f"; done` (expect exactly context-monitor-hook-event and version-command); (b) `integration-baseline.txt` from `npm --prefix sdk run test:integration 2>&1 | tail -60` (expect ~14 failures; capture the failing test names from the vitest summary block). These files are the comparison targets for Task 3.

    Edit `sdk/src/query/state-document.ts` (single source):
    - `stateExtractField` bold pattern becomes `^[ \t]*\*\*${escaped}:\*\*[ \t]*(.+)` with flags `im` (line-anchored, same-line leading indentation tolerated, value must be non-empty as before). Keep the plain pattern as is.
    - `stateReplaceField` bold pattern becomes `^([ \t]*\*\*${escaped}:\*\*[ \t]*)(.*)` with flags `im`. Plain pattern becomes `^(${escaped}:[ \t]*)(.*)` with flags `im` (its `\s*` spans newlines exactly like the bold one). In the replacer, when the captured prefix does not end in a space or tab and `newValue` is non-empty, emit a single space between prefix and value so an empty `**Status:**` row becomes `**Status:** value`; otherwise keep the prefix byte-exact so the existing `old value -> new value` tests stay unchanged. Use `[ \t]*` everywhere; never `\s*` in these patterns. Add a two-line comment stating the invariant (patterns are line-anchored and confined to same-line horizontal whitespace so a match can never cross a newline), phrased as behavior, with no issue numbers.
    - In `KNOWN_TEMPLATE_DEFAULTS`, rewrite the entry that contains the literal U+2014 character so the character is expressed as the JavaScript unicode escape for U+2014 (backslash, u, 2014) inside the same single-quoted string. Add a short comment that the escape keeps the literal dash out of source while still matching legacy template text. The ASCII-hyphen variant on the next line stays as is.

    Create `sdk/scripts/gen-state-document.mjs`: read `sdk/src/query/state-document.ts`, run `ts.transpileModule` (import `typescript`) with `compilerOptions: { module: ts.ModuleKind.CommonJS, target: ts.ScriptTarget.ES2022, removeComments: false, newLine: ts.NewLineKind.LineFeed }`, prepend a header (`'use strict';` then a block comment: GENERATED FILE, DO NOT EDIT; Source: sdk/src/query/state-document.ts; Regenerate: cd sdk && npm run gen:state-document; one-sentence module description; ASCII hyphens only), and write the result to `bin/lib/state-document.generated.cjs` (resolve the path relative to the script, `../../bin/lib/...`). Support `--out <path>` to write elsewhere (used by the freshness test) and `--check` to exit 1 with a message when the committed file differs. Ensure the output ends with exactly one trailing newline. Register `"gen:state-document": "node scripts/gen-state-document.mjs"` in `sdk/package.json` scripts. Run `cd sdk && npm run gen:state-document`, then `node -e "const m=require('./bin/lib/state-document.generated.cjs'); for (const k of ['stateExtractField','stateReplaceField','stateReplaceFieldWithFallback','normalizeStateStatus','computeProgressPercent','shouldPreserveExistingProgress','normalizeProgressNumbers']) if (typeof m[k] !== 'function') throw new Error('missing '+k)"` to confirm the seven historical exports survive (the additional template-default exports are fine and expected). Confirm `grep -c 'Object.defineProperty(exports, "__esModule"' bin/lib/state-document.generated.cjs` is 1 and `require` of `bin/lib/state-document.cjs` (the thin re-export) still works.

    Anchor the `state get` bold reader in `bin/lib/state.cjs:101` and `sdk/src/query/state.ts:319`: pattern `^[ \t]*\*\*${fieldEscaped}:\*\*[ \t]*(.*)` with flags `im`. These two are a hand-synced twin pair; make the edits textually identical.

    Tests. Append to `sdk/src/query/state-document.test.ts` the behavior cases above (`describe('line anchoring')`). Append to `sdk/src/query/state.test.ts` one `state get` case with a prose lookalike above an indented real row (find the existing `stateGet`/`state get` describe block and mirror its fixture style). Create `tests/state-field-line-anchor.test.cjs` (zero-dep harness mirroring `tests/frontmatter-bom.test.cjs`): require `../bin/lib/state-document.cjs` and assert the behavior cases; plus one subprocess case writing a `.planning/STATE.md` fixture with `Note that **Status:** in prose.` above `  **Status:** Ready to execute` and asserting `node bin/gsd-tools.cjs state get Status` returns `Ready to execute`. Create `tests/state-document-generated-fresh.test.cjs`: run `node sdk/scripts/gen-state-document.mjs --out <tmpfile>` via execFileSync and `assert.strictEqual` the bytes to the committed file; second check: `--check` exits 0. Run `npm --prefix sdk run test -- state-document state.test` and both new CJS tests.
  </action>
  <verify>
    <automated>cd /Users/jnuyens/src/gsd-plugin && node -e "const {stateReplaceField,stateExtractField}=require('./bin/lib/state-document.generated.cjs');const c='## Position\n**Status:**\n**Progress:** 3/10 total_phases\nNote that **Status:** in prose is not a row.\n';if(stateExtractField(c,'Status')!==null)throw new Error('prose read as field');const r=stateReplaceField(c,'Status','Ready');if(!r.includes('**Progress:** 3/10 total_phases'))throw new Error('ate next line: '+JSON.stringify(r));if(!/^\*\*Status:\*\* Ready$/m.test(r))throw new Error('bad write: '+JSON.stringify(r));if(!r.includes('Note that **Status:** in prose is not a row.'))throw new Error('prose overwritten');const c2='Note that **Status:** in prose.\n  **Status:** real\n';if(stateExtractField(c2,'Status')!=='real')throw new Error('indented real row not read');if(stateReplaceField('Status:\nnext: keep\n','Status','v').indexOf('next: keep')<0)throw new Error('plain ate next line');console.log('cjs ok')" && node tests/state-field-line-anchor.test.cjs && node tests/state-document-generated-fresh.test.cjs && cd sdk && npx vitest run --project unit src/query/state-document.test.ts src/query/state.test.ts</automated>
  </verify>
  <done>Both baseline files exist in the scratchpad; `.ts` patterns are line-anchored with `[ \t]*` only; `sdk/scripts/gen-state-document.mjs` exists and `npm run gen:state-document` regenerates `bin/lib/state-document.generated.cjs` deterministically (freshness test passes); the seven historical CJS exports are intact; `state get` readers anchored in both twins; new CJS tests and appended vitest cases pass; the inline #32-class check prints `cjs ok`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decimal-aware init ordering, line-anchored analyze scans with fence masking, lowest-successor fallback (both twins + tests)</name>
  <files>bin/lib/init.cjs, sdk/src/query/init-complex.ts, bin/lib/core.cjs, sdk/src/query/helpers.ts, bin/lib/roadmap.cjs, sdk/src/query/roadmap.ts, bin/lib/phase.cjs, sdk/src/query/phase-lifecycle.ts, sdk/src/query/init-complex.test.ts, sdk/src/query/roadmap.test.ts, sdk/src/query/phase-lifecycle.test.ts, tests/decimal-phase-order-init.test.cjs, tests/roadmap-analyze-line-anchor.test.cjs, tests/phase-complete-lowest-successor.test.cjs</files>
  <behavior>
    - init progress on phases 7, 7.1, 7.2, 7.10, 8 (dirs `07-a`, `07.1-b`, `07.2-c`, `07.10-d`, `08-e`; roadmap checkboxes `[x]` for 7 and 7.1, `[ ]` for 7.2, 7.10, 8) returns `phases.map(p=>p.number)` equal to `['7','7.1','7.2','7.10','8']` and `next_phase.number` equal to `'7.2'` (the old parseInt sort placed 7.10 before 7.2 and reported 7.10)
    - roadmap analyze on a roadmap with real `### Phase 1:` and `### Phase 2:` plus a fenced code block containing a column-0 `### Phase 3: Fake`, a blockquote `> ### Phase 4: Quoted`, and prose `see ### Phase 5: inline mention` returns `phase_count` 2 and no phase numbered 3, 4, or 5; goal/depends_on extraction for phases 1 and 2 is unchanged
    - roadmap analyze checklist scan: a `- [ ] **Phase 6: Fake**` line inside a fenced block is not reported as a phase missing a detail section
    - phase complete 1 on a roadmap whose headings appear in order 1, 3, 2 (only dir `01-a` on disk with a PLAN+SUMMARY pair) reports `next_phase` `2` (old code returned `3`); a roadmap with 1, 3, 2.1, 2 reports `2`; 999.x is still skipped
  </behavior>
  <action>
    Item 2 (decimal ordering). `bin/lib/init.cjs`: add `comparePhaseNum` to the core.cjs destructure at line 8; replace the comparator body at :1414-1419 with `.sort((a, b) => comparePhaseNum(a, b))` (comparePhaseNum already handles non-numeric names via localeCompare); replace `:1490` with `phases.sort((a, b) => comparePhaseNum(String(a.number), String(b.number)))`. `sdk/src/query/init-complex.ts`: add `comparePhaseNum` to the `./helpers.js` import block (:29-36); replace `:361-366` and `:449` the same way. Leave the leading-zero normalizer at init.cjs:951 alone (already decimal-preserving). Do not touch agent names.

    Item 3 (analyze anchoring + fence mask). Add an exported helper `maskFencedBlocks(content)` to `bin/lib/core.cjs` (export at the module.exports block near :2165) and `sdk/src/query/helpers.ts`: walk lines, toggle fence state on lines whose trimmed start is three backticks or three tildes, and for the fence lines themselves and every line inside a fence replace every character except `\n` with a space. Output length and every `\n` offset are identical to the input (index-preserving), which is the whole point: callers keep slicing the ORIGINAL content at the match indices. Add a one-paragraph doc comment stating the index-preservation contract. Twin bodies must be textually identical apart from TS type annotations.
    In `bin/lib/roadmap.cjs` (cmdRoadmapAnalyze) right after `const content = extractCurrentMilestone(...)` at :203, add `const scanContent = maskFencedBlocks(content);` (import `maskFencedBlocks` from core.cjs in the existing destructure). Change `phasePattern` at :207 to `/^[ \t]{0,3}#{2,4}\s*Phase\s+([A-Za-z]?\d+[A-Z]?(?:\.\d+)*)\s*:\s*([^\n]+)/gim` and run `phasePattern.exec(scanContent)` (:220) while all `content.slice(...)` calls (:228-232 and the goal/mode/depends extraction) stay on `content`. Change `checklistPattern` at :324 to `/^[ \t]*-\s*\[[ x]\]\s*\*\*Phase\s+([A-Za-z]?\d+[A-Z]?(?:\.\d+)*)/gim` and exec it against `scanContent`. Mirror exactly in `sdk/src/query/roadmap.ts` (`roadmapAnalyze`: :709 scanContent, :713 phasePattern + :717 exec, :815 checklistPattern + :818 exec; import `maskFencedBlocks` from `./helpers.js`). Do NOT change `extractPhasesFromSection` (:460) or the `nextHeader` section-boundary lookups; they are out of scope for this port.

    Item 4 (lowest successor). `bin/lib/phase.cjs:1319-1330`: replace the `break`-on-first loop with a minimum selection: for each match (still skipping `999.x`), if `comparePhaseNum(pm[1], phaseNum) > 0 && (nextPhaseNum === null || comparePhaseNum(pm[1], nextPhaseNum) < 0)` then set `nextPhaseNum = pm[1]`, `nextPhaseName = <same slugify expression as today>`, `isLastPhase = false`; no `break`. Update the preceding comment to say the fallback selects the numerically lowest successor regardless of heading order (no issue numbers in the new comment text; the existing `#1591` reference line may stay as it is pre-existing). Mirror in `sdk/src/query/phase-lifecycle.ts:1243-1253`. Leave the sorted filesystem scan untouched.

    Tests. CJS: create `tests/decimal-phase-order-init.test.cjs` (mirror `tests/progress-and-frontier.test.cjs` Part B: mkproj, ROADMAP with a `- [x] Phase 7: A` / `- [x] Phase 7.1: B` / `- [ ] Phase 7.2: C` / `- [ ] Phase 7.10: D` / `- [ ] Phase 8: E` checklist plus `### Phase N:` detail sections, empty phase dirs `07-a 07.1-b 07.2-c 07.10-d 08-e`, run `init progress`, assert order and `next_phase.number === '7.2'`); create `tests/roadmap-analyze-line-anchor.test.cjs` (mirror `tests/letter-phase-ids.test.cjs` makeProject; roadmap per behavior list; run `roadmap analyze`; assert `phase_count === 2`, numbers `['1','2']`, and that the fenced `- [ ] **Phase 6: Fake**` does not appear in any missing-detail/malformed list field the output carries); create `tests/phase-complete-lowest-successor.test.cjs` (mirror `tests/phase-complete-backlog-sentinel.test.cjs` ROADMAP/STATE fixtures; headings in order 1, 3, 2 and a second fixture 1, 3, 2.1, 2; dir `01-a` with `01-01-PLAN.md` + `01-01-SUMMARY.md`; run `phase complete 1`; assert `next_phase` is `'2'` in both and that a `### Phase 999.1:` heading never wins). Every CJS harness: `#!/usr/bin/env node`, `'use strict'`, `check()` + `failures` + `process.exit(failures ? 1 : 0)`, cleanup in `finally`.
    SDK: append equivalent cases to `sdk/src/query/init-complex.test.ts` (`await initProgress([], tmpDir)`), `sdk/src/query/roadmap.test.ts` (`await roadmapAnalyze([], tmpDir)`), and `sdk/src/query/phase-lifecycle.test.ts` (`setupTestProject(tmpDir, { roadmap, state, phases: ['01-a'] })` plus writing the PLAN/SUMMARY pair, then `await phaseComplete(['1'], tmpDir)` and assert `result.data.next_phase === '2'`). Run `cd sdk && npx vitest run --project unit src/query/init-complex.test.ts src/query/roadmap.test.ts src/query/phase-lifecycle.test.ts`.
  </action>
  <verify>
    <automated>cd /Users/jnuyens/src/gsd-plugin && node tests/decimal-phase-order-init.test.cjs && node tests/roadmap-analyze-line-anchor.test.cjs && node tests/phase-complete-lowest-successor.test.cjs && node tests/progress-and-frontier.test.cjs && node tests/letter-phase-ids.test.cjs && node tests/phase-complete-backlog-sentinel.test.cjs && grep -c "comparePhaseNum" bin/lib/init.cjs sdk/src/query/init-complex.ts && grep -c "maskFencedBlocks" bin/lib/roadmap.cjs sdk/src/query/roadmap.ts && ! grep -n "parseInt(a.number" bin/lib/init.cjs sdk/src/query/init-complex.ts && cd sdk && npx vitest run --project unit src/query/init-complex.test.ts src/query/roadmap.test.ts src/query/phase-lifecycle.test.ts</automated>
  </verify>
  <done>Both twins use `comparePhaseNum` for both init sorts; analyze scans run against fence-masked, line-anchored content while body slicing stays on the original; the ROADMAP fallback selects the numerically lowest successor; three new CJS tests plus the three neighbouring existing CJS tests pass; appended vitest cases pass.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild SDK, prove the fixes through the built SDK CLI, regenerate dist/bm, run every hard gate against the baselines</name>
  <files>sdk/dist/, dist/bm/, .github/workflows/check-drift.yml</files>
  <action>
    1. `npm --prefix sdk run build` (tsc + esbuild bundle). Then prove item 1 through the BUILT SDK module, not source: `node --input-type=module -e "const {stateReplaceField,stateExtractField}=await import('/Users/jnuyens/src/gsd-plugin/sdk/dist/query/state-document.js'); const c='**Status:**\n**Progress:** 3/10 total_phases\nNote that **Status:** in prose.\n'; const r=stateReplaceField(c,'Status','Ready'); if(!r.includes('**Progress:** 3/10 total_phases')||!/^\*\*Status:\*\* Ready$/m.test(r)||stateExtractField(c,'Status')!==null) throw new Error(JSON.stringify(r)); console.log('sdk ok')"`. Then prove items 2 to 4 through `node sdk/dist/cli.js query init.progress`, `query roadmap.analyze`, and `query phase.complete 1` run with `cwd` set to fresh copies of the same fixtures the Task 2 CJS tests build (re-create the three temp trees inline); parse JSON, read `.data ?? root`, and assert the same three outcomes (order + next_phase 7.2; phase_count 2; next_phase 2). Record the exact commands and outputs for the SUMMARY.
    2. Add the five new CJS tests to `.github/workflows/check-drift.yml` in the existing one-line-per-test pattern (alphabetical placement next to their neighbours, same indentation).
    3. Stage new files so build-bm sees them: `git add -N sdk/scripts/gen-state-document.mjs tests/state-field-line-anchor.test.cjs tests/state-document-generated-fresh.test.cjs tests/decimal-phase-order-init.test.cjs tests/roadmap-analyze-line-anchor.test.cjs tests/phase-complete-lowest-successor.test.cjs`. Then `node bin/build-bm.cjs` and `node bin/build-bm.cjs --check` (must PASS). Also `node tests/bm-parity.test.cjs` and `node tests/build-bm-drift.test.cjs`.
    4. Hard gates, each compared to the Task 1 baselines in the scratchpad:
       - CJS battery: `for f in tests/*.test.cjs; do node "$f" >/dev/null 2>&1 || echo "FAIL $f"; done > <scratch>/cjs-after.txt; diff <scratch>/cjs-baseline.txt <scratch>/cjs-after.txt` must show no added lines (only the two known failures).
       - SDK unit: `npm --prefix sdk run test` must be fully green.
       - SDK integration: `npm --prefix sdk run test:integration 2>&1 | tail -60 > <scratch>/integration-after.txt`; the set of failing test names must be a subset of the baseline set (zero NEW). If a golden changes ONLY because a fix legitimately corrects output (e.g. a golden roadmap fixture with a fenced `## Phase` heading), update that golden and state why in the SUMMARY; otherwise do not touch goldens.
       - Hygiene over added lines (dash bytes in octal, forbidden word via bracket regex so the gate itself stays clean): `git diff -U0 -- bin/lib sdk/src tests sdk/scripts | grep '^+' | grep -v '^+++' | LC_ALL=C grep -nE $'#[0-9]{4}|\342\200\224|\342\200\223|canon[i]cal'` must print nothing (the `git add -N` above makes the new files visible to `git diff`). The generated CJS is included in this scan; the U+2014 escape from Task 1 is what keeps it clean.
       - Confirm no agent-name regressions: `grep -rn "gsd-planner\|gsd-executor\|gsd-verifier" tests/*.test.cjs sdk/src/query/*.test.ts | grep -v bm-` should be empty for the files this task touched.
    5. Confirm no version or CHANGELOG change: `git diff --stat -- CHANGELOG.md package.json .claude-plugin/plugin.json .claude-plugin/marketplace.json` is empty.
    6. Write the SUMMARY with: the four ports and their upstream PR numbers (this is the ONLY place PR numbers belong), the generator creation rationale (no generator existed; header was aspirational), the fence-mask design (index-preserving, scan-only), the roadmap-fallback-only scope decision for item 4, the U+2014 escape, the note that our tree has no smart-entry surface, before/after gate counts, and any golden updated with justification.
  </action>
  <verify>
    <automated>cd /Users/jnuyens/src/gsd-plugin && node --input-type=module -e "const {stateReplaceField,stateExtractField}=await import('/Users/jnuyens/src/gsd-plugin/sdk/dist/query/state-document.js'); const c='**Status:**\n**Progress:** 3/10 total_phases\nNote that **Status:** in prose.\n'; const r=stateReplaceField(c,'Status','Ready'); if(!r.includes('**Progress:** 3/10 total_phases')||!/^\*\*Status:\*\* Ready$/m.test(r)||stateExtractField(c,'Status')!==null) throw new Error(JSON.stringify(r)); console.log('sdk ok')" && node bin/build-bm.cjs --check && node tests/bm-parity.test.cjs && node tests/build-bm-drift.test.cjs && npm --prefix sdk run test && test -z "$(git diff -U0 -- bin/lib sdk/src tests sdk/scripts | grep '^+' | grep -v '^+++' | LC_ALL=C grep -E $'#[0-9]{4}|\342\200\224|\342\200\223|canon[i]cal')" && test -z "$(git diff --stat -- CHANGELOG.md package.json .claude-plugin/plugin.json .claude-plugin/marketplace.json)" && grep -c "state-field-line-anchor\|decimal-phase-order-init\|roadmap-analyze-line-anchor\|phase-complete-lowest-successor\|state-document-generated-fresh" .github/workflows/check-drift.yml</automated>
  </verify>
  <done>Built SDK proves all four fixes via `sdk/dist`; `dist/bm` regenerated and `--check` passes; CJS battery diff vs baseline shows no new failures; SDK unit green; integration failing set is a strict subset of baseline; hygiene scan over added lines is empty; no version/CHANGELOG diff; CI workflow lists the five new tests; SUMMARY written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .planning markdown -> CLI parsers | ROADMAP.md / STATE.md are user-authored (and agent-authored) text parsed by regex; malformed or adversarial-looking content (prose lookalikes, fenced examples) crosses here |
| Generator script -> bin/lib | Build-time transform of a TS source into a committed runtime module |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-mb0-01 | Tampering | stateReplaceField / stateExtractField | mitigate | Line-anchored `^[ \t]*` patterns with same-line whitespace only; regression tests prove a lookalike cannot redirect a read or a write and an empty field cannot clobber its neighbour |
| T-mb0-02 | Tampering | roadmap analyze phase/checklist scans | mitigate | Fence mask + line anchor so fenced or quoted examples cannot mint phantom phases; index-preserving mask keeps body extraction on original text |
| T-mb0-03 | Denial of Service | new multiline regexes on large ROADMAP/STATE | accept | Patterns are linear-time (no nested quantifiers added); inputs are small project files |
| T-mb0-04 | Tampering | gen-state-document.mjs output drift | mitigate | Freshness test byte-compares committed CJS to a fresh regeneration; `--check` mode available for CI |
| T-mb0-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed; generator uses the already-present `typescript` devDependency |
</threat_model>

<verification>
- Task 1 inline check prints `cjs ok`; Task 3 inline check prints `sdk ok` (built SDK).
- Five new CJS tests pass individually; `tests/progress-and-frontier`, `tests/letter-phase-ids`, `tests/phase-complete-backlog-sentinel` still pass (neighbouring behaviour preserved).
- `cd sdk && npm run gen:state-document && git diff --exit-code -- ../bin/lib/state-document.generated.cjs` is clean after commit (generator is idempotent).
- CJS battery diff vs baseline: no new `FAIL` lines. SDK unit fully green. Integration failing set is a subset of baseline.
- `node bin/build-bm.cjs --check` PASS; bm parity and drift tests pass.
- Hygiene scan over added lines in bin/lib, sdk/src, sdk/scripts, tests returns nothing.
</verification>

<success_criteria>
- All four ports land in both twins with tests, and the STATE.md module is regenerated (not hand-edited) from its single source by a committed generator.
- Every mandated fixture from the task constraints is executed by a real command against the actual CJS and the built SDK and passes.
- Zero new failures across the CJS battery, SDK unit, and SDK integration suites; dist/bm regenerated and drift check green; hygiene clean; no version bump, no CHANGELOG entry, no Co-Authored-By.
</success_criteria>

<output>
Create `/Users/jnuyens/src/gsd-plugin/.planning/quick/260920-mb0-port-4-upstream-gsd-core-v1-12-v1-14-fix/260920-mb0-SUMMARY.md` when done (upstream PR numbers belong there and nowhere in product code or commit messages).
</output>
