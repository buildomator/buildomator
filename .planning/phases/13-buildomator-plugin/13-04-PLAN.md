---
phase: 13-buildomator-plugin
plan: 04
type: execute
wave: 2
depends_on: [13-03]
gap_closure: true
autonomous: true
requirements: [BM-01, BM-02, BM-03]
files_modified:
  - tests/bm-parity.test.cjs
  - tests/build-bm-drift.test.cjs
  - bin/build-bm.cjs
  - .github/workflows/install-smoke.yml
  - dist/bm

must_haves:
  truths:
    - "A planted synthetic violation (cache/gsd-plugin/gsd, /gsd:x, gsd:gsd-x, /gsd[:-]x) is FLAGGED by the census detector - a durable fail-closed proof in CI (D-06/D-08)"
    - "A synthetic input containing only allow-listed tokens yields zero flags (positive control's clean case)"
    - "A real scan of dist/bm (skipping STAMP_EXCLUDE) finds zero non-allow-listed gsd tokens (SC2, D-08)"
    - "The drift tamper case never mutates the committed dist/bm tree; it tampers a temp copy via BM_DIST_DIR (WR-01)"
    - "No GSD plan-id metadata remains in install-smoke.yml (CLAUDE.md hygiene, WR-04)"
  artifacts:
    - path: "tests/bm-parity.test.cjs"
      provides: "Fail-closed census: positive-control unit + real dist/bm scan with D-08 allow-list"
      min_lines: 90
    - path: "bin/build-bm.cjs"
      provides: "BM_DIST_DIR env override on check() so integration tampering targets an isolated copy"
      contains: "BM_DIST_DIR"
  key_links:
    - from: "tests/build-bm-drift.test.cjs"
      to: "bin/build-bm.cjs check() via BM_DIST_DIR"
      via: "cp -R dist/bm to temp, tamper temp, run --check with BM_DIST_DIR set"
      pattern: "BM_DIST_DIR"
    - from: ".github/workflows/check-drift.yml"
      to: "tests/bm-parity.test.cjs"
      via: "existing bm-build-drift run step (no new CI wiring)"
      pattern: "bm-parity"
---

<objective>
Turn the ad-hoc zero-leak grep into a fail-closed census that CANNOT silently pass a new
class of gsd leak, fix the shared-mutable-state test race (WR-01), and remove the plan-id
hygiene leak in install-smoke.yml (WR-04).

Purpose: the reviewer's core structural criticism (CR-01/CR-02) is that both existing
gates only looked for the colon-form /gsd: token, so the ~19 un-stamped hook carriers and
the sanitizer slipped through. The root fix is a detector that is proven to FLAG planted
violations (positive control) and then run against the real dist/bm - so any future leak
of cache/gsd-plugin/gsd, gsd:gsd-<agent>, /gsd:x, or /gsd[:-]x fails CI instead of
shipping. bm-build-drift already runs tests/bm-parity.test.cjs, so no new CI wiring.

Output: a hardened parity census, an isolated tamper test, a cleaned CI comment, and a
regenerated dist/bm.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-buildomator-plugin/13-CONTEXT.md
@.planning/phases/13-buildomator-plugin/13-REVIEW.md
@.planning/phases/13-buildomator-plugin/13-03-PLAN.md

<interfaces>
Existing test scaffolding to extend:
- tests/bm-parity.test.cjs: zero-dep check(name, fn) harness, includedSourceFiles() via
  git ls-files + shouldExclude, an execFileSync('grep', ['-rIl', '/gsd:', OUT]) zero-leak
  case, and a spawnSync --check case. Add the census here.
- bin/build-bm.cjs check(root): today hardcodes committed = path.join(root, 'dist', 'bm').
  Add a BM_DIST_DIR override (default dist/bm) so an integration test can point --check at
  an isolated copy.
- tests/build-bm-drift.test.cjs "--check exits 1 ... after tampering" case
  (lines ~285-296): currently fs.appendFileSync on the committed dist/bm/.claude-plugin/
  plugin.json then rebuilds in place. Rewrite to isolate.
- STAMP_EXCLUDE is exported from bin/build-bm.cjs (added in 13-03); import it here so the
  census skips exactly the same carriers.
</interfaces>

D-08 census allow-list (tokens that legitimately contain gsd and must NOT be flagged):
- gsd-plugin (marketplace id)
- gsd-<alnum> filenames (gsd-tools.cjs, gsd-sdk, gsd-session-state.sh, gsd-validate-commit.sh, etc.)
- gsd:// and the regex-escaped gsd:\/ URI form
- gsd_<alnum> (MCP tool names)
- camelCase gsd[A-Z] identifiers
- open-gsd / opengsd
- .gsd (dotfile/dir) and the gsd/{milestone} branch template
- MCP internal name:'gsd', the plugin.json gsd keyword, and branding GSD / Get Shit Done / [gsd]

Violation classes the detector MUST flag (post-transform, these should never appear in dist/bm):
- cache/gsd-plugin/gsd (un-stamped hook fallback)
- /gsd:<x> (un-rewritten slash command)
- gsd:gsd-<x> (un-rewritten agent ref)
- /gsd[:-]<x> (un-rewritten sanitizer literal)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the fail-closed census (positive control + real scan) to bm-parity.test.cjs</name>
  <files>tests/bm-parity.test.cjs</files>
  <behavior>
    Factor a pure detector function (in-test) that, given a text string and its rel path,
    returns the list of violating tokens after removing allow-listed tokens. Prove it two ways:
    - Positive control (durable RED proof): feed the detector synthetic strings containing
      each violation class - cache/gsd-plugin/gsd, /gsd:plan-phase, gsd:gsd-executor,
      and the /gsd[:-] literal - and assert EACH is flagged (non-empty result). Feed a
      synthetic string containing ONLY allow-listed tokens (gsd-plugin, gsd-tools.cjs,
      gsd://state, gsd_get_state, gsdParser, open-gsd, .gsd, GSD, Get Shit Done) and assert
      ZERO flags. This unit does not read dist/bm, so it stays green independent of the tree
      and durably documents what "flagged" means.
    - Real scan: enumerate dist/bm files (walk OUT, or git ls-files mapped into OUT), skip
      any rel path in STAMP_EXCLUDE and any non-text/binary file, run the detector on each,
      and assert the aggregate violation list is empty.
    Confirm fail-closed behavior before wiring: temporarily plant one synthetic violation
    into a copied fixture (NOT the committed tree) and confirm the positive-control asserts
    it is flagged; the real-scan case must be green against the clean 13-03 dist/bm.
  </behavior>
  <action>
    Extend tests/bm-parity.test.cjs (keep the existing 4 cases):
    - Import STAMP_EXCLUDE from ../bin/build-bm.cjs.
    - Implement the detector: strip allow-listed tokens first (the D-08 allow-list above),
      then match the residual for the four violation classes. Prefer explicit regex/string
      matches per class over one mega-regex so failures name the class. Do NOT special-case
      by widening the allow-list to make a real leak pass - if the real scan flags a token
      that is genuinely legitimate but not yet allow-listed, STOP and surface it rather than
      silently expanding the list (fail-closed governance).
    - Add the positive-control check() cases (each violation class flagged; allow-listed-only
      clean) and the real-scan check() case (dist/bm minus STAMP_EXCLUDE has zero violations).
    - The existing grep-based '/gsd:' zero-leak case may remain or be folded into the census;
      if folded, keep an equivalent assertion.
    - No em-dashes. No GSD phase/plan ids in code or comments.
  </action>
  <verify>
    <automated>node tests/bm-parity.test.cjs</automated>
  </verify>
  <done>bm-parity.test.cjs passes: the detector flags every planted violation class, passes the allow-listed-only case, and the real dist/bm scan (skipping STAMP_EXCLUDE) reports zero violations.</done>
</task>

<task type="auto">
  <name>Task 2: Isolate the drift tamper test via BM_DIST_DIR (WR-01), fix install-smoke hygiene (WR-04), regenerate</name>
  <files>bin/build-bm.cjs, tests/build-bm-drift.test.cjs, .github/workflows/install-smoke.yml, dist/bm</files>
  <action>
    WR-01 in bin/build-bm.cjs:
    - In check(root), read committed dir from process.env.BM_DIST_DIR when set, else
      path.join(root, 'dist', 'bm'). Keep generate() writing to a fresh temp (unchanged);
      only the diff target becomes overridable. Update the check() doc comment.
    WR-01 in tests/build-bm-drift.test.cjs:
    - Rewrite the "--check exits 1 and names the path after tampering" case so it NEVER
      mutates the committed dist/bm: cp -R dist/bm to an mkdtemp dir, append a byte to the
      copy's .claude-plugin/plugin.json, run node bin/build-bm.cjs --check with
      BM_DIST_DIR=<temp>, assert exit 1 and that the output names plugin.json, then rm the
      temp in a finally. Do not run a rebuild against the shared tree. This removes the
      shared-mutable-state race with bm-parity.test.cjs.
    WR-04 in .github/workflows/install-smoke.yml (line ~113):
    - Reword the bm-package-smoke comment to drop the plan id, e.g. "Proves the committed
      dist/bm package resolves its own SDK and hooks entirely via ${CLAUDE_PLUGIN_ROOT},
      while the repo/cache identity stays gsd-plugin (D-10/D-11/D-12)." No .planning ids.
    Regenerate: run node bin/build-bm.cjs so dist/bm reflects the reworded install-smoke.yml
    (install-smoke.yml is in STAMP_EXCLUDE, so it is copied without the hook stamp; the
    command-ref rewrite still applies to its text as before).
    - No em-dashes. No GSD phase/plan ids in code, comments, or CI files.
  </action>
  <verify>
    <automated>node bin/build-bm.cjs && node tests/build-bm-drift.test.cjs && node tests/bm-parity.test.cjs && node bin/build-bm.cjs --check && echo GATES_PASS</automated>
  </verify>
  <done>check() honors BM_DIST_DIR; the tamper case runs against an isolated temp copy and never touches committed dist/bm; install-smoke.yml carries no plan id; --check and both test suites are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate -> release tag | The census/drift gates are the release gate; a false-negative ships a broken bm package |
| test process -> shared dist/bm tree | A mutating test can corrupt the tree another test reads (race) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Repudiation | census detector | mitigate | Positive control feeds each violation class and asserts it is flagged, so a silently broken detector fails CI (fail-closed) |
| T-13-04-02 | Tampering | committed dist/bm during tests | mitigate | BM_DIST_DIR isolates the tamper case to a temp copy; the shared tree is never mutated |
| T-13-04-03 | Information disclosure | install-smoke.yml shipped in dist/bm | mitigate | Remove the plan-id comment per CLAUDE.md hygiene; regenerate |
| T-13-04-SC | Tampering | npm/pip/cargo installs | accept | No package-manager operations in this plan; nothing installed |
</threat_model>

<verification>
- node tests/bm-parity.test.cjs passes: positive control flags all four violation classes; allow-listed-only case clean; real dist/bm scan (minus STAMP_EXCLUDE) zero violations.
- node tests/build-bm-drift.test.cjs passes; tamper case uses BM_DIST_DIR and leaves committed dist/bm untouched (git status shows no unexpected dist/bm churn after the run).
- node bin/build-bm.cjs --check exits 0.
- grep -n 'from plan\|12-01\|13-0' .github/workflows/install-smoke.yml returns no plan-id metadata.
</verification>

<success_criteria>
- The census is fail-closed: proven to flag cache/gsd-plugin/gsd, /gsd:x, gsd:gsd-x, and /gsd[:-]x, and to pass an allow-listed-only input.
- Real dist/bm scan reports zero non-allow-listed gsd tokens.
- The drift tamper test is race-free (isolated temp copy via BM_DIST_DIR).
- install-smoke.yml carries no GSD plan id in source or in its dist/bm copy.
</success_criteria>

<output>
Create `.planning/phases/13-buildomator-plugin/13-04-SUMMARY.md` when done.
</output>
