---
phase: 13-buildomator-plugin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/bm-transform.cjs
  - tests/bm-transform.test.cjs
  - bin/build-bm.cjs
  - tests/build-bm-drift.test.cjs
  - dist/bm/**
autonomous: true
requirements: [BM-02, BM-03]

must_haves:
  truths:
    - "A fresh `node bin/build-bm.cjs` leaves ZERO leading-slash `/gsd:` command references anywhere in dist/bm (all 1,512 source occurrences become `/bm:`)"
    - "dist/bm/hooks/hooks.json falls back to cache/gsd-plugin/bm at all 17 sites while gsd-tools.cjs, run-bash-hook.cjs, gsd-*.sh/.js filenames, and the GSD: stderr prefix survive byte-for-byte"
    - "dist/bm/.claude-plugin/plugin.json registers its MCP server under the key bm; the gsd key is absent; the server config object is otherwise identical"
    - "dist/bm/mcp/server.cjs is byte-identical to mcp/server.cjs (gsd_* tools and gsd:// URIs untouched)"
    - "node bin/build-bm.cjs --check exits 0 against the committed dist/bm, and node tests/build-bm-drift.test.cjs passes with the widened assertions"
  artifacts:
    - path: "bin/lib/bm-transform.cjs"
      provides: "Pure exported transform helpers rewriteCommandRefs and stampHookFallback"
      exports: ["rewriteCommandRefs", "stampHookFallback"]
    - path: "tests/bm-transform.test.cjs"
      provides: "Unit tests proving the rewrite covers capture/local-patches/edit-phase/extract-learnings and spares gsd://, gsd-tools.cjs, gsd-sdk, cache/gsd-plugin/gsd"
    - path: "bin/build-bm.cjs"
      provides: "generate() applies the transform on the copy path; stampBmManifest rekeys mcpServers gsd to bm"
      contains: "rewriteCommandRefs"
    - path: "tests/build-bm-drift.test.cjs"
      provides: "Whitelist walk compares text files against the transform of source, not raw source; mcpServers assertions expect the bm key; hooks.json case expects the stamped fallback"
  key_links:
    - from: "bin/build-bm.cjs"
      to: "bin/lib/bm-transform.cjs"
      via: "require('./lib/bm-transform.cjs') inside generate()"
      pattern: "require\\(.*bm-transform"
    - from: "tests/build-bm-drift.test.cjs"
      to: "bin/lib/bm-transform.cjs"
      via: "requires the same helpers to compute the expected transformed bytes"
      pattern: "rewriteCommandRefs"
    - from: "bin/build-bm.cjs check()"
      to: "bin/build-bm.cjs generate()"
      via: "check() regenerates through the identical generate() path, so the transform flows through --check automatically"
      pattern: "generate\\("
---

<objective>
Widen the deterministic bm transform T from "identity stamp only" (Phase 12) to
T = stampBmManifest (now incl. mcpServers rekey) + rewriteCommandRefs + stampHookFallback,
applied inside generate() so build and --check share one code path. Regenerate and commit
dist/bm through the wider transform.

Purpose: makes the generated bm package self-consistent (a bm user's `/gsd:plan-phase`
breadcrumbs become `/bm:plan-phase` instead of invoking the sibling gsd plugin), closes the
known Phase 12 limitation (hooks fallback pointing at the gsd cache dir), and gives the bm
MCP server its own manifest key. Implements D-01, D-02 (refined per RESEARCH F-1), D-03,
D-04, D-05.

Decision refinement to flag for the checker: D-02 said "reuse the skill-name alternation
regex". RESEARCH F-1 measured that the alternation UNDER-rewrites (misses /gsd:capture x35,
/gsd:local-patches x22, /gsd:edit-phase x8, /gsd:extract-learnings whose skill dir is
extract_learnings). The plan uses the anchored leading-slash substitution instead: simpler
AND complete, and it naturally spares gsd:// (38 occurrences, never leading-slash), all
gsd-* filenames, and cache-path literals. This honors D-01's stated goal (bm
self-consistency) and was endorsed as RESEARCH Open Question 1's recommendation.

Output: bin/lib/bm-transform.cjs + unit tests, extended bin/build-bm.cjs, widened
tests/build-bm-drift.test.cjs, regenerated dist/bm.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/13-buildomator-plugin/13-CONTEXT.md
@.planning/phases/13-buildomator-plugin/13-RESEARCH.md
@.planning/phases/13-buildomator-plugin/13-PATTERNS.md
@bin/build-bm.cjs
@tests/build-bm-drift.test.cjs
</context>

<interfaces>
<!-- Existing contracts the executor builds against. Extracted 2026-07-06. Use directly. -->

From bin/build-bm.cjs (current exports, line 222):
  module.exports = { stampBmManifest, shouldExclude };
  function stampBmManifest(srcManifest)  // returns stamped copy; currently name/displayName/description only
  function shouldExclude(relPath)        // pure filter; EXCLUDE_ROOTS = .git .planning .claude node_modules dist scratchpad
  function includedFiles(root)           // git ls-files -z filtered through shouldExclude
  function generate(root, outDir)        // copy loop currently fs.copyFileSync for EVERY file, then writes stamped plugin.json
  function check(root)                   // regenerates via generate() into a temp dir and byte-compares vs committed dist/bm

From bin/maintenance/rewrite-command-namespace.cjs line 67 (reuse this filter verbatim):
  const textExt = /\.(md|json|cjs|js|ts|tsx|txt|yml|yaml|sh|html)$/i;

Measured facts (RESEARCH F-1..F-3, re-verified 2026-07-06):
  - 1,512 `/gsd:` occurrences across 219 shipped files
  - hooks/hooks.json: exactly 17 `cache/gsd-plugin/gsd` literals; identity tokens on the same lines
    that MUST survive: gsd-tools.cjs, run-bash-hook.cjs, gsd-*.js hook filenames,
    gsd-session-state.sh / gsd-validate-commit.sh / gsd-phase-boundary.sh args, `GSD:` stderr (17x)
  - mcp/server.cjs: 0 `/gsd:` occurrences (so rewriteCommandRefs is a no-op on it; byte-identity holds)
  - .claude-plugin/plugin.json: exactly one mcpServers entry, key `gsd`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create bin/lib/bm-transform.cjs with tests-first unit coverage</name>
  <files>bin/lib/bm-transform.cjs, tests/bm-transform.test.cjs, dist/bm/**</files>
  <read_first>
    - bin/build-bm.cjs (pure-helper + module.exports shape to copy; 'use strict' header convention)
    - tests/build-bm-drift.test.cjs (zero-dep harness shape: node:assert, check(name, fn) runner, failure counter, process.exit(1) footer)
    - .planning/phases/13-buildomator-plugin/13-PATTERNS.md (verified helper excerpts: the anchored rewriteCommandRefs regex and the split/join stampHookFallback literal, plus a representative hooks.json resolver line for the fixture)
    - bin/maintenance/rewrite-command-namespace.cjs (the OLD alternation approach; do NOT reuse its token source, only note its textExt filter for Task 2)
  </read_first>
  <behavior>
    rewriteCommandRefs(text) -> text:
    - Test: rewrites `/gsd:plan-phase`, `/gsd:capture`, `/gsd:local-patches`, `/gsd:edit-phase`, `/gsd:extract-learnings` to the `/bm:` form (the F-1 under-coverage cases MUST each have an explicit assertion)
    - Test: rewrites at string start, after whitespace, after a backtick, and after a paren
    - Test: SPARES `gsd://config` and `gsd://state` (no leading slash, F-3), `gsd-tools.cjs`, `gsd-sdk`, `gsd-session-state.sh`, `cache/gsd-plugin/gsd`, and a bare `gsd:` with an identifier char before it (e.g. `abcgsd:`)
    - Test: idempotent on already-rewritten text (running twice equals running once)
    stampHookFallback(hooksJsonText) -> text:
    - Test: on a fixture copied from one real hooks.json resolver line, exactly the literal `cache/gsd-plugin/gsd` becomes `cache/gsd-plugin/bm`; `gsd-tools.cjs`, `run-bash-hook.cjs`, the `GSD:` stderr prefix, and `.sh`/`.js` filenames/args on the same line survive unchanged
    - Test: a fixture with N occurrences of the literal has all N replaced and zero remaining
  </behavior>
  <action>
    RED first: write tests/bm-transform.test.cjs on the drift-test harness shape (node:assert
    + check runner + exit-1 footer, run as `node tests/bm-transform.test.cjs`), requiring
    { rewriteCommandRefs, stampHookFallback } from ../bin/lib/bm-transform.cjs. Run it and
    confirm it fails (module absent). GREEN: create bin/lib/bm-transform.cjs: 'use strict',
    CJS module.exports, camelCase identifiers, zero dependencies (no fs/child_process needed
    for pure string helpers). rewriteCommandRefs uses the leading-boundary anchored
    substitution from 13-PATTERNS.md (preceding char is start-of-string or a
    non-alphanumeric; the slash is part of the matched token; global flag; capture-and-keep
    the boundary char). This implements D-01 and refines D-02 per RESEARCH F-1 (anchored
    substitution instead of the skill-name alternation). stampHookFallback replaces exactly
    the literal `cache/gsd-plugin/gsd` with `cache/gsd-plugin/bm` via split/join (D-04);
    never a regex over bare `gsd`. JSDoc on each helper describes BEHAVIOR only; do not put
    phase/plan/decision IDs in code comments (generated-code hygiene). Finally run
    `node bin/build-bm.cjs` to regenerate dist/bm so the two new tracked files land in the
    committed package (the transform is not wired yet, so they copy verbatim this task) and
    commit dist/bm together with the new files to keep --check green.
  </action>
  <verify>
    <automated>node tests/bm-transform.test.cjs && node bin/build-bm.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/bm-transform.test.cjs` exits 0 with every behavior case above asserted
    - Test file contains explicit assertions for the four F-1 under-coverage commands: grep -c "capture\|local-patches\|edit-phase\|extract-learnings" tests/bm-transform.test.cjs >= 4
    - bin/lib/bm-transform.cjs exports exactly { rewriteCommandRefs, stampHookFallback }; `node -e "const t=require('./bin/lib/bm-transform.cjs'); console.log(typeof t.rewriteCommandRefs, typeof t.stampHookFallback)"` prints "function function"
    - `node bin/build-bm.cjs --check` exits 0 (regenerated dist/bm committed with the new files)
    - No new package installed: git diff shows no package.json/package-lock.json dependency change
  </acceptance_criteria>
  <done>Both pure helpers exist with passing tests-first unit coverage proving full command-ref coverage and identity-string sparing; dist/bm regenerated and --check green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the transform into generate(), rekey mcpServers, widen the drift test, regenerate dist/bm</name>
  <files>bin/build-bm.cjs, tests/build-bm-drift.test.cjs, dist/bm/**</files>
  <read_first>
    - bin/build-bm.cjs (generate() copy loop; stampBmManifest and its now-stale header comment; check(); module.exports line 222)
    - tests/build-bm-drift.test.cjs (SRC_MANIFEST fixture ~lines 50-61; the two mcpServers cases ~lines 79-88; hooks.json byte-equal case ~lines 156-162; whitelist walk ~lines 164-186)
    - .planning/phases/13-buildomator-plugin/13-PATTERNS.md (the "Extending generate()" and mcpServers-rekey excerpts; the drift-test widening notes)
  </read_first>
  <action>
    Three coordinated edits that MUST land in ONE commit (the widened drift walk compares the
    committed dist/bm, so build change + test widening + regeneration are atomic; splitting
    them leaves an intermediate commit with a red gate).

    (1) bin/build-bm.cjs: require { rewriteCommandRefs, stampHookFallback } from
    ./lib/bm-transform.cjs. Add a TEXT_EXT constant reusing the extension set from
    rewrite-command-namespace.cjs line 67 verbatim: md|json|cjs|js|ts|tsx|txt|yml|yaml|sh|html
    (case-insensitive). Inside the generate() copy loop, branch: files matching TEXT_EXT are
    read as utf8, passed through rewriteCommandRefs, additionally through stampHookFallback
    when rel === 'hooks/hooks.json', then written; all other files stay on fs.copyFileSync.
    CRITICAL (RESEARCH Pitfall 4): the transform lives INSIDE generate() so check()'s
    temp-dir regeneration runs identical logic; adding it on a build-only path makes --check
    report every file as differing. Extend stampBmManifest: after the existing
    name/displayName/description stamp, rekey the single mcpServers entry gsd to bm
    (guard: only when mcpServers.gsd exists and mcpServers.bm does not; the server config
    object is carried through unchanged). Update the stampBmManifest header comment, which
    currently claims the mcpServers key stays gsd: describe the new rekey behavior in plain
    terms, no decision/phase IDs in code comments. Re-export the transform helpers from
    module.exports (add rewriteCommandRefs, stampHookFallback alongside stampBmManifest,
    shouldExclude) so tests require them from one place.

    (2) tests/build-bm-drift.test.cjs: widen four spots. (a) The two mcpServers assertions
    (~79-88): rename and flip them to assert out.mcpServers.bm deep-equals the source gsd
    server config and out.mcpServers.gsd is absent. (b) The hooks.json byte-equal case
    (~156-162): compare dist/bm/hooks/hooks.json against
    stampHookFallback(rewriteCommandRefs(sourceText)) instead of raw source. (c) The
    whitelist walk (~164-186): for files matching the same TEXT_EXT filter, compare the
    dist copy against rewriteCommandRefs(sourceText) (plus stampHookFallback for
    hooks/hooks.json); binary files stay byte-equal to raw source. (d) The plugin.json
    branch of the walk: normalize mcpServers alongside the three stamped keys (delete
    mcpServers from both sides after asserting the bm rekey explicitly, or assert
    b.mcpServers.bm deep-equals a.mcpServers.gsd). Import the helpers via
    require('../bin/build-bm.cjs') re-exports. These flips are the EXPECTED D-03 widening,
    not a regression.

    (3) Run `node bin/build-bm.cjs` to regenerate dist/bm through the wider transform and
    stage the full dist/bm diff (roughly 219 text files gain /bm: refs; hooks.json gains 17
    stamped fallbacks; plugin.json gains the bm server key). Note: shipped copies of the
    build tooling and tests inside dist/bm get their own /gsd: literals rewritten too; that
    is expected and deterministic (RESEARCH A2), those copies are never executed to build.
  </action>
  <verify>
    <automated>node tests/bm-transform.test.cjs && node tests/build-bm-drift.test.cjs && node bin/build-bm.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - `node bin/build-bm.cjs --check` exits 0 against the committed dist/bm
    - `node tests/build-bm-drift.test.cjs` exits 0 with the widened assertions
    - Zero command-ref leaks: `grep -rIl '/gsd:' dist/bm` produces no output (grep exits 1)
    - Hook stamp exact: `grep -o 'cache/gsd-plugin/bm' dist/bm/hooks/hooks.json | wc -l` prints 17 and `grep -o 'cache/gsd-plugin/gsd' dist/bm/hooks/hooks.json | wc -l` prints 0
    - Identity tokens survive: occurrence counts of `gsd-tools.cjs`, `run-bash-hook.cjs`, and `GSD:` are identical between hooks/hooks.json and dist/bm/hooks/hooks.json (compare `grep -o <token> <file> | wc -l` pairs)
    - MCP server untouched: `cmp -s mcp/server.cjs dist/bm/mcp/server.cjs` exits 0
    - Manifest rekeyed: `node -e "const m=require('./dist/bm/.claude-plugin/plugin.json'); if(!m.mcpServers.bm||m.mcpServers.gsd)process.exit(1)"` exits 0
    - Manifest still schema-valid: `node bin/validate-plugin.cjs dist/bm/.claude-plugin/plugin.json` exits 0
  </acceptance_criteria>
  <done>dist/bm is a deterministic transform of source: all /gsd: refs read /bm:, hooks fall back to the bm cache dir, the MCP server registers under the bm key with byte-identical server code, and both the drift test and --check gate the result.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree -> published dist/bm | the committed, published package is generated bytes; any transform bug ships to installs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | rewriteCommandRefs / stampHookFallback | mitigate | Anchored substitution + exact-literal stamp only (never blanket gsd->bm); unit tests assert gsd://, gsd-tools.cjs, gsd-sdk, cache paths, and GSD: stderr survive; cmp proves mcp/server.cjs byte-identical |
| T-13-02 | Tampering | build vs --check divergence | mitigate | Transform lives inside generate(); check() regenerates through the same generate(), so the gate cannot silently pass drifted output (RESEARCH Pitfall 4) |
| T-13-03 | Information disclosure | untracked/secret file entering dist/bm | mitigate | Carried Phase 12 control: git ls-files tracked-only enumeration + shouldExclude; this plan does not alter includedFiles() |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed this phase; the install-smoke "no node_modules touched" invariant enforces it; legitimacy gate moot |
</threat_model>

<verification>
Full local gate after the plan:
node tests/bm-transform.test.cjs && node tests/build-bm-drift.test.cjs && node bin/build-bm.cjs --check
plus the grep/cmp census assertions listed in Task 2 acceptance criteria.
</verification>

<success_criteria>
- T = stampBmManifest(+mcpServers rekey) + rewriteCommandRefs + stampHookFallback is single-sourced and applied inside generate()
- Committed dist/bm equals T(source) byte-for-byte, proven by --check and the widened drift test
- Zero `/gsd:` leaks in dist/bm; 17/17 hook fallbacks stamped; server.cjs byte-identical; manifest key bm
</success_criteria>

<output>
Create `.planning/phases/13-buildomator-plugin/13-01-SUMMARY.md` when done.
</output>
