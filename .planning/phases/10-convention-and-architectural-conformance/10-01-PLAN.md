---
phase: 10-convention-and-architectural-conformance
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/conventions.test.cjs
  - bin/lib/conventions.cjs
autonomous: true
requirements: [CONV-01, CONV-02, CONV-03, CONV-04]
nyquist_compliant: true

must_haves:
  truths:
    - "deriveConventions derives all four axes (file-name casing, identifier casing, export style, import style) by majority vote, returning a named convention for an axis when one variant dominates at >=70% over >=8 samples (D-01)"
    - "deriveConventions returns a contested hotspot (not a named convention) when the dominant share is below 70% (e.g. the repo-wide CJS/SDK export split)"
    - "deriveConventions returns insufficient-data when an axis has fewer than minSamples observations"
    - "checkConformance flags a changed file that deviates from a named convention and passes a conforming one"
    - "checkConformance never emits a finding for a contested axis (you cannot deviate from author's choice)"
    - "verb-vs-body flags a read-verb function whose body has a strong mutation signal; passes a mutating-verb function with a pure body"
    - "architectural-split classifies process.env (direct-env) vs injected config, and catch bodies as swallow/rethrow/wrap"
    - "Every finding carries tier 'CONVENTION' and blocking:false; no finding sets blocking:true"
    - "Non-JS/TS input skips idiom checks gracefully (returns no idiom findings, never throws)"
    - "Both public functions never throw; on bad input they return a skipped result with the same emptied field set as the success path"
  artifacts:
    - path: "bin/lib/conventions.cjs"
      provides: "deriveConventions + checkConformance single source of truth (D-04)"
      exports: ["deriveConventions", "checkConformance", "summarizeAxis", "classifyCasing", "sanitizePaths"]
      min_lines: 200
    - path: "tests/conventions.test.cjs"
      provides: "Zero-dep unit + integration tests for CONV-01..04, D-03, D-05"
      min_lines: 80
  key_links:
    - from: "tests/conventions.test.cjs"
      to: "bin/lib/conventions.cjs"
      via: "require('../bin/lib/conventions.cjs')"
      pattern: "require.*bin/lib/conventions"
    - from: "bin/lib/conventions.cjs"
      to: "summarizeAxis dominance/entropy decision"
      via: "share >= 0.70 && total >= 8 gate"
      pattern: "0\\.70|dominanceThreshold|minSamples"
---

<objective>
Build the single deterministic source of truth for convention derivation and conformance checking: `bin/lib/conventions.cjs`. This module is called by BOTH `gsd-pattern-mapper` (to write the PATTERNS.md Conventions section) and `gsd-code-reviewer` (to emit CONVENTION-tier findings) per D-04 — no second extraction implementation anywhere.

This is a TDD plan: the test file is written FIRST (Wave 0 scaffold), then the module is implemented to make it pass. The module is a pure, never-throwing function pair using only Node built-ins (zero new runtime dependency — hard milestone constraint).

Purpose: CONV-01 (4-axis majority-vote derivation + entropy), CONV-02 (per-file conformance), CONV-03 (verb-vs-body intent), CONV-04 (architectural-split), all reporting at the advisory `CONVENTION` tier (D-03) and skipping idiom checks gracefully on non-JS/TS input (D-05).
Output: `bin/lib/conventions.cjs` (module), `tests/conventions.test.cjs` (tests).
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-convention-and-architectural-conformance/10-CONTEXT.md
@.planning/phases/10-convention-and-architectural-conformance/10-RESEARCH.md
@.planning/phases/10-convention-and-architectural-conformance/10-PATTERNS.md
@.planning/phases/10-convention-and-architectural-conformance/10-VALIDATION.md

<interfaces>
<!-- Public contract this module MUST expose. Downstream plans (subcommand wiring, agents) call ONLY these. -->

deriveConventions(files, opts?) -> {
  skipped: false,
  axes: [ { name, status: 'named'|'contested'|'insufficient-data', dominant, share, entropy, contested, total, variants } ]
} | { skipped: true, reason, axes: [] }

checkConformance(changedFiles, derived) -> {
  skipped: false,
  findings: [ { tier: 'CONVENTION', blocking: false, file, line, deviation, convention, fix } ]
} | { skipped: true, reason, findings: [] }

Axis names (D-01, 4 axes): 'file-name-casing', 'identifier-casing', 'export-style', 'import-style'.
Tunables (Discretion): dominanceThreshold default 0.70, minSamples default 8.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for derivation + conformance (Wave 0 scaffold)</name>
  <files>tests/conventions.test.cjs</files>
  <read_first>
    - tests/base-branch-resolver.test.cjs (full file — the zero-dep harness analog: shebang, `node:assert`, bare `check(name, fn)` runner, failure counter, child_process integration check, `process.exit(1)` footer)
    - .planning/phases/10-convention-and-architectural-conformance/10-VALIDATION.md (Per-Task Verification Map — the exact behaviors to assert)
    - .planning/phases/10-convention-and-architectural-conformance/10-PATTERNS.md (section `tests/conventions.test.cjs (NEW)` — line ranges to copy from the analog)
  </read_first>
  <behavior>
    - CONV-01: deriveConventions names a convention at >=70% share over >=8 samples; entropy ~0 for single variant
    - CONV-01: deriveConventions marks an axis contested (status 'contested', dominant null) when top share < 0.70; entropy ~1 for an even split
    - CONV-01: deriveConventions returns status 'insufficient-data' when an axis has < minSamples (8) observations
    - CONV-02: checkConformance returns a finding for a file deviating from a NAMED convention; returns none for a conforming file
    - CONV-02: checkConformance never emits a finding for a CONTESTED axis
    - CONV-03: verb-vs-body flags a `get*`/`read*` function whose body assigns to a param/outer scope or calls fs.writeFileSync/.push on an arg; passes a `save*`/`update*` function with a pure return body
    - CONV-04: arch-split classifies a file containing process.env as direct-env vs an injected-config file; classifies catch bodies as swallow / rethrow / wrap
    - D-03: every finding has tier === 'CONVENTION' and blocking === false; assert NO finding has blocking === true
    - D-05: a non-JS/TS input (e.g. a .py path or .md content) yields no idiom findings and does not throw (skipped or empty findings, never an exception)
    - integration: spawning `node bin/gsd-tools.cjs verify conventions --check --files <csv>` returns parseable JSON (this assertion will go RED until Plan 02 wires the subcommand — mark it pending/skipped in this plan's test run OR scope it behind a guard; see action)
  </behavior>
  <action>
    Create `tests/conventions.test.cjs` mirroring `tests/base-branch-resolver.test.cjs` exactly for scaffold: `#!/usr/bin/env node`, `'use strict'`, `require('node:assert')`, `require('../bin/lib/conventions.cjs')`, the bare `check(name, fn)` runner with a `failures` counter, and the `process.exit(1)` summary footer.

    Write one `check(...)` per behavior bullet above using `node:assert` (strictEqual / ok / deepStrictEqual). Build small in-memory fixtures: for derivation, pass synthetic file lists or pre-tallied variant counts to `summarizeAxis`; for conformance, pass tiny source strings as the changed-file bodies (the module reads source text). Use the verb taxonomy from RESEARCH §Pattern 6 (read-only: get/list/find/read/is/has/...; mutating: set/update/create/delete/save/write/push/...) and the catch taxonomy from §Pattern 7 (swallow/rethrow/wrap).

    For the gsd-tools integration assertion: this module exists but the subcommand is wired in Plan 02. Do NOT make this plan's `node tests/conventions.test.cjs` fail because of unwired plumbing. Either (a) gate the integration `check` behind a try/catch that records it as a known-pending skip when the subcommand prints "Unknown verify subcommand", or (b) assert only that the module's `checkConformance` JSON shape is valid here and move the spawn-gsd-tools assertion to verify in Plan 02. Pick (a): wrap the spawn in try/catch, print `pending - integration (wired in 10-02)` and do not increment failures when the subcommand is not yet routed.

    Initially this test MUST fail (RED) because `bin/lib/conventions.cjs` does not yet exist. Commit RED: `test(10-01): add failing tests for conventions module`.
  </action>
  <verify>
    <automated>node tests/conventions.test.cjs; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/conventions.test.cjs` exists, is executable-shaped (shebang + 'use strict'), and `require`s `../bin/lib/conventions.cjs`
    - Running `node tests/conventions.test.cjs` exits NON-zero (RED) before the module exists (cannot require a missing module / assertions fail)
    - At least one `check(...)` exists for each of: CONV-01 (named), CONV-01 (contested), CONV-01 (insufficient-data), CONV-02 (flag+pass), CONV-02 (contested-skip), CONV-03, CONV-04, D-03, D-05
    - The gsd-tools spawn assertion does not count as a failure when the subcommand is not yet routed (prints a pending line)
  </acceptance_criteria>
  <done>Failing test file committed; `node tests/conventions.test.cjs` exits non-zero pending the module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bin/lib/conventions.cjs (deriveConventions + checkConformance) to GREEN</name>
  <files>bin/lib/conventions.cjs</files>
  <read_first>
    - bin/lib/drift.cjs (lines 31-34 header/imports; 44-83 classifier table + first-match classifyFile; 66 SAFE_PATH_RE; 121-229 detectDrift never-throw + skipped() shape; 292-302 sanitizePaths; 369-379 module.exports) — the never-throw contract, classifier-table, and path-sanitization analog
    - bin/lib/schema-detect.cjs (lines 17-37 frozen {pattern,label} table; 142/156/180 explicit `blocking:` field) — the classifier table shape and explicit-blocking-field pattern (invert it: CONVENTION is always blocking:false)
    - .planning/phases/10-convention-and-architectural-conformance/10-RESEARCH.md (§Code Examples "Normalized Shannon entropy + dominance" summarizeAxis; "Identifier extraction" extractIdentifiers + string-blanking pre-pass; §Pattern 6 verb taxonomy + body mutation signals; §Pattern 7 DI-vs-env + catch classification; §Pitfall 2 brace-counting; §Pitfall 4 local-vs-outer mutation guard)
    - .planning/phases/10-convention-and-architectural-conformance/10-PATTERNS.md (section `bin/lib/conventions.cjs (NEW)` — exact analog line ranges and the finding shape)
  </read_first>
  <action>
    Create `bin/lib/conventions.cjs`. Header: `'use strict'`, `require('node:fs')`, `require('node:path')` — Node built-ins only, NO new runtime dependency, do NOT import platformWriteSync (this module never writes; mapper/reviewer own writes).

    Implement the frozen classifier table `CASING_RULES = [{ re, label }]` (kebab / snake / camel / Pascal / CONSTANT / other) and `classifyCasing(name)` as a first-match loop, mirroring `drift.cjs` classifyFile and `schema-detect.cjs` SCHEMA_PATTERNS shape.

    Implement `summarizeAxis(counts, opts)` exactly per RESEARCH §Code Examples: normalized Shannon entropy `H_norm = -Σ p·log(p) / log(n)`, dominant = max share; `dominanceThreshold` default 0.70, `minSamples` default 8; return `status` of 'insufficient-data' (total < minSamples), 'contested' (share < threshold), or 'named'.

    Implement `blankSpans(src)` — the string/template/regex/comment-blanking pre-pass (Pitfall 2) — and `extractIdentifiers(src)` (regex over the blanked source: `function X`, `const|let X =`, `class X`) per RESEARCH §Code Examples.

    Implement `deriveConventions(files, opts)`: for each readable file in the (sanitized) `files` list, tally the 4 axes — `file-name-casing` (basename via classifyCasing), `identifier-casing` (extractIdentifiers, classify per kind), `export-style` (module.exports/exports. vs export/export default), `import-style` (require( vs import...from). Run each tally through `summarizeAxis`. Return `{ skipped:false, axes:[...] }`. Support an optional `opts.scope` directory so a `bin/lib/*.cjs` file is judged against CJS, not the whole repo (RESEARCH Pattern 4 / Pitfall 1).

    Implement `checkConformance(changedFiles, derived)`: for each changed file, emit findings ONLY against axes whose status is 'named' (skip contested + insufficient-data — CONV-02 / Pitfall 5). Run the JS/TS rule packs: verb-vs-body (flag ONLY read-verb name + strong mutation signal on a PARAMETER/outer-scope var or side-effecting I/O — NOT local-array mutation, per Pitfall 4; do NOT flag the reverse direction, Pattern 6) and architectural-split (DI-vs-env + catch swallow/rethrow/wrap, ship conservatively per Pattern 7). On a non-JS/TS file extension, SKIP the idiom rule packs (D-05) — emit no idiom findings, never error. Every finding object is `{ tier:'CONVENTION', blocking:false, file, line, deviation, convention, fix }`.

    Never-throw contract (load-bearing): copy the `detectDrift` try/catch + `skipped(reason)` shape from `drift.cjs:121-229`. Both public functions validate input at the top, wrap the body in try/catch, and return a skipped result whose field set mirrors the success path emptied (`axes:[]` / `findings:[]`). Reuse `SAFE_PATH_RE` and `sanitizePaths` verbatim from `drift.cjs` before reading any path.

    Export: `module.exports = { deriveConventions, checkConformance, summarizeAxis, classifyCasing, sanitizePaths }`.

    Iterate to GREEN against `tests/conventions.test.cjs`. Tune the verb taxonomy / catch taxonomy until the unit checks pass. Commit GREEN: `feat(10-01): implement conventions derivation + conformance module`.
  </action>
  <verify>
    <automated>node tests/conventions.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/conventions.test.cjs` exits 0 (all unit checks green; integration assertion still pending until 10-02)
    - `node -e "const c=require('./bin/lib/conventions.cjs'); ['deriveConventions','checkConformance','summarizeAxis','classifyCasing','sanitizePaths'].forEach(f=>{if(typeof c[f]!=='function')throw new Error('missing '+f)})"` exits 0
    - `node -e "const c=require('./bin/lib/conventions.cjs'); const r=c.deriveConventions(null); if(!r.skipped||!Array.isArray(r.axes))throw new Error('bad skip shape')"` exits 0 (never-throw on bad input, emptied success-path fields)
    - `node -e "const c=require('./bin/lib/conventions.cjs'); const r=c.checkConformance([{file:'x.py',src:'def f():\n pass'}],{axes:[]}); if(r.findings.some(f=>f.blocking===true))throw new Error('blocking finding')"` exits 0 (D-03 + D-05: no blocking finding, non-JS skips idiom checks)
    - grep confirms zero new runtime deps: `grep -nE "require\\('(?!node:)[^.]" bin/lib/conventions.cjs` returns nothing (only node: built-ins and relative requires)
  </acceptance_criteria>
  <done>Module implemented; all unit tests green; never-throw + zero-dep + blocking:false contracts proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller -> deriveConventions/checkConformance | A file-path list (from `--files` or a corpus scan) and raw source text cross into the module; both are untrusted at the module boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | path inputs to deriveConventions/checkConformance | mitigate | Reuse `SAFE_PATH_RE` + `sanitizePaths` from drift.cjs verbatim; reject absolute paths and `..` traversal before any fs read (V5) |
| T-10-02 | Denial of Service | identifier/idiom regexes on adversarial source | mitigate | Keep all regexes linear (no nested quantifiers / catastrophic backtracking); cap per-file scan size; the string-blanking pre-pass is linear |
| T-10-03 | Denial of Service | malformed/binary file fed as source | mitigate | Never-throw contract: per-file scan wrapped in try/catch, skip non-text, return `{skipped:true,reason}` rather than crash the review path |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | N/A this plan installs zero packages (Node built-ins only); no [ASSUMED]/[SUS] package, no install task — RESEARCH Package Legitimacy Audit: not applicable |
</threat_model>

<verification>
- `node tests/conventions.test.cjs` exits 0 (unit suite green)
- Module exports the 5 named functions; never throws on null/garbage input
- No finding ever sets `blocking:true` (D-03); non-JS/TS input emits no idiom findings (D-05)
- Zero new runtime dependency (grep gate above)
</verification>

<success_criteria>
- `bin/lib/conventions.cjs` exists as the single source of truth (D-04) with deriveConventions + checkConformance
- 4-axis majority-vote derivation with normalized-entropy + 0.70/minSamples-8 gates (CONV-01)
- Per-file conformance scoped to named axes, contested axes never flagged (CONV-02)
- Verb-vs-body (CONV-03) and architectural-split (CONV-04) JS/TS rule packs that skip on non-JS/TS (D-05)
- Every finding tier 'CONVENTION', blocking:false (D-03)
- `tests/conventions.test.cjs` green; zero new runtime deps
</success_criteria>

<output>
Create `.planning/phases/10-convention-and-architectural-conformance/10-01-SUMMARY.md` when done
</output>
