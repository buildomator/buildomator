---
phase: 10-convention-and-architectural-conformance
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - agents/gsd-pattern-mapper.md
  - agents/gsd-code-reviewer.md
  - workflows/code-review.md
autonomous: true
requirements: [CONV-01, CONV-02, CONV-03, CONV-04]
nyquist_compliant: true

must_haves:
  truths:
    - "gsd-pattern-mapper runs `verify conventions --derive` and writes a new additive `## Conventions` section into PATTERNS.md, leaving the existing analog-mapping output unchanged (D-02)"
    - "The Conventions section names a convention only at >=70% dominance and lists below-threshold axes as contested hotspots, naming the CJS<->SDK dual resolver as the prototype intentional split"
    - "gsd-code-reviewer defines a CONVENTION advisory tier ranked below WARNING that never blocks and never gates"
    - "gsd-code-reviewer calls `verify conventions --check --files <csv>` at review time (standalone, no dependency on the mapper having run) and emits CONVENTION-tier findings into REVIEW.md"
    - "The verb-vs-body and architectural-split checks live in the reviewer's depth_levels as JS/TS rule packs that skip gracefully on no-pack languages"
  artifacts:
    - path: "agents/gsd-pattern-mapper.md"
      provides: "Step that derives conventions and writes the PATTERNS.md Conventions section (CONV-01)"
      contains: "verify conventions"
    - path: "agents/gsd-code-reviewer.md"
      provides: "CONVENTION advisory tier + the 3 checks (CONV-02/03/04)"
      contains: "CONVENTION"
    - path: "workflows/code-review.md"
      provides: "review-path wiring note for the conventions call"
      contains: "conventions"
  key_links:
    - from: "agents/gsd-pattern-mapper.md"
      to: "gsd-tools.cjs verify conventions --derive"
      via: "Bash invocation in the derive step"
      pattern: "verify conventions --derive"
    - from: "agents/gsd-code-reviewer.md"
      to: "gsd-tools.cjs verify conventions --check"
      via: "Bash invocation at review time"
      pattern: "verify conventions --check"
---

<objective>
Surface the conventions module to the two consuming agents and the review workflow (the markdown layer). `gsd-pattern-mapper` gains a step that derives conventions and writes the additive `## Conventions` section into PATTERNS.md (CONV-01 / D-02). `gsd-code-reviewer` gains the advisory `CONVENTION` tier (D-03) and the three checks (conformance / verb-vs-body / architectural-split) as JS/TS rule packs in its depth_levels (CONV-02/03/04, D-05). `workflows/code-review.md` gets a minimal note wiring the call into the existing review path (reviewer self-invokes per D-04).

These are insertion-point edits — keep them terse (minimize-workflow-tokens steer; agent markdown loads every invocation).
Purpose: CONV-01 (PATTERNS.md Conventions section), CONV-02/03/04 (reviewer emits CONVENTION findings), all via the SAME module from 10-01 invoked through the 10-02 subcommand.
Output: edits to gsd-pattern-mapper.md, gsd-code-reviewer.md, code-review.md.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-convention-and-architectural-conformance/10-CONTEXT.md
@.planning/phases/10-convention-and-architectural-conformance/10-RESEARCH.md
@.planning/phases/10-convention-and-architectural-conformance/10-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Conventions-derivation step to gsd-pattern-mapper</name>
  <files>agents/gsd-pattern-mapper.md</files>
  <read_first>
    - agents/gsd-pattern-mapper.md (Step 5 "Identify Shared Patterns" at :136 and Step 6 "Write PATTERNS.md" at :145 — the insertion point is between them)
    - .planning/phases/10-convention-and-architectural-conformance/10-RESEARCH.md (§Pattern 4 — the `## Conventions` section shape: axis table with Dominant/Share/Entropy/Status columns + the contested-hotspot note naming the CJS<->SDK dual resolver; §"Agent invocation" — the `${CLAUDE_PLUGIN_ROOT:-...}` $ROOT resolver form)
    - .planning/phases/10-convention-and-architectural-conformance/10-PATTERNS.md (Insertion Points -> gsd-pattern-mapper.md — terse, no analog excerpt)
  </read_first>
  <action>
    Insert a terse Step 5.5 "Derive Conventions" between Step 5 (:136) and Step 6 (:145). The step: resolve `$ROOT` via the standard `${CLAUDE_PLUGIN_ROOT:-$(ls -d "$HOME/.claude/plugins/cache/gsd-plugin/gsd/"*/ 2>/dev/null|sort -V|tail -1)}` form, run `node "$ROOT/bin/gsd-tools.cjs" verify conventions --derive --scope <relevant-dir>` via Bash, parse the JSON `axes`. Instruct the mapper to write a new additive `## Conventions` section in PATTERNS.md (per D-02 the existing analog-mapping output is unchanged) using the §Pattern 4 shape: an axis table (File-name casing, Identifier casing, Export style, Import style) with Dominant / Share / Entropy / Status columns, where Status is "named contract" at >=70% dominance and "contested hotspot" below. Below the table, a "Contested hotspots (author's choice)" note that names the CJS<->SDK dual resolver as the prototype intentional-contested split and advises reviewers/planners to match the directory's local style. Keep the prose compact.
  </action>
  <verify>
    <automated>grep -q "verify conventions --derive" agents/gsd-pattern-mapper.md && grep -q "## Conventions" agents/gsd-pattern-mapper.md</automated>
  </verify>
  <acceptance_criteria>
    - `agents/gsd-pattern-mapper.md` contains a step invoking `node "$ROOT/bin/gsd-tools.cjs" verify conventions --derive` via Bash
    - The step instructs writing an additive `## Conventions` section to PATTERNS.md with the 4-axis table (Dominant/Share/Entropy/Status)
    - The step instructs a contested-hotspot note that names the CJS<->SDK dual resolver
    - `grep -q "70" agents/gsd-pattern-mapper.md` near the conventions step (the named-vs-contested threshold is stated)
    - The existing Step 5 / Step 6 content is preserved (additive only — D-02)
  </acceptance_criteria>
  <done>Pattern-mapper derives conventions and writes the additive Conventions section to PATTERNS.md.</done>
</task>

<task type="auto">
  <name>Task 2: Add the CONVENTION tier + 3 checks to gsd-code-reviewer and wire the workflow</name>
  <files>agents/gsd-code-reviewer.md, workflows/code-review.md</files>
  <read_first>
    - agents/gsd-code-reviewer.md (the `<adversarial_stance>` classification list at :28-31 — currently BLOCKER + WARNING only; `<depth_levels>` at :59-90 — the language-aware per-language check lists, home for the JS/TS rule packs)
    - workflows/code-review.md (lines 354-403 the Bash structural pre-pass / fallow block + :405 the spawn_reviewer step; :355 the existing `${CLAUDE_PLUGIN_ROOT:-...}` resolver)
    - .planning/phases/10-convention-and-architectural-conformance/10-RESEARCH.md (§Pattern 5 conformance scoped to changed files; §Pattern 6 verb-vs-body direction-restricted; §Pattern 7 architectural-split ship-conservatively; D-03 never-block; D-04 reviewer standalone; D-05 skip on no-pack language)
    - .planning/phases/10-convention-and-architectural-conformance/10-PATTERNS.md (Insertion Points -> gsd-code-reviewer.md + workflows/code-review.md)
  </read_first>
  <action>
    In `agents/gsd-code-reviewer.md` `<adversarial_stance>` (:28-31), add a third tier line below WARNING: `- **CONVENTION** — advisory consistency deviation; NEVER blocks, NEVER gates a merge; states the deviation, the derived convention it violates, and a suggested fix (recommend-fix framing).` (ranked below WARNING — D-03). In `<depth_levels>` (:59-90), add the three checks as language-aware JS/TS rule packs that skip gracefully on no-pack languages (D-05): (1) convention conformance against the derived named axes (scoped to the changed `files`, never flag a contested axis — Pattern 5), (2) verb-vs-body intent (read-verb name + body mutation signal, ONE direction only — Pattern 6), (3) architectural-split (DI-vs-env + catch swallow/rethrow/wrap, ship conservatively — Pattern 7). Add the invocation: the reviewer calls the SAME module at review time via Bash — `node "$ROOT/bin/gsd-tools.cjs" verify conventions --check --files "<csv>"` — parses the JSON, and emits CONVENTION-tier findings into REVIEW.md. State it is standalone: does NOT depend on the mapper having run (D-04). Keep all additions terse.

    In `workflows/code-review.md`, add a minimal note near the structural pre-pass / spawn_reviewer step (:354-405) that the reviewer self-invokes `verify conventions --check --files ...` for the changed-`files` set using the existing `${CLAUDE_PLUGIN_ROOT:-...}` resolver at :355. Prefer reviewer self-invocation (D-04 standalone) over the workflow pre-computing — the workflow note is one or two lines.
  </action>
  <verify>
    <automated>grep -q "CONVENTION" agents/gsd-code-reviewer.md && grep -q "verify conventions --check" agents/gsd-code-reviewer.md && grep -q "conventions" workflows/code-review.md</automated>
  </verify>
  <acceptance_criteria>
    - `agents/gsd-code-reviewer.md` `<adversarial_stance>` defines a `CONVENTION` tier described as never-blocking / never-gating, ranked below WARNING
    - The reviewer's `<depth_levels>` lists the three checks (conformance, verb-vs-body, architectural-split) as JS/TS rule packs that skip on no-pack languages
    - `agents/gsd-code-reviewer.md` contains `verify conventions --check --files` invocation and states it is standalone (D-04)
    - `workflows/code-review.md` references the conventions check in the review path with the existing resolver
    - BLOCKER / WARNING tier semantics in the reviewer are unchanged (additive only — D-03)
  </acceptance_criteria>
  <done>Reviewer has the CONVENTION tier + 3 standalone checks; workflow wires the call into the review path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review workflow -> reviewer subagent -> verify conventions --check | the changed-`files` list crosses from the workflow into the subcommand; the module re-sanitizes paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Elevation of Privilege | a CONVENTION finding escalating to a blocking gate | mitigate | Reviewer markdown pins the tier below WARNING and states never-blocks/never-gates (D-03); BLOCKER/WARNING semantics untouched |
| T-10-08 | Tampering | changed-file paths spliced into the subcommand from the workflow | mitigate | The `--files` CSV is sanitized inside the module (SAFE_PATH_RE) from 10-01/10-02; markdown only forwards the existing changed-`files` list |
| T-10-09 | Repudiation | contested axes silently dropped (losing the Phase 11 head start) | accept | Contested axes are reported as hotspots in the PATTERNS.md section (D-01), not suppressed; low risk, documentation-only |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs zero packages (markdown-only edits) — no [ASSUMED]/[SUS] package, no install task |
</threat_model>

<verification>
- pattern-mapper invokes `verify conventions --derive` and writes the `## Conventions` section (additive)
- reviewer defines the CONVENTION tier (never-block) + the 3 JS/TS rule-pack checks + the `--check` invocation (standalone)
- code-review.md wires the call into the review path
- BLOCKER/WARNING semantics and existing pattern-mapper output unchanged
- Manual UAT (per VALIDATION.md): a real `/gsd:code-review` run on a branch with a deliberately convention-violating file surfaces an advisory CONVENTION finding that does not block
</verification>

<success_criteria>
- CONV-01: PATTERNS.md gains a derived `## Conventions` section naming conventions at >=70% and listing contested hotspots (CJS<->SDK resolver)
- CONV-02/03/04: reviewer emits CONVENTION-tier findings for conformance / verb-vs-body / architectural-split via the shared module, standalone (D-04)
- D-03 advisory tier never blocks; D-05 idiom checks skip on no-pack languages
- Edits are terse (minimize-workflow-tokens)
</success_criteria>

<output>
Create `.planning/phases/10-convention-and-architectural-conformance/10-03-SUMMARY.md` when done
</output>
