---
phase: 10-convention-and-architectural-conformance
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - bin/lib/verify.cjs
  - bin/lib/verify-command-router.cjs
  - sdk/src/query/command-manifest.verify.ts
  - bin/lib/command-aliases.generated.cjs
  - bin/gsd-tools.cjs
  - .github/workflows/check-drift.yml
autonomous: true
requirements: [CONV-01, CONV-02, CONV-03, CONV-04]
nyquist_compliant: true

must_haves:
  truths:
    - "node bin/gsd-tools.cjs verify conventions --derive --scope bin/lib emits valid JSON with the 4 derived axes"
    - "node bin/gsd-tools.cjs verify conventions --check --files a,b emits valid JSON findings; every finding tier 'CONVENTION' and blocking false"
    - "The subcommand never exits non-zero: bad input emits {skipped:true,reason} JSON and returns 0"
    - "verify conventions is registered: VERIFY_SUBCOMMANDS in command-aliases.generated.cjs includes 'conventions'"
    - "The new test file runs in CI via a check-drift.yml job"
  artifacts:
    - path: "bin/lib/verify.cjs"
      provides: "cmdVerifyConventions JSON subcommand handler"
      contains: "cmdVerifyConventions"
    - path: "bin/lib/verify-command-router.cjs"
      provides: "conventions dispatch branch"
      contains: "conventions"
    - path: "sdk/src/query/command-manifest.verify.ts"
      provides: "verify.conventions manifest entry"
      contains: "verify.conventions"
    - path: ".github/workflows/check-drift.yml"
      provides: "conventions CI test job"
      contains: "conventions.test.cjs"
  key_links:
    - from: "bin/lib/verify-command-router.cjs"
      to: "verify.cmdVerifyConventions"
      via: "conventions subcommand branch"
      pattern: "cmdVerifyConventions"
    - from: "bin/lib/verify.cjs cmdVerifyConventions"
      to: "bin/lib/conventions.cjs"
      via: "lazy require + output() JSON"
      pattern: "require\\('\\./conventions\\.cjs'\\)"
    - from: "sdk/src/query/command-manifest.verify.ts"
      to: "bin/lib/command-aliases.generated.cjs"
      via: "manifest + hand-synced command-aliases.generated.cjs entry"
      pattern: "verify\\.conventions"
---

<objective>
Wire `bin/lib/conventions.cjs` into the `gsd-tools.cjs verify conventions` JSON subcommand so both agents can invoke it via Bash (`node bin/gsd-tools.cjs verify conventions --derive|--check ...`). In this flat plugin layout the subcommand is reachable via the hardcoded router branch (Task 1); registration is recorded in two hand-synced source-of-truth lists: the TS manifest (`command-manifest.verify.ts`) and the CJS alias map (`command-aliases.generated.cjs`), which in this repo is maintained directly (the generator does not emit the `.cjs`). Plus the CI test job so the module is gated on every push.

Purpose: make the single-source-of-truth module reachable from subagent Bash with a stable JSON contract (CONV-01..04 wiring surface), record the subcommand in both the TS manifest and the CJS alias map, and gate `tests/conventions.test.cjs` in CI.
Output: edits to verify.cjs, verify-command-router.cjs, command-manifest.verify.ts, hand-synced command-aliases.generated.cjs entry, gsd-tools.cjs help text, check-drift.yml.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/3.7.1/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-convention-and-architectural-conformance/10-RESEARCH.md
@.planning/phases/10-convention-and-architectural-conformance/10-PATTERNS.md

<interfaces>
<!-- The module contract this subcommand wraps (from 10-01). Do not re-derive — call it. -->
deriveConventions(files, opts?) -> { skipped, axes }
checkConformance(changedFiles, derived) -> { skipped, findings:[{tier:'CONVENTION',blocking:false,...}] }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cmdVerifyConventions handler + router branch</name>
  <files>bin/lib/verify.cjs, bin/lib/verify-command-router.cjs, bin/gsd-tools.cjs</files>
  <read_first>
    - bin/lib/verify.cjs (lines 1188-1262 cmdVerifySchemaDrift: signature, lazy require, usage error, output({...},raw) snake_case JSON; 1272-1303 cmdVerifyCodebaseDrift: emit-wrapper + {skipped:true} on every failure path, never exit non-zero; 1399-1412 module.exports object; line 8 output import from ./core.cjs)
    - bin/lib/verify-command-router.cjs (lines 3 VERIFY_SUBCOMMANDS import; 20-28 schema-drift / codebase-drift branches + the final unknown-subcommand else)
    - bin/gsd-tools.cjs (lines 114-115 usage-comment listing verify schema-drift/codebase-drift; 610-619 case 'verify': delegates wholesale to routeVerifyCommand — body needs NO edit)
    - bin/lib/conventions.cjs (the module from 10-01 — the deriveConventions/checkConformance contract being wrapped)
  </read_first>
  <action>
    In `bin/lib/verify.cjs`, add `function cmdVerifyConventions(cwd, opts, raw)` mirroring `cmdVerifySchemaDrift` (1188) and `cmdVerifyCodebaseDrift` (1272): lazily `require('./conventions.cjs')`, read `opts` for `--derive` / `--check` / `--scope <dir>` / `--files <csv>`. On `--derive`: scan the scope corpus and call `deriveConventions`, then `output({ mode: 'derive', axes: result.axes, skipped: result.skipped || false }, raw)`. On `--check`: split the `--files` CSV (run it through the module's `sanitizePaths`), read each file's source, call `checkConformance`, then `output({ mode: 'check', findings: result.findings, skipped: result.skipped || false }, raw)`. Match cmdVerifyCodebaseDrift's never-exit-nonzero contract: every failure/usage path emits a `{ skipped: true, reason }` JSON via the output wrapper and returns; the subcommand must NEVER exit non-zero so the review/plan gate cannot fail the phase. Use snake_case JSON keys per the schema-drift analog. Append `cmdVerifyConventions` to `module.exports` at 1399-1412.

    In `bin/lib/verify-command-router.cjs`, add a `} else if (subcommand === 'conventions') {` branch before the final unknown-subcommand `else` (after the `codebase-drift` branch at :25-26): parse `args.slice(2)` for `--derive` / `--check` / `--scope <value>` / `--files <value>` into a `parsedOpts` object, then call `verify.cmdVerifyConventions(cwd, parsedOpts, raw)`. This hardcoded branch — not the alias map — is what makes the subcommand reachable in this flat layout.

    In `bin/gsd-tools.cjs`, add one `verify conventions ...` line to the usage-comment block at :114-115 (help text only; the `case 'verify':` body at 610-619 already delegates to routeVerifyCommand — do NOT touch it).
  </action>
  <verify>
    <automated>node bin/gsd-tools.cjs verify conventions --check --files bin/lib/conventions.cjs | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);if(j.findings&&j.findings.some(f=>f.blocking===true))process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `node bin/gsd-tools.cjs verify conventions --check --files bin/lib/conventions.cjs` prints parseable JSON with a `findings` array; no finding has `blocking:true`
    - `node bin/gsd-tools.cjs verify conventions --derive --scope bin/lib` prints parseable JSON with an `axes` array of length 4
    - `node bin/gsd-tools.cjs verify conventions` (no flags) prints a skipped/usage JSON and exits 0 (echo $? is 0)
    - `grep -c cmdVerifyConventions bin/lib/verify.cjs` is >= 2 (definition + export)
    - `grep -q "conventions" bin/lib/verify-command-router.cjs` succeeds
  </acceptance_criteria>
  <done>Handler + router branch added; subcommand emits valid JSON and never exits non-zero.</done>
</task>

<task type="auto">
  <name>Task 2: Register subcommand in TS manifest + hand-synced alias map, add CI test job, run the test</name>
  <files>sdk/src/query/command-manifest.verify.ts, bin/lib/command-aliases.generated.cjs, .github/workflows/check-drift.yml</files>
  <read_first>
    - sdk/src/query/command-manifest.verify.ts (lines 13-14 the verify.schema-drift / verify.codebase-drift manifest entries — copy the shape)
    - bin/lib/command-aliases.generated.cjs (lines ~226-241 the verify.schema-drift / verify.codebase-drift blocks in the VERIFY_COMMAND_ALIASES array — copy the block shape; note: despite the GENERATED banner, no script in this repo emits this `.cjs`, so it is maintained directly here)
    - .github/workflows/check-drift.yml (lines 33-42 the mcp-stdio-framing job — single `node tests/<f>.test.cjs` step scaffold; 19-31 the handoff-schema job installs ajv devDeps, NOT needed here)
    - tests/conventions.test.cjs (the integration assertion that prints `pending - integration (wired in 10-02)` until the subcommand is routed — Task 2 must RUN this and confirm it is no longer pending)
  </read_first>
  <action>
    Register `verify.conventions` in BOTH source-of-truth lists, by hand, kept in sync:

    1. In `sdk/src/query/command-manifest.verify.ts`, add one manifest entry after the verify.codebase-drift line (:14): `{ family: 'verify', canonical: 'verify.conventions', aliases: ['verify conventions'], mutation: false, outputMode: 'json' }`. Match the existing entry shape exactly.

    2. In `bin/lib/command-aliases.generated.cjs`, add the matching entry DIRECTLY to the `VERIFY_COMMAND_ALIASES` array, mirroring the `verify.schema-drift` block at ~:226-233: `{ "canonical": "verify.conventions", "aliases": ["verify conventions"], "subcommand": "conventions", "mutation": false }`. The repo's `gen-command-aliases.ts` generator writes ONLY `sdk/src/query/command-aliases.generated.ts` — it does NOT emit this `.cjs` in the flat plugin layout, so it is maintained directly. Keep this `.cjs` entry and the TS manifest entry in sync by hand (this dual hand-sync IS the CJS<->SDK contested-source split this milestone surfaces). This registration only feeds `VERIFY_SUBCOMMANDS` (used for the unknown-subcommand error string); actual routing is the hardcoded router branch from Task 1.

    Do NOT run `sdk/scripts/check-command-aliases-fresh.mjs` — it is broken independent of this work (line 41 requires a nonexistent `get-shit-done/bin/lib/command-aliases.generated.cjs` sibling path and crashes with MODULE_NOT_FOUND). Fixing it is OUT OF SCOPE for Phase 10. Do NOT depend on it.

    3. In `.github/workflows/check-drift.yml`, add a `conventions` job mirroring the mcp-stdio-framing job (:33-42): `actions/checkout@v4` + `actions/setup-node@v4` node-version '22' + a `run: node tests/conventions.test.cjs` step. No `npm ci` (zero-dep test).

    4. Run `node tests/conventions.test.cjs` and confirm it exits 0 AND the previously-pending integration assertion is no longer pending (the spawn of `verify conventions --check` now routes through Task 1's branch and returns parseable JSON instead of the unknown-subcommand path). If the integration check still prints `pending`, Task 1's router branch is not wired correctly — fix that, do not leave it pending.
  </action>
  <verify>
    <automated>node -e "const a=require('./bin/lib/command-aliases.generated.cjs'); process.exit(a.VERIFY_SUBCOMMANDS.includes('conventions')?0:1)" && node bin/gsd-tools.cjs verify conventions --derive --scope bin/lib | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);process.exit(Array.isArray(j.axes)&&j.axes.length===4?0:1)})" && node tests/conventions.test.cjs && grep -q "conventions.test.cjs" .github/workflows/check-drift.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "verify.conventions" sdk/src/query/command-manifest.verify.ts` succeeds
    - `grep -q "verify.conventions" bin/lib/command-aliases.generated.cjs` succeeds (hand-synced entry)
    - `node -e "const a=require('./bin/lib/command-aliases.generated.cjs'); process.exit(a.VERIFY_SUBCOMMANDS.includes('conventions')?0:1)"` exits 0 (subcommand registered)
    - `node bin/gsd-tools.cjs verify conventions --derive --scope bin/lib` emits valid JSON with an `axes` array of length 4 (subcommand routes and the module is wired)
    - `.github/workflows/check-drift.yml` contains a job that runs `node tests/conventions.test.cjs` with setup-node@v4 node 22
    - `node tests/conventions.test.cjs` exits 0 AND the integration assertion is no longer pending (it actually spawned `verify conventions` and got valid JSON)
  </acceptance_criteria>
  <done>Manifest + hand-synced alias-map entries added, subcommand registered + routes, CI job added, and the test run confirms the integration assertion flipped green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subagent Bash -> verify conventions subcommand | `--files` CSV and `--scope` dir cross from agent-controlled args into the CLI; untrusted at this boundary |
| CLI handler -> conventions.cjs module | the handler forwards paths to the module, which re-sanitizes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | `--files` / `--scope` path args to cmdVerifyConventions | mitigate | Run the CSV through the module's `sanitizePaths` (SAFE_PATH_RE) before reading; reject absolute / traversal paths (V5) |
| T-10-05 | Denial of Service | the subcommand failing the review/plan gate | mitigate | Never-exit-nonzero contract mirrored from cmdVerifyCodebaseDrift: every failure emits {skipped:true,reason} JSON and returns 0 |
| T-10-06 | Tampering | drift between the TS manifest and the hand-synced command-aliases.generated.cjs entry | mitigate | Add the `verify.conventions` entry to BOTH the TS manifest and the `.cjs` alias map by hand and keep them in sync (the repo's generator does not emit the `.cjs`); this dual hand-sync is itself the CJS<->SDK contested-source split this milestone surfaces |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs zero packages; the conventions CI job runs without `npm ci` (zero-dep test) — no [ASSUMED]/[SUS] package |
</threat_model>

<verification>
- `verify conventions --check` and `--derive` emit valid JSON; no finding blocking:true
- subcommand never exits non-zero (bad input -> skipped JSON, exit 0)
- `VERIFY_SUBCOMMANDS` (from command-aliases.generated.cjs) includes 'conventions'; TS manifest has the matching entry
- `tests/conventions.test.cjs` integration assertion now green (no longer pending); CI job runs it
</verification>

<success_criteria>
- `gsd-tools.cjs verify conventions --derive|--check` reachable by short form with a stable snake_case JSON contract
- Subcommand registered in both the TS manifest and the hand-synced `.cjs` alias map; routed via the hardcoded router branch
- Never-exit-nonzero subcommand contract preserved
- `tests/conventions.test.cjs` gated in check-drift.yml and its integration assertion flipped green
</success_criteria>

<output>
Create `.planning/phases/10-convention-and-architectural-conformance/10-02-SUMMARY.md` when done
</output>
</output>
