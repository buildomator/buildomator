---
phase: 15-buildomator-identity-and-communications
plan: 05
type: execute
wave: 3
depends_on: [15-04]
files_modified: []
autonomous: false
requirements: [BRAND-01, BRAND-03]
must_haves:
  truths:
    - "A human confirms the generated dist/bm README migration prose reads correctly in the bm package"
    - "A human confirms the rebranded README renders as Buildomator with the new logo"
    - "A human confirms the migration story reads like a person wrote it"
  artifacts: []
  key_links:
    - from: "dist/bm/README.md migration section"
      to: "correct bm-package prose"
      via: "human read"
      pattern: "Migrating"
---

<objective>
Human verification of the one thing no automated gate can catch: the semantic
correctness and voice of the rebranded prose, in both the gsd source README and the
generated bm package. The bm-parity census only flags literal leaked gsd: tokens; a
wrong-but-clean sentence (for example a migration line that flipped to "/bm: retires"
or reads awkwardly in bm) passes every gate. A person reads it.

Purpose: Closes the semantic slice of BRAND-01 (person-written Buildomator prose) and
BRAND-03 (migration story reads correctly). This is the phase's final sign-off.
Output: Human approval, or a list of prose fixes to route back to plan 15-01/15-02.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-buildomator-identity-and-communications/15-VALIDATION.md
@.planning/phases/15-buildomator-identity-and-communications/15-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human read of the generated bm prose and the rebranded README</name>
  <action>
    Present the generated bm migration prose, the bm manifest description, the rendered
    README, and the four 2026-10-01 sentences to the user for a semantic sign-off, using
    the steps under how-to-verify. This is a read-and-confirm checkpoint: make no source
    edits here. If the user reports a problem, route the fix to source in plan 15-01 or
    15-02 and regenerate dist/bm via plan 15-04.
  </action>
  <what-built>
    The full Buildomator rebrand: a rebranded README.md (H1, intro, /bm: examples,
    Migrating from /gsd: section, new logo, version 4.1.0), Buildomator-worded
    plugin.json + marketplace entries, a CHANGELOG [4.1.0] migration entry, an aligned
    2026-10-01 nudge date, and a regenerated dist/bm package that passed every automated
    gate (drift, parity/census, version alignment, schema).
  </what-built>
  <how-to-verify>
    1. Read the generated bm migration prose and manifest description by eye:
       `sed -n '/Migrating/,/^## /p' dist/bm/README.md`
       `node -e "console.log(require('./dist/bm/.claude-plugin/plugin.json').description)"`
       Expect: NO "/bm: retires ..." sentence, NO "Buildomator -- Buildomator", NO
       "(legacy /gsd:)" in the bm manifest, and the migration section reads correctly
       for a Buildomator user.
    2. Open README.md in a Markdown preview: confirm the Buildomator logo renders, the
       H1 reads "Buildomator for Claude Code", the intro and Migrating section read like
       a person wrote them (no em-dashes, no "canonical", no AI-marketing tells), the
       command examples show /bm:, and the Plugin version reads 4.1.0.
    3. Eyeball the four 2026-10-01 sentences for consistent wording:
       `grep -rn "2026-10-01" CHANGELOG.md README.md .claude-plugin/marketplace.json bin/gsd-tools.cjs`
    4. Skim the CHANGELOG [4.1.0] entry: it explains the rebrand, the additive /bm: +
       retained /gsd: strategy, and the v5.0 retirement on 2026-10-01.
  </how-to-verify>
  <resume-signal>
    Type "approved" if the generated bm prose and the rebranded README read correctly,
    or describe the exact sentences to fix (they will be corrected in source in plan
    15-01/15-02 and dist/bm regenerated via plan 15-04).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated bm prose -> bm users | semantically wrong but gate-clean prose can only be caught by a human read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | Tampering | wrong-but-clean migration prose in dist/bm/README.md | mitigate | Blocking human read of the generated bm migration section and manifest description |
| T-15-13 | Repudiation | inconsistent retirement wording across the four sites | mitigate | Human eyeballs all four 2026-10-01 sentences for matching wording |
</threat_model>

<verification>
- Human confirms dist/bm migration prose and manifest description are correct
- Human confirms the rebranded README renders as Buildomator (logo, H1, /bm:, version)
- Human confirms the four retirement sentences read consistently
</verification>

<success_criteria>
A human has confirmed the Buildomator prose reads correctly and person-written in both
the source README and the generated bm package, completing the phase.
</success_criteria>

<output>
Create `.planning/phases/15-buildomator-identity-and-communications/15-05-SUMMARY.md` when done
</output>
