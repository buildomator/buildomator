---
phase: 15-buildomator-identity-and-communications
plan: 04
type: execute
wave: 2
depends_on: [15-01, 15-02, 15-03]
files_modified:
  - dist/bm
  - .claude-plugin/marketplace.json
autonomous: true
requirements: [BRAND-01, BRAND-02, BRAND-03]
must_haves:
  truths:
    - "dist/bm is regenerated from the rebranded source and the committed tree equals a fresh deterministic build"
    - "The bm package has zero leaked gsd: tokens and a schema-valid manifest"
    - "The retirement date 2026-10-01 appears in all four required source locations"
    - "plugin.json, both marketplace entries, and the README version line all read 4.1.0"
    - "buildomator.com is wired in plugin.json and both marketplace entries"
  artifacts:
    - path: "dist/bm/.claude-plugin/plugin.json"
      provides: "Regenerated bm manifest at 4.1.0 with the fixed Buildomator description"
    - path: "dist/bm/README.md"
      provides: "Regenerated bm README with flipped command examples"
  key_links:
    - from: "bin/build-bm.cjs"
      to: "dist/bm + marketplace versions"
      via: "regenerate + syncMarketplaceVersions"
      pattern: "dist/bm"
---

<objective>
Regenerate the committed dist/bm package from the rebranded source and run the full
automated gate suite plus the machine-checkable consistency assertions (retirement
date in all four locations, buildomator.com wiring, version alignment at 4.1.0).

Purpose: Proves the bm byte-transform still produces a deterministic, parity-clean,
schema-valid Buildomator package after the branding edits, and that metadata is
consistent. Covers the automated slice of BRAND-01/02/03 verification.
Output: Regenerated dist/bm tree, version-synced marketplace, green gate suite.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-buildomator-identity-and-communications/15-RESEARCH.md
@.planning/phases/15-buildomator-identity-and-communications/15-VALIDATION.md
</context>

<dependency_note>
Depends on 15-01 (README + logo), 15-02 (plugin.json/marketplace/CHANGELOG/nudge), and
15-03 (fixed stampBmManifest + test). All source edits must be in place before the
regen: `node bin/build-bm.cjs` reads plugin.json, README, and the transform to rebuild
dist/bm, and `syncMarketplaceVersions` writes both marketplace entries' version from
plugin.json. This plan modifies dist/bm/** and (via the sync) marketplace.json; both
are downstream of Wave 1 so there is no same-wave file conflict.
</dependency_note>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate dist/bm and confirm deterministic drift-clean build</name>
  <files>dist/bm, .claude-plugin/marketplace.json</files>
  <read_first>
    - .planning/phases/15-buildomator-identity-and-communications/15-RESEARCH.md (Don't Hand-Roll + Runtime State Inventory: dist/bm is always regenerated, never edited)
    - bin/build-bm.cjs (build + --check + syncMarketplaceVersions behavior)
  </read_first>
  <action>
    From the repo root run `node bin/build-bm.cjs` to regenerate the committed dist/bm
    tree and sync both marketplace entries' version from plugin.json. Do NOT hand-edit
    anything under dist/bm. Stage the full regenerated dist/bm tree (and marketplace.json
    if the sync changed it). Then run `node bin/build-bm.cjs --check` and confirm exit 0
    (committed tree equals a fresh deterministic build).
  </action>
  <verify>
    <automated>node bin/build-bm.cjs &amp;&amp; node bin/build-bm.cjs --check</automated>
  </verify>
  <acceptance_criteria>
    - `node bin/build-bm.cjs` exits 0 and dist/bm is regenerated
    - `node bin/build-bm.cjs --check` exits 0 (no drift between committed dist/bm and a fresh build)
    - Both marketplace entries read version 4.1.0 after the sync
    - No file under dist/bm was hand-edited
  </acceptance_criteria>
  <done>The committed dist/bm reflects the rebranded source and passes the drift gate.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full gate suite and metadata-consistency assertions</name>
  <files>dist/bm</files>
  <read_first>
    - .planning/phases/15-buildomator-identity-and-communications/15-VALIDATION.md (Full suite command + Validation Architecture sections a-d)
  </read_first>
  <action>
    From the repo root, run the full validation suite and consistency checks and confirm
    each passes:
    (1) `node tests/build-bm-drift.test.cjs`
    (2) `node tests/bm-parity.test.cjs`  (skill/full inventory + fail-closed gsd-leak census + --check)
    (3) `node bin/validate-plugin.cjs dist/bm/.claude-plugin/plugin.json`
    (4) `node bin/maintenance/check-version-alignment.cjs` and `node tests/version-alignment.test.cjs`
    (5) retirement-date consistency: `grep -rn "2026-10-01" CHANGELOG.md README.md .claude-plugin/marketplace.json bin/gsd-tools.cjs` shows a hit in each of the four files
    (6) buildomator.com wiring: plugin.json homepage+repository and both marketplace entries homepage+repository equal https://buildomator.com; README contains a buildomator.com link
    (7) semantic negative checks on the generated bm package: `dist/bm/README.md` contains no "/bm: retires" and the migration section is present; `dist/bm/.claude-plugin/plugin.json` description contains neither "Buildomator -- Buildomator" nor "legacy /gsd:".
    Report the outcome of each. Any failure blocks the plan.
  </action>
  <verify>
    <automated>node tests/build-bm-drift.test.cjs &amp;&amp; node tests/bm-parity.test.cjs &amp;&amp; node bin/validate-plugin.cjs dist/bm/.claude-plugin/plugin.json &amp;&amp; node bin/maintenance/check-version-alignment.cjs &amp;&amp; node tests/version-alignment.test.cjs &amp;&amp; for f in CHANGELOG.md README.md .claude-plugin/marketplace.json bin/gsd-tools.cjs; do grep -q "2026-10-01" "$f" || { echo "missing date in $f"; exit 1; }; done &amp;&amp; node -e "const p=require('./.claude-plugin/plugin.json'); if(p.homepage!=='https://buildomator.com'||p.repository!=='https://buildomator.com')throw 'plugin links'; const m=require('./.claude-plugin/marketplace.json'); m.plugins.forEach(e=>{if(e.homepage!=='https://buildomator.com'||e.repository!=='https://buildomator.com')throw e.name+' links';}); const d=require('./dist/bm/.claude-plugin/plugin.json').description; if(/Buildomator -- Buildomator/.test(d)||/legacy \/gsd:/.test(d))throw 'bm desc'; console.log('ok')" &amp;&amp; grep -q 'buildomator.com' README.md &amp;&amp; ! grep -q '/bm: retires' dist/bm/README.md</automated>
  </verify>
  <acceptance_criteria>
    - build-bm-drift, bm-parity, version-alignment.test all exit 0
    - `node bin/validate-plugin.cjs dist/bm/.claude-plugin/plugin.json` exits 0
    - `node bin/maintenance/check-version-alignment.cjs` exits 0 (plugin.json == both marketplace entries; milestone major 4 == plugin major 4)
    - `grep "2026-10-01"` hits CHANGELOG.md, README.md, marketplace.json, and bin/gsd-tools.cjs
    - plugin.json and both marketplace entries have homepage==repository=="https://buildomator.com"; README contains a buildomator.com link
    - dist/bm/.claude-plugin/plugin.json description contains no "Buildomator -- Buildomator" and no "legacy /gsd:"
    - dist/bm/README.md contains no "/bm: retires"
  </acceptance_criteria>
  <done>The full gate suite is green and metadata is consistent at 4.1.0 with buildomator.com wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source edits -> committed dist/bm | the regen is the only place the bm package is produced; a stale or hand-edited tree ships wrong |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Tampering | stale committed dist/bm vs source | mitigate | Regenerate then `--check` exit 0; bm-parity census scans the committed tree |
| T-15-10 | Repudiation | version/date drift across manifests | mitigate | check-version-alignment + version-alignment.test + 4-file date grep |
| T-15-11 | Information disclosure | leaked gsd: token or wrong bm prose passing a green gate | mitigate | Automated negative greps here; human read in plan 15-05 for wrong-but-clean prose |
</threat_model>

<verification>
- dist/bm regenerated; `node bin/build-bm.cjs --check` exits 0
- build-bm-drift, bm-parity, version-alignment, validate-plugin all green
- 2026-10-01 present in all four source files
- buildomator.com wired in plugin.json + both marketplace entries; README linked
- bm manifest description clean (no doubling, no legacy clause)
</verification>

<success_criteria>
The committed dist/bm is a deterministic, parity-clean, schema-valid Buildomator
package at 4.1.0; the retirement date and buildomator.com links are consistent across
all required locations; every automated gate passes.
</success_criteria>

<output>
Create `.planning/phases/15-buildomator-identity-and-communications/15-04-SUMMARY.md` when done
</output>
