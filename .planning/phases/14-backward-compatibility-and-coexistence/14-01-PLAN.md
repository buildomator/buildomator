---
phase: 14-backward-compatibility-and-coexistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/lib/state.cjs
  - bin/lib/checkpoint.cjs
  - tests/handoff-write-lock.test.cjs
autonomous: true
requirements: [COMPAT-03]
must_haves:
  truths:
    - "writeCheckpoint acquires and releases a lock around the HANDOFF.json write (no bare fs.writeFileSync) (D-04)"
    - "Interleaved HANDOFF.json and STATE.md writes leave both files valid and parseable"
    - "A crashed writer's stale HANDOFF.json.lock is reclaimed, not a permanent block"
  artifacts:
    - path: "bin/lib/state.cjs"
      provides: "acquireStateLock + releaseStateLock exported for cross-module reuse"
      contains: "acquireStateLock"
    - path: "bin/lib/checkpoint.cjs"
      provides: "HANDOFF.json write serialized through the O_EXCL lock"
      contains: "acquireStateLock"
    - path: "tests/handoff-write-lock.test.cjs"
      provides: "interleaved-write corruption + lock-acquisition assertions"
      min_lines: 40
  key_links:
    - from: "bin/lib/checkpoint.cjs"
      to: "bin/lib/state.cjs"
      via: "require('./state.cjs') acquireStateLock/releaseStateLock"
      pattern: "acquireStateLock"
    - from: "tests/handoff-write-lock.test.cjs"
      to: "bin/lib/checkpoint.cjs"
      via: "spawn concurrent writeCheckpoint writers"
      pattern: "writeCheckpoint"
---

<objective>
Close the single confirmed COMPAT-03 gap: `bin/lib/checkpoint.cjs:398` writes
`HANDOFF.json` with a bare `fs.writeFileSync`, bypassing the O_EXCL lock + atomic path
that STATE.md writes already use. With both the `gsd` and `bm` plugins enabled, two
identical PostToolUse/PreCompact writers can interleave and truncate the file mid-write,
leaving invalid JSON. Route the HANDOFF.json write through the same
`acquireStateLock`/`releaseStateLock` primitive (D-04), and prove serialization with an
interleaved-write test.

Purpose: Guarantee shared project state stays uncorrupted under two concurrent writers.
Output: Exported lock helpers, a locked HANDOFF.json write, and a coexistence test.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-backward-compatibility-and-coexistence/14-RESEARCH.md
@.planning/phases/14-backward-compatibility-and-coexistence/14-PATTERNS.md

<interfaces>
<!-- Verified from the codebase. Use directly - no exploration. -->

bin/lib/state.cjs (already path-generic; NOT yet exported):
  function acquireStateLock(statePath)   // line 947: appends '.lock', O_EXCL, 10-retry,
                                         //   reclaims locks >10s old, exit-cleanup (#1916),
                                         //   fails OPEN on non-EEXIST. Returns lockPath.
  function releaseStateLock(lockPath)    // line 985: unlink, best-effort
  // Existing locked-write template (writeStateMd, lines 1002-1007):
  //   const lockPath = acquireStateLock(statePath);
  //   try { platformWriteSync(statePath, synced); } finally { releaseStateLock(lockPath); }
  // module.exports block starts at line 1926 (acquireStateLock/releaseStateLock NOT listed).

bin/lib/checkpoint.cjs writeCheckpoint (lines 376-404):
  // Guard (a) refuse to create .planning/ (382), Guard (b) skip trivial auto checkpoint (393),
  // then line 397-398:
  //   const outPath = path.join(planningDirPath, 'HANDOFF.json');
  //   fs.writeFileSync(outPath, JSON.stringify(data, null, 2) + '\n', 'utf-8');  // <-- GAP
  // Wrapped in try{...}catch{ data.partial = true; } - 5s PreCompact budget, MUST NOT throw.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export the lock helpers and route the HANDOFF.json write through them</name>
  <files>bin/lib/state.cjs, bin/lib/checkpoint.cjs</files>
  <read_first>
    - bin/lib/state.cjs lines 943-1008 (acquireStateLock/releaseStateLock + the writeStateMd locked-write template) and lines 1926-1955 (module.exports block)
    - bin/lib/checkpoint.cjs lines 376-404 (writeCheckpoint, the two guards, the bare write at 398, the surrounding try/catch)
    - .planning/phases/14-backward-compatibility-and-coexistence/14-PATTERNS.md "bin/lib/state.cjs (MODIFY ...)" and "bin/lib/checkpoint.cjs (MODIFY ...)" sections
  </read_first>
  <behavior>
    - After the change, `require('./bin/lib/state.cjs').acquireStateLock` and `.releaseStateLock` are both functions.
    - writeCheckpoint still returns `data` and never throws (the 5s-budget try/catch is preserved).
    - The HANDOFF.json write is bracketed by acquireStateLock(outPath) ... releaseStateLock(lockPath) in a try/finally.
    - Both guard (a) (no .planning/) and guard (b) (skip trivial auto checkpoint) are preserved and still short-circuit BEFORE any lock is taken.
  </behavior>
  <action>
    Add `acquireStateLock` and `releaseStateLock` to the `module.exports` object in
    bin/lib/state.cjs (the block at line 1926). Do NOT change the lock function bodies -
    they are already path-generic (append `.lock` to any target). In
    bin/lib/checkpoint.cjs writeCheckpoint, `require('./state.cjs')` for the two helpers and
    replace the bare `fs.writeFileSync(outPath, ...)` at line 398 with an
    acquireStateLock(outPath) / try / fs.writeFileSync / finally / releaseStateLock(lockPath)
    sequence, mirroring the writeStateMd template at state.cjs:1002-1007. Keep the outer
    `try { ... } catch { data.partial = true; }` so a lock or write failure still flags
    partial instead of crashing PreCompact. Do not move or weaken guard (a)/(b) above the
    write. Two different files (STATE.md, HANDOFF.json) intentionally get two different
    `.lock` files - only writers to the SAME file must serialize.
  </action>
  <acceptance_criteria>
    - `node -e "const s=require('./bin/lib/state.cjs'); if(typeof s.acquireStateLock!=='function'||typeof s.releaseStateLock!=='function') process.exit(1)"` exits 0
    - `grep -n "acquireStateLock" bin/lib/checkpoint.cjs` shows a require and a call
    - `grep -c "fs.writeFileSync" bin/lib/checkpoint.cjs` - the HANDOFF.json write is inside a try/finally that calls releaseStateLock (verify by reading, not just count)
    - `node -e "const c=require('./bin/lib/checkpoint.cjs'); c.writeCheckpoint(process.cwd());"` completes without throwing
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('./bin/lib/state.cjs'); const c=require('./bin/lib/checkpoint.cjs'); if(typeof s.acquireStateLock!=='function'||typeof s.releaseStateLock!=='function'){console.error('lock helpers not exported');process.exit(1)}; console.log('ok')"</automated>
  </verify>
  <done>acquireStateLock/releaseStateLock are exported from state.cjs; checkpoint.cjs writes HANDOFF.json only inside the lock; writeCheckpoint still never throws.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Interleaved-write coexistence test for HANDOFF.json + STATE.md</name>
  <files>tests/handoff-write-lock.test.cjs</files>
  <read_first>
    - tests/checkpoint-write-guards.test.cjs lines 19-41 (zero-dep harness: check/assert/withTempDir) and its fail-loud footer
    - tests/bm-parity.test.cjs (spawnSync child-process pattern for concurrency + the `if (failures > 0) process.exit(1)` footer)
    - bin/lib/checkpoint.cjs writeCheckpoint (the function under test) and bin/lib/state.cjs acquireStateLock (the primitive)
  </read_first>
  <behavior>
    - Test A (lock acquisition): calling writeCheckpoint on a temp project with an active phase/task writes a HANDOFF.json that parses as valid JSON, and no leftover HANDOFF.json.lock remains afterward.
    - Test B (interleaved writers stay valid): spawn N concurrent child processes (spawnSync fan-out or Promise.all of spawn) that each call writeCheckpoint against the SAME temp .planning/, then assert HANDOFF.json parses as valid JSON after every run and ends with a trailing newline. Run >=20 iterations to expose truncation.
    - Test C (STATE.md coexists): interleave a HANDOFF.json writer and a STATE.md writer against the same temp project; assert both files remain independently valid (JSON / frontmatter parse) and that they use distinct .lock paths (no cross-block).
  </behavior>
  <action>
    Create tests/handoff-write-lock.test.cjs using the zero-dep harness copied verbatim from
    tests/checkpoint-write-guards.test.cjs (check/assert/withTempDir + fail-loud footer with
    `process.exit(1)`). Use `fs.mkdtempSync(path.join(os.tmpdir(), 'gsd-handoff-lock-test-'))`
    for isolation and seed a minimal `.planning/` with STATE.md so the guards pass and a
    non-trivial checkpoint is written (pass an options.source or a phase/task so guard (b)
    does not skip). For the concurrency case, spawn child `node -e` processes that
    `require` the repo's checkpoint.cjs and call writeCheckpoint in a tight loop; after the
    fan-out, JSON.parse the file and assert no "Unexpected end of JSON input". Assert the
    exported acquireStateLock is actually exercised (e.g. spy by asserting a `.lock` file is
    created-then-removed, or assert no stale `HANDOFF.json.lock` remains). Do NOT use any
    test framework, watch flag, or external dependency.
  </action>
  <acceptance_criteria>
    - `node tests/handoff-write-lock.test.cjs` exits 0 and prints an all-passed footer
    - The test asserts HANDOFF.json remains valid JSON after >=20 interleaved writes
    - Sanity: temporarily reverting Task 1 (bare write) makes the concurrency case fail - note this in the SUMMARY rather than committing the revert
    - No `HANDOFF.json.lock` file remains in the temp dir after the test completes
  </acceptance_criteria>
  <verify>
    <automated>node tests/handoff-write-lock.test.cjs</automated>
  </verify>
  <done>tests/handoff-write-lock.test.cjs passes: interleaved HANDOFF.json + STATE.md writers leave valid, parseable files and writeCheckpoint provably takes/releases the lock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| two plugin processes → same .planning/ file | `gsd` and `bm` copies (or two sessions) write HANDOFF.json/STATE.md concurrently on the local filesystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Tampering | checkpoint.cjs HANDOFF.json write | mitigate | Route through acquireStateLock(outPath) O_EXCL + atomic write (Task 1); interleaved-write test asserts valid JSON after >=20 concurrent writes (Task 2) |
| T-14-04 | Denial of Service | HANDOFF.json.lock left by a crashed writer | mitigate | Reuse acquireStateLock's existing 10s stale-lock reclaim + exit-time cleanup (#1916); do not reimplement. Lock fails OPEN on non-EEXIST so a lock error never blocks the write |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan - Node built-ins (`fs`, `path`, `os`) only; nothing to audit |
</threat_model>

<verification>
- `node tests/handoff-write-lock.test.cjs` passes.
- `node tests/checkpoint-write-guards.test.cjs` still passes (guards (a)/(b) preserved).
- No stale `.lock` files after the suite.
</verification>

<success_criteria>
COMPAT-03 gap closed: the only unprotected shared-state writer (HANDOFF.json) now serializes
through the same O_EXCL lock as STATE.md, and an automated coexistence test proves interleaved
writes stay uncorrupted.
</success_criteria>

<output>
Create `.planning/phases/14-backward-compatibility-and-coexistence/14-01-SUMMARY.md` when done.
</output>
