---
phase: 14-backward-compatibility-and-coexistence
plan: 03
type: execute
wave: 2
depends_on: [14-02]
files_modified:
  - bin/gsd-tools.cjs
  - hooks/run-bash-hook.cjs
  - hooks/hooks.json
  - tests/hook-single-fire.test.cjs
autonomous: true
requirements: [COMPAT-01, COMPAT-02, COMPAT-04]
must_haves:
  truths:
    - "Every merged hook goes through the election at one of two single dispatch points: the gsd-tools.cjs hook branches (session-start, post-tool-use, pre-compact, stop) AND the run-bash-hook.cjs bash dispatch (session-state, validate-commit, phase-boundary)"
    - "With both plugins active, each of the three named bash hooks (session-state, validate-commit, phase-boundary) fires exactly once - literally, not just in effect - because the gsd copy yields via run-bash-hook.cjs when the bm marker is present"
    - "A gsd-only session (no marker) runs every hook exactly as it does today (COMPAT-01)"
    - "With the marker present, the gsd copy performs no state-mutating checkpoint write and does not run its bash hooks (COMPAT-02)"
    - "The gsd session-start hook emits a non-blocking deprecation nudge mentioning /bm: and v5.0, even when it yields the stateful work (COMPAT-04, D-05 nudge, D-06 exemption)"
  artifacts:
    - path: "bin/gsd-tools.cjs"
      provides: "election at the top of each state-mutating hook branch + the sentinel-wrapped nudge"
      contains: "shouldYield"
    - path: "hooks/run-bash-hook.cjs"
      provides: "election at the single bash-hook dispatch point (covers session-state, validate-commit, phase-boundary)"
      contains: "shouldYield"
    - path: "tests/hook-single-fire.test.cjs"
      provides: "both-active single-fire (gsd-tools branches AND run-bash-hook) + SessionStart-residual + gsd-only no-op assertions"
      min_lines: 60
  key_links:
    - from: "bin/gsd-tools.cjs"
      to: "bin/lib/coexist.cjs"
      via: "require pluginIdentity/markBmActive/shouldYield"
      pattern: "coexist"
    - from: "hooks/run-bash-hook.cjs"
      to: "bin/lib/coexist.cjs"
      via: "require pluginIdentity/markBmActive/shouldYield"
      pattern: "coexist"
    - from: "tests/hook-single-fire.test.cjs"
      to: "hooks/run-bash-hook.cjs"
      via: "spawn the bash dispatch with/without a marker + assert single effective fire"
      pattern: "run-bash-hook"
---

<objective>
Wire the Plan 02 election into BOTH shared hook dispatch points so a both-active session
collapses to a single effective fire (COMPAT-02) while a gsd-only session is untouched
(COMPAT-01), and emit the non-blocking `/gsd:` -> `/bm:` deprecation nudge from the gsd
SessionStart hook, exempt from the yield (COMPAT-04, D-05/D-06).

Per the user's widened D-02 election scope, the election covers ALL merged hooks, not just
the state-mutating ones. Two dispatch points carry it: (1) `bin/gsd-tools.cjs case 'hook'`
for the four state-mutating branches (`session-start`, `post-tool-use`, `pre-compact`,
`stop`), and (2) `hooks/run-bash-hook.cjs` - the single launcher for the three bash hooks
(`gsd-session-state.sh`, `gsd-validate-commit.sh`, `gsd-phase-boundary.sh`). Wiring the
election into run-bash-hook.cjs is one extra wiring point that makes "fires exactly once"
literal for those three named bash hooks (satisfies SC2 directly). The D-03 accepted
SessionStart residual still applies: the first pre-marker event may double-fire once, then
the session is clean. The nudge is wrapped in exact-literal sentinels so Plan 04 can strip
it from the bm package.

Purpose: Turn the tested election primitive into live single-fire across every merged hook + surface the nudge.
Output: election in gsd-tools.cjs + run-bash-hook.cjs, a sentinel-wrapped nudge, a full
hooks.json classification table, and a single-fire test covering both dispatch points.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-backward-compatibility-and-coexistence/14-RESEARCH.md
@.planning/phases/14-backward-compatibility-and-coexistence/14-PATTERNS.md
@bin/lib/coexist.cjs
@hooks/run-bash-hook.cjs

<interfaces>
<!-- Verified from the codebase. Use directly - no exploration. -->

bin/lib/coexist.cjs (Plan 02):
  pluginIdentity(resolvedPath?) -> 'gsd' | 'bm'
  markBmActive(sessionId)      // bm self-announces; no-op on malformed id; refreshes mtime
  isBmActive(sessionId) -> bool
  shouldYield(identity, sessionId) -> bool   // 'gsd' && marker present

bin/gsd-tools.cjs `case 'hook'` (line 1259). Branches, all inside `case 'hook'`:
  - session-start (1261): reads stdin JSON -> hookInput.session_id (1262-1266);
      emits the resume directive via process.stdout.write(systemMsg) (1301) - the nudge template;
      then does workspace.json + handoff logic.
  - pre-compact (1334): checkpoint.writeCheckpoint(cwd, {source:'auto-compact'})
  - stop (1343): rate-limit nudge only (reads transcript) - reaches no writeCheckpoint,
      but classify it explicitly.
  - post-tool-use (1393): throttled checkpoint.writeCheckpoint(cwd, {source:'auto-postool'}) (1417),
      60s mtime throttle at 1410-1415.
  // session_id read pattern to reuse verbatim: lines 1262-1266.
  // Non-blocking stdout context pattern to mirror: line 1301.

hooks/run-bash-hook.cjs (single bash-hook launcher; invoked as its own process by the
  hooks.json inline resolver with the hook filename as argv[2]):
  - main() (line 103): reads process.argv[2] (e.g. 'gsd-session-state.sh'), resolveCandidates,
      runHook() -> cp.spawnSync('bash', [hookPath], { stdio: ['inherit','inherit','pipe'] }) (line 78).
  - __filename carries the /gsd/ vs /bm/ identity segment (Phase 13 stampHookFallback), so
      pluginIdentity(__filename) is valid here just like in gsd-tools.cjs.
  - IMPORTANT: gsd-validate-commit.sh returns exit 2 to BLOCK a non-conforming commit. When
      the gsd copy yields, the bm copy still runs validate-commit and returns its exit 2, so
      the block is single-sourced and never lost.

The three run-bash-hook.cjs targets registered in hooks/hooks.json:
  - gsd-session-state.sh   (SessionStart)                       - line 18
  - gsd-validate-commit.sh (PreToolUse, matcher Bash)           - line 88
  - gsd-phase-boundary.sh  (PostToolUse, matcher Write|Edit|MultiEdit) - line 130
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Single-fire test - spawn BOTH dispatch points (Wave 0)</name>
  <files>tests/hook-single-fire.test.cjs</files>
  <read_first>
    - tests/bm-parity.test.cjs (spawnSync of a node CLI + assertions on its effect) and its fail-loud footer
    - tests/checkpoint-write-guards.test.cjs lines 19-41 (harness + withTempDir)
    - bin/gsd-tools.cjs lines 1259-1424 (the four hook branches, especially post-tool-use writeCheckpoint at 1417 and the 60s throttle at 1410-1415)
    - hooks/run-bash-hook.cjs lines 44-146 (resolveCandidates + main; the bash dispatch to add the election to)
    - bin/lib/coexist.cjs (markBmActive to plant a marker for a chosen session_id)
  </read_first>
  <behavior>
    - gsd-tools no-op (COMPAT-01): with NO marker, spawning `node bin/gsd-tools.cjs hook post-tool-use` (stdin `{"session_id":"S1"}`) against a temp .planning/ writes/refreshes HANDOFF.json exactly as today.
    - gsd-tools single effective fire (COMPAT-02): plant a bm marker for session S2 via coexist.markBmActive('S2'), then spawn the gsd hook for S2; assert the gsd copy performs NO new HANDOFF.json write (mtime unchanged / file absent), i.e. it yielded.
    - run-bash-hook single effective fire (COMPAT-02, W2): with a bm marker present for a session, spawn `node hooks/run-bash-hook.cjs gsd-session-state.sh` with that session_id on stdin (repo path resolves to gsd identity) and assert the gsd copy YIELDS - it does not invoke the bash script, so its observable stdout effect is absent; with NO marker, the same dispatch DOES run the bash script (control). Cover at least session-state AND validate-commit so both named bash hooks are proven to fire exactly once.
    - SessionStart residual bounded: assert shouldYield('gsd', S3) === false when the marker has not yet landed (models the at-most-one gsd fire before bm's marker), and === true once markBmActive(S3) is called.
    - Because the repo copy resolves identity to 'gsd' (path has no /bm/), every spawned dispatch is the gsd copy - exactly the copy whose yield we must prove.
  </behavior>
  <action>
    Create tests/hook-single-fire.test.cjs with the zero-dep harness. Use a temp project
    (mkdtempSync) seeded with `.planning/STATE.md` and a non-trivial HANDOFF context so the
    checkpoint guards pass. For the gsd-tools no-op case, delete any existing marker, spawnSync
    the repo's `bin/gsd-tools.cjs hook post-tool-use` with a session_id on stdin and assert
    HANDOFF.json exists/updates. For the gsd-tools yield case, call coexist.markBmActive(sessionId)
    to plant the marker BEFORE spawning, force the throttle open (remove HANDOFF.json or
    back-date its mtime beyond 60s), spawn the gsd hook, and assert no new write happened.
    For the run-bash-hook cases, spawnSync `node hooks/run-bash-hook.cjs <hook>.sh` with a
    session_id on stdin: once with no marker (assert the bash hook's observable effect is
    present), once after markBmActive (assert the effect is absent - the gsd copy yielded). Use
    gsd-session-state.sh and gsd-validate-commit.sh as the two proven bash hooks; pick an
    observable signal that does not require bash to be present on every CI runner (skip/guard
    the case if `bash` is unavailable, matching how existing tests gate on optional tooling).
    Add direct shouldYield truth-table assertions for the residual bound. Clean up markers and
    temp dirs. Fail-loud footer with `process.exit(1)`. This test is RED until Task 2.
  </action>
  <acceptance_criteria>
    - `node tests/hook-single-fire.test.cjs` is RED before Task 2 (the gsd copy still writes / still runs the bash hook despite the marker) and GREEN after
    - The test proves the gsd copy makes NO HANDOFF.json write when the marker is present (gsd-tools branch)
    - The test proves the gsd run-bash-hook dispatch does NOT run its bash hook when the marker is present, and DOES when absent (covers session-state + validate-commit)
    - The test proves the gsd copy DOES write when no marker is present (COMPAT-01 no-op)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('tests/hook-single-fire.test.cjs','utf8'); if(!/markBmActive/.test(t)||!/post-tool-use|session-start/.test(t)||!/run-bash-hook/.test(t)){console.error('single-fire test does not exercise marker + both dispatch points');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <done>tests/hook-single-fire.test.cjs asserts gsd-only no-op, both-active single fire for the gsd-tools branches AND the run-bash-hook bash dispatch, and the bounded SessionStart residual; RED until the election is wired.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the election into both dispatch points + classify every hook</name>
  <files>bin/gsd-tools.cjs, hooks/run-bash-hook.cjs, hooks/hooks.json</files>
  <read_first>
    - bin/gsd-tools.cjs lines 1259-1425 (all four hook branches + session_id read at 1262-1266)
    - hooks/run-bash-hook.cjs full file (main at 103, the bash spawn at 77-91, the stdin `inherit` on line 79)
    - bin/lib/coexist.cjs (the four helpers)
    - .planning/phases/14-backward-compatibility-and-coexistence/14-PATTERNS.md "bin/gsd-tools.cjs (MODIFY ...)" and "hooks/hooks.json (MODIFY - audit only)" sections
    - hooks/hooks.json (every entry, to classify)
  </read_first>
  <behavior>
    - gsd-tools branches: at the top of each state-mutating branch (session-start, post-tool-use, pre-compact, stop), resolve identity = pluginIdentity(__filename); if 'bm', markBmActive(sessionId) FIRST/synchronously; if shouldYield('gsd', sessionId), skip the state-mutating work and exit that branch cleanly (exit 0, no side effects).
    - run-bash-hook.cjs: before running the bash child, resolve identity = pluginIdentity(__filename); read session_id from stdin (buffer it, do not consume it from the bash child); if 'bm', markBmActive(sessionId) FIRST; if shouldYield('gsd', sessionId), exit 0 WITHOUT running bash (yield). Otherwise run bash exactly as today, forwarding the buffered stdin bytes to the child.
    - No behavior change when no marker exists: gsd-only path is byte-identical in effect to today, for both dispatch points (fail-open when session_id is missing/malformed, since shouldYield returns false).
    - validate-commit's exit-2 block is preserved: when the gsd copy yields, the bm copy runs validate-commit and returns exit 2, so a non-conforming commit is still blocked exactly once.
  </behavior>
  <action>
    In bin/gsd-tools.cjs `case 'hook'`, `require('./lib/coexist.cjs')` for pluginIdentity,
    markBmActive, shouldYield. Ensure each state-mutating branch has the `session_id` from
    stdin (session-start already reads it at 1262-1266; for post-tool-use/pre-compact/stop read
    it from the same stdin drain they already perform, or parse once). At the TOP of each of
    the four branches: compute `identity = pluginIdentity(__filename)`; if `identity === 'bm'`
    call `markBmActive(sessionId)` before any other work; then if `shouldYield('gsd', sessionId)`
    short-circuit that branch's checkpoint/workspace side effects without writing. The `stop`
    branch writes no shared state (rate-limit read only), but add the election guard for
    uniformity and document why it is benign.

    In hooks/run-bash-hook.cjs, `require('../bin/lib/coexist.cjs')` for pluginIdentity,
    markBmActive, shouldYield. In main(), before the candidate loop runs bash: read stdin once
    into a buffer using the fail-open pattern from gsd-tools.cjs (try `fs.readFileSync(0,'utf-8')`
    in a try/catch; parse JSON; `sessionId = parsed.session_id`); compute
    `identity = pluginIdentity(__filename)`; if `identity === 'bm'` call `markBmActive(sessionId)`;
    if `shouldYield('gsd', sessionId)` `process.exit(0)` WITHOUT invoking bash. When not
    yielding, forward the buffered stdin bytes to the bash child (pass the buffer via the
    spawnSync `input` option and change the child's stdin from `'inherit'` to a pipe) so the
    bash hooks that read stdin still receive it unchanged. Do not alter the Cygwin fork-retry
    logic or the exit-code passthrough.

    Add a classification note in hooks/hooks.json's top-level `description` (or an adjacent
    doc list in the plan) covering EVERY hooks.json entry. Frame it as: both shared dispatch
    points now enforce the election at their single entry, so any hook added through either
    dispatch is automatically elected; the only un-elected entries are the standalone read-only
    JS detectors (idempotent, mutate no shared state, so double-running is a no-op), and a new
    detector-style hook must be added to this table. Do NOT touch the standalone JS hook
    scripts. No GSD phase/wave bookkeeping in any code or comment.

    Classification table to embed (every hooks.json entry, with one-line evidence):

    Elected via `gsd-tools.cjs hook` dispatch:
    - session-start   - state-mutating: writes workspace.json + HANDOFF via writeCheckpoint. ELECTED.
    - post-tool-use   - state-mutating: throttled writeCheckpoint (HANDOFF.json). ELECTED.
    - pre-compact     - state-mutating: writeCheckpoint on compact. ELECTED.
    - stop            - read-only: rate-limit nudge from transcript, no shared-state write. Elected for uniformity (benign).

    (No pre-tool-use branch exists in this dispatch: PreToolUse is served by the standalone JS guards listed below, and by gsd-validate-commit.sh via run-bash-hook.)

    Elected via `run-bash-hook.cjs` dispatch (W2 - one extra wiring point):
    - gsd-session-state.sh   - reads .planning/STATE.md, prints the session summary to stdout; double-emission is duplicate noise. ELECTED - fires exactly once.
    - gsd-validate-commit.sh - inspects the pending commit message, returns exit 2 to BLOCK a non-conforming commit; double-run = duplicate validation. ELECTED - validates/blocks exactly once (bm copy owns the block when gsd yields).
    - gsd-phase-boundary.sh  - reads planning state to detect a phase-boundary crossing, emits an advisory; double-run = duplicate advisory. ELECTED - fires exactly once.

    Read-only advisory JS detectors (NOT elected; idempotent, mutate no shared state, safe to double-run):
    - gsd-shadowing-sdk-detector.js (SessionStart) - scans for a shadowing SDK install, warns only.
    - gsd-staleness-reminder.js (SessionStart)     - compares planning mtimes, emits a reminder.
    - gsd-prompt-guard.js (PreToolUse Edit|Write)  - guards prompt-injection patterns, advisory.
    - gsd-workflow-guard.js (PreToolUse Edit|Write) - enforces GSD-workflow entry, advisory.
    - gsd-read-guard.js (PreToolUse Edit|Write)     - guards edits to protected paths, advisory.
    - gsd-auth-detector.js (PostToolUse Bash)       - detects auth prompts in output, advisory.
    - gsd-read-injection-scanner.js (PostToolUse Read) - scans read content for injection, advisory.
    - gsd-context-monitor.js (PostToolUse / SubagentStop) - reports context usage, advisory.

    Note in the plan/SUMMARY that "fires exactly once" is now LITERAL for the three named bash
    hooks (session-state, validate-commit, phase-boundary), not merely "in effect".
  </action>
  <acceptance_criteria>
    - `node tests/hook-single-fire.test.cjs` exits 0 (GREEN)
    - `grep -n "coexist" bin/gsd-tools.cjs` shows the require and the election calls
    - `grep -c "shouldYield" bin/gsd-tools.cjs` is >= 4 (one guard per state-mutating branch)
    - `grep -n "coexist" hooks/run-bash-hook.cjs` shows the require and the election call
    - `grep -n "shouldYield" hooks/run-bash-hook.cjs` present (the bash-dispatch yield)
    - gsd-only regression: `node tests/checkpoint-write-guards.test.cjs` still passes
    - hooks.json (or the plan) classification enumerates every entry: the three run-bash-hook targets (session-state, validate-commit, phase-boundary) each with one-line evidence, plus each read-only JS detector
    - `node -e "require('./hooks/run-bash-hook.cjs')"` is NOT used as a check (it calls main and exits); instead assert via the single-fire test that it yields under a marker
  </acceptance_criteria>
  <verify>
    <automated>node tests/hook-single-fire.test.cjs && node tests/checkpoint-write-guards.test.cjs && grep -q "shouldYield" hooks/run-bash-hook.cjs && echo PASS</automated>
  </verify>
  <done>Both dispatch points run the election; the gsd copy yields (no checkpoint write; no bash-hook run) when the bm marker is present and is a no-op otherwise; validate-commit's block is preserved via the bm copy; hooks.json carries the full election classification for every entry.</done>
</task>

<task type="auto">
  <name>Task 3: Emit the deprecation nudge from gsd SessionStart, exempt from the yield</name>
  <files>bin/gsd-tools.cjs</files>
  <read_first>
    - bin/gsd-tools.cjs lines 1261-1333 (session-start branch: the stdin read at 1262-1266 and the existing stdout context emit at 1301)
    - .planning/phases/14-backward-compatibility-and-coexistence/14-RESEARCH.md "Pattern 3: Non-blocking nudge from SessionStart" + "Anti-Patterns" (do NOT use systemMessage JSON field)
    - .planning/phases/14-backward-compatibility-and-coexistence/14-PATTERNS.md "Non-blocking nudge template" note
    - /Users/jnuyens/.claude/CLAUDE.md (no em-dashes; plain non-marketing copy)
  </read_first>
  <behavior>
    - On SessionStart, when identity === 'gsd', the hook writes a one-time nudge to stdout that mentions `/bm:` and the v5.0 retirement, then continues.
    - The nudge is emitted BEFORE / independent of the shouldYield decision (D-06 exemption): a both-active gsd session still shows it even though it yields the stateful session-state work.
    - The nudge is non-blocking (stdout only; never systemMessage; never exit non-zero) and appears at most once per session (SessionStart fires once).
    - The nudge emit block is bracketed by exact-literal sentinel comments so Plan 04 can strip it from the bm package.
  </behavior>
  <action>
    In the session-start branch of bin/gsd-tools.cjs, add a nudge emit guarded by
    `pluginIdentity(__filename) === 'gsd'`, placed so it runs before the election's yield for
    the stateful work (D-06 exemption). Emit via `process.stdout.write(...)` matching the
    existing resume-directive pattern at line 1301 - NOT the `systemMessage` JSON field (the
    research anti-pattern). Wrap the emit in two exact-literal sentinel comments on their own
    lines, `// BM-NUDGE-START` and `// BM-NUDGE-END`, with nothing between them except the
    guarded emit, so Plan 04's suppressNudge transform can remove the block by exact literal.
    The message must be plain and non-marketing, contain the literal `/bm:` and the literal
    `v5.0`, state that both prefixes work through the 4.x line and `/gsd:` retires at v5.0,
    and contain NO em-dashes or en-dashes. Keep it to one or two sentences. Ensure the nudge
    never blocks: it writes to stdout and the branch continues; no throw, no exit(1). Note that
    the D-06 exemption means the nudge is emitted even in a both-active gsd session where the
    session-start election yields the stateful work. Do not add any GSD phase/wave bookkeeping
    to the emitted text or the code comments.
  </action>
  <acceptance_criteria>
    - `printf '{"session_id":"NUDGE1","source":"startup"}' | node bin/gsd-tools.cjs hook session-start` stdout contains both `/bm:` and `v5.0`
    - The stdout nudge contains no em-dash or en-dash character
    - `grep -n "BM-NUDGE-START" bin/gsd-tools.cjs` and `grep -n "BM-NUDGE-END" bin/gsd-tools.cjs` are both present, with the guarded stdout emit between them, each sentinel on its own line
    - The command exits 0 (non-blocking)
  </acceptance_criteria>
  <verify>
    <automated>out=$(printf '{"session_id":"NUDGE1","source":"startup"}' | node bin/gsd-tools.cjs hook session-start 2>/dev/null); printf '%s' "$out" | grep -q "/bm:" && printf '%s' "$out" | grep -q "v5.0" && printf '%s' "$out" | node -e "let s='';process.stdin.on('data',c=>s+=c).on('end',()=>process.exit(/[\u2014\u2013]/.test(s)?1:0))" && echo PASS || { echo FAIL; exit 1; }</automated>
  </verify>
  <done>The gsd SessionStart hook emits a non-blocking, em-dash-free nudge mentioning /bm: and v5.0, sentinel-wrapped on its own lines, exempt from the yield; command exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook stdin JSON → dispatch | session_id and source arrive from Claude Code; both dispatch points compose a marker path from session_id (validated in Plan 02) |
| gsd copy ↔ bm copy | two byte-different hook command strings both fire; the election at each of the two dispatch points decides which runs the work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Denial of Service (self) | a merged hook skips the election and double-fires | mitigate | Both shared dispatch points (gsd-tools.cjs hook AND run-bash-hook.cjs) enforce the single-sourced coexist election; every hooks.json entry is classified (elected via one of the two dispatches, or read-only advisory / idempotent), so a future hook cannot silently skip it (Task 2). "Exactly once" is literal for the three named bash hooks |
| T-14-12 | Tampering | run-bash-hook consumes stdin so the bash hook loses its input | mitigate | run-bash-hook buffers stdin once and forwards the exact bytes to the bash child via the spawnSync `input` option; validate-commit's exit-2 block is preserved through the bm copy (Task 2) |
| T-14-03 | Spoofing | forged marker forces the gsd copy to yield | accept | Local user-owned temp only; yielding hands the identical-format write to the bm copy under the Plan 01 lock, and the bm copy runs the bash hooks; the read-only nudge still emits. No data loss (inherited from Plan 02 threat model) |
| T-14-07 | Denial of Service | nudge blocks the command | mitigate | Nudge is stdout-only and never exits non-zero; acceptance asserts exit 0 (Task 3) |
| T-14-01 | Tampering | session_id → marker path | mitigate | session_id validated in coexist.markBmActive/isBmActive (Plan 02); both dispatch points only pass it through |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs - Node built-ins only |
</threat_model>

<verification>
- `node tests/hook-single-fire.test.cjs` passes (single-fire for both dispatch points + gsd-only no-op).
- `node tests/checkpoint-write-guards.test.cjs` passes (no regression).
- `node tests/coexist.test.cjs` still passes (Plan 02 unaffected).
- `grep -q "shouldYield" hooks/run-bash-hook.cjs` (bash dispatch elected).
- SessionStart nudge appears on stdout with /bm: + v5.0 and no em-dashes.
</verification>

<success_criteria>
Both-active sessions collapse to a single effective fire across EVERY merged hook: the four
gsd-tools state-mutating branches yield, and the three named bash hooks (session-state,
validate-commit, phase-boundary) each fire exactly once via the run-bash-hook election.
gsd-only sessions are unchanged (COMPAT-01), and every gsd SessionStart surfaces the
non-blocking /bm: + v5.0 nudge even when yielding (COMPAT-02, COMPAT-04). The nudge block is
sentinel-wrapped for Plan 04 suppression.
</success_criteria>

<output>
Create `.planning/phases/14-backward-compatibility-and-coexistence/14-03-SUMMARY.md` when done.
</output>
