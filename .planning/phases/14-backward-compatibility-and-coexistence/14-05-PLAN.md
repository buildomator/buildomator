---
phase: 14-backward-compatibility-and-coexistence
plan: 05
type: execute
wave: 4
depends_on: [14-01, 14-02, 14-03, 14-04]
files_modified:
  - .github/workflows/check-drift.yml
  - .github/workflows/install-smoke.yml
  - .planning/phases/14-backward-compatibility-and-coexistence/14-VALIDATION.md
autonomous: true
requirements: [COMPAT-01, COMPAT-02, COMPAT-03, COMPAT-04]
must_haves:
  truths:
    - "Every coexistence test runs as a CI gate on push, so a regression in single-fire, write-locking, or nudge suppression blocks the release"
    - "install-smoke proves a both-plugins single-fire at the package level (bm marker present -> the gsd copy yields)"
    - "The phase VALIDATION.md per-task map (pre-filled at planning time) is confirmed complete and its Status column reflects the executed results"
  artifacts:
    - path: ".github/workflows/check-drift.yml"
      provides: "run: steps for the four new coexistence tests"
      contains: "coexist.test.cjs"
    - path: ".github/workflows/install-smoke.yml"
      provides: "both-plugins single-fire smoke step in bm-package-smoke"
      contains: "bm-active"
    - path: ".planning/phases/14-backward-compatibility-and-coexistence/14-VALIDATION.md"
      provides: "confirmed per-task verification map, nyquist_compliant: true"
      contains: "nyquist_compliant: true"
  key_links:
    - from: ".github/workflows/check-drift.yml"
      to: "tests/coexist.test.cjs, tests/hook-single-fire.test.cjs, tests/handoff-write-lock.test.cjs, tests/nudge-emission.test.cjs"
      via: "run: node tests/<file>"
      pattern: "tests/.*\\.test\\.cjs"
---

<objective>
Turn the four coexistence tests into release gates and prove single-fire at the package
level. Extend the existing `bm-build-drift` job in `check-drift.yml` (or add a sibling
`bm-coexistence` job) with a `run:` step per new test, and extend the `bm-package-smoke` job
in `install-smoke.yml` with a both-plugins single-fire smoke (plant a bm-active marker, run
the gsd hook, assert it yields). Confirm `14-VALIDATION.md` (already populated at planning
time) and flip its per-task Status column to reflect the executed results.

Purpose: Make coexistence correctness a standing CI gate, not a one-time check.
Output: extended CI jobs and a confirmed phase validation contract.
</objective>

<execution_context>
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/workflows/execute-plan.md
@/Users/jnuyens/.claude/plugins/cache/gsd-plugin/gsd/4.0.4/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-backward-compatibility-and-coexistence/14-RESEARCH.md
@.planning/phases/14-backward-compatibility-and-coexistence/14-VALIDATION.md

<interfaces>
<!-- Verified from the codebase. Use directly - no exploration. -->

.github/workflows/check-drift.yml - `bm-build-drift` job (line 127). Existing run: steps:
  - node tests/build-bm-drift.test.cjs
  - node bin/build-bm.cjs --check
  - node tests/bm-parity.test.cjs
  - node bin/validate-plugin.cjs dist/bm/.claude-plugin/plugin.json
  - node bin/maintenance/check-version-alignment.cjs
  (zero-dep; no npm ci; checkout@v4 + setup-node@22)

.github/workflows/install-smoke.yml - `bm-package-smoke` job (line 111). Runs in a
  debian:trixie container; steps set CLAUDE_PLUGIN_ROOT="$PWD/dist/bm" and exercise the bm
  wrapper + a primary-path-wins hook tripwire (lines 141-165). Extend here.

New tests to gate (from Plans 01-04):
  tests/coexist.test.cjs, tests/hook-single-fire.test.cjs,
  tests/handoff-write-lock.test.cjs, tests/nudge-emission.test.cjs

14-VALIDATION.md is already populated at planning time (per-task map for every task in plans
  01-05, quick/full run commands, nyquist_compliant: true). This plan confirms it and updates
  the Status column; it does NOT re-template it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate the four coexistence tests in check-drift.yml</name>
  <files>.github/workflows/check-drift.yml</files>
  <read_first>
    - .github/workflows/check-drift.yml lines 127-160 (the bm-build-drift job: checkout, setup-node@22, the existing run: steps)
    - .planning/phases/14-backward-compatibility-and-coexistence/14-RESEARCH.md "Validation Architecture" -> Sampling Rate (per-wave-merge includes build-bm --check + bm-parity)
    - .planning/phases/14-backward-compatibility-and-coexistence/14-PATTERNS.md "CI (MODIFY ...)" section
  </read_first>
  <behavior>
    - Each of the four new coexistence tests runs as its own `run:` step on push.
    - The job keeps the existing build-bm --check and bm-parity steps (nudge suppression must not break parity).
    - No npm ci added (all tests are zero-dep Node built-ins).
  </behavior>
  <action>
    In .github/workflows/check-drift.yml, add four `run:` steps that each execute one new
    test: `node tests/coexist.test.cjs`, `node tests/hook-single-fire.test.cjs`,
    `node tests/handoff-write-lock.test.cjs`, `node tests/nudge-emission.test.cjs`. Place
    them in the `bm-build-drift` job after the existing `bm-parity` step, OR add a sibling
    `bm-coexistence` job that reuses the same checkout@v4 + setup-node@22 preamble (no npm
    ci). Give each step a descriptive `name:` referencing the COMPAT requirement it gates
    (COMPAT-01/02 for coexist + hook-single-fire, COMPAT-03 for handoff-write-lock, COMPAT-04
    for nudge-emission). Keep the existing steps intact. Match the file's YAML indentation
    exactly. No em-dashes in step names.
  </action>
  <acceptance_criteria>
    - `grep -c "coexist.test.cjs\|hook-single-fire.test.cjs\|handoff-write-lock.test.cjs\|nudge-emission.test.cjs" .github/workflows/check-drift.yml` is 4
    - The existing `node bin/build-bm.cjs --check` and `node tests/bm-parity.test.cjs` steps are still present
    - The workflow parses as valid YAML
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/check-drift.yml'))" && test "$(grep -c 'coexist.test.cjs\|hook-single-fire.test.cjs\|handoff-write-lock.test.cjs\|nudge-emission.test.cjs' .github/workflows/check-drift.yml)" = "4" && echo PASS</automated>
  </verify>
  <done>All four coexistence tests run as CI gates in check-drift.yml; existing bm gates preserved; YAML valid.</done>
</task>

<task type="auto">
  <name>Task 2: Both-plugins single-fire smoke in install-smoke.yml + confirm VALIDATION.md</name>
  <files>.github/workflows/install-smoke.yml, .planning/phases/14-backward-compatibility-and-coexistence/14-VALIDATION.md</files>
  <read_first>
    - .github/workflows/install-smoke.yml lines 111-165 (bm-package-smoke job: container, prereqs, CLAUDE_PLUGIN_ROOT="$PWD/dist/bm" steps, the primary-path-wins tripwire)
    - bin/lib/coexist.cjs (markBmActive/isBmActive - how the smoke plants a marker)
    - bin/gsd-tools.cjs hook post-tool-use branch (what "yield" suppresses: the HANDOFF.json write)
    - .planning/phases/14-backward-compatibility-and-coexistence/14-VALIDATION.md (already filled - the file to confirm/finalize, not re-template)
  </read_first>
  <behavior>
    - The smoke plants a bm-active marker for a session_id, runs the gsd copy's `hook post-tool-use` for that session against a synthetic project, and asserts the gsd copy did NOT write HANDOFF.json (it yielded) - the package-level single-fire proof.
    - A control assertion: with no marker, the same gsd hook DOES write HANDOFF.json (proves the smoke is not a false pass).
    - 14-VALIDATION.md is confirmed complete (already filled at planning time: per-task map for every task in plans 01-05, quick/full commands, nyquist_compliant: true); its Per-Task Verification Map Status column is updated to green for the tasks that passed this run.
  </behavior>
  <action>
    In .github/workflows/install-smoke.yml `bm-package-smoke`, add a step (after the existing
    tripwire) that creates a synthetic project with a `.planning/` + STATE.md, runs the gsd
    copy (repo `bin/gsd-tools.cjs`, whose path identity is 'gsd') with `hook post-tool-use`
    and a session_id on stdin twice: first with no marker (assert HANDOFF.json is written),
    then after planting a marker via
    `node -e "require('$GITHUB_WORKSPACE/bin/lib/coexist.cjs').markBmActive('SID')"` (assert
    HANDOFF.json is NOT re-written, i.e. the gsd copy yielded). Reuse the surrounding
    container + path conventions; keep the gsd install-smoke job byte-untouched (only extend
    bm-package-smoke).

    Then CONFIRM 14-VALIDATION.md rather than re-fill it: it was populated at planning time,
    so verify it still enumerates every task in plans 01-05 with a concrete automated command,
    that the quick/full commands and `nyquist_compliant: true` are present, and flip the
    Per-Task Verification Map Status column entries to green (and set `wave_0_complete: true`
    in the frontmatter) now that the Wave 0 test files exist and pass. Do NOT blank,
    re-template, or remove any pre-filled rows. No em-dashes.
  </action>
  <acceptance_criteria>
    - The install-smoke workflow parses as valid YAML
    - `grep -Eq "markBmActive|bm-active|coexist" .github/workflows/install-smoke.yml` succeeds (the marker-plant step exists)
    - The smoke has both a marker-present (yield) assertion and a no-marker control assertion
    - `grep -q "nyquist_compliant: true" .planning/phases/14-backward-compatibility-and-coexistence/14-VALIDATION.md`
    - The VALIDATION.md per-task map still has a row for every task in plans 01-05 with a concrete automated command
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/install-smoke.yml'))" && grep -q "nyquist_compliant: true" .planning/phases/14-backward-compatibility-and-coexistence/14-VALIDATION.md && grep -Eq "markBmActive|bm-active|coexist" .github/workflows/install-smoke.yml && echo PASS</automated>
  </verify>
  <done>install-smoke.yml proves package-level single-fire (gsd yields when the bm marker is present, writes when absent); 14-VALIDATION.md is confirmed filled and marked nyquist_compliant with the Status column reflecting the run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed source → CI gate | CI is the release gate; a coexistence regression that escapes the gate ships to users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10 | Repudiation / Tampering | a coexistence regression ships because no gate covers it | mitigate | Every coexistence test runs on push in check-drift.yml (Task 1); install-smoke adds a package-level single-fire smoke with a no-marker control to catch false passes (Task 2) |
| T-14-11 | Denial of Service | a flaky marker/temp-dir smoke blocks CI on unrelated PRs | accept | The smoke uses a fixed session_id and a fresh synthetic project per run; markers live in the ephemeral CI temp dir and do not persist across jobs |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs - the jobs run zero-dep Node built-ins (no npm ci) |
</threat_model>

<verification>
- `check-drift.yml` and `install-smoke.yml` both parse as valid YAML.
- The four coexistence tests appear as `run:` steps in check-drift.yml.
- install-smoke has a marker-present (yield) assertion and a no-marker control.
- 14-VALIDATION.md is confirmed complete and `nyquist_compliant: true`.
</verification>

<success_criteria>
Coexistence correctness (COMPAT-01..04) is a standing release gate: the four tests run on
every push, a package-level both-plugins single-fire smoke proves the gsd copy yields, and the
phase validation contract is complete.
</success_criteria>

<output>
Create `.planning/phases/14-backward-compatibility-and-coexistence/14-05-SUMMARY.md` when done.
</output>
